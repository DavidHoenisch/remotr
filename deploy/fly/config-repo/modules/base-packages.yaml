configurations:
  - name: base-packages
    description: Starter fleet configuration (edit modules and manifest, then compose)
    targetDistros:
      - Debian
      - Ubuntu
      - Arch
    packages:
      - name: curl
        present: true
        packageManager: apt
      - name: curl
        present: true
        packageManager: pacman
