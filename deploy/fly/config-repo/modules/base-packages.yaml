kind: module
schemaVersion: 1
configurations:
  - name: base-packages
    description: Starter fleet configuration (edit modules and manifest, then render)
    targetDistros:
      - Debian
      - Ubuntu
      - Arch
    targetArch:
      - x86
    resources:
      - kind: package
        name: curl
        lifecycle: present
        present: true
