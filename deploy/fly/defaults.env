# Default Docker Hub image for Fly bootstrap (override with REMOTR_IMAGE).
# Published by .github/workflows/remotr-server-docker.yml
REMOTR_DOCKER_USER=dh1689
REMOTR_IMAGE_TAG=latest

# Fly org is account-specific — set REMOTR_FLY_ORG when running bootstrap
# (e.g. REMOTR_FLY_ORG=archangel). Not defaulted here.
