# Fly bootstrap builds from checked-out source by default.
# Set REMOTR_IMAGE to a trusted digest-pinned image and REMOTR_BUILD_FROM_SOURCE=0
# only when you intentionally want to deploy a prebuilt image.
