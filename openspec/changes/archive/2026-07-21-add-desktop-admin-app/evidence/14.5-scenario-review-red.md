# 14.5 Desktop scenario release review — red evidence

- Verification IDs: every `OS-DOA`, `OS-DFV`, and `OS-DFA` scenario.
- Public seam: the machine-readable final scenario review joined to the live
  OpenSpec inventory, parity inventory, package target policy, and approved
  evidence exceptions.
- Selected layers: focused release-review contract, the already-passed final
  desktop/native selectors, strict traceability/OpenSpec validation, and the
  complete root regression suite.

## Intended red

Command:

`env GOCACHE=/tmp/remotr-14.5-gocache mise exec -- go test ./internal/desktoplayout -run '^TestEveryDesktopScenarioHasPassingReleaseReview$' -count=1`

The focused contract failed because the final machine-readable review did not
yet exist. The contract requires all 83 canonical desktop scenarios exactly
once, a passing selector or registered exception for each, and release claims
that exactly match the live partial-parity and unsigned Linux/amd64 DEB
inventories. This is the intended final-review red.

## Green and audit result

- `14.5-scenario-review.json` contains all 83 canonical scenarios exactly once:
  29 `OS-DFA`, 35 `OS-DFV`, and 19 `OS-DOA` records.
- Every record is `passed` with one or more selectors exercised by final gates
  14.1, 14.2, or 14.4. No desktop scenario uses an evidence exception.
- The focused release-review contract and complete `internal/desktoplayout`
  package pass.
- Strict OpenSpec validation, traceability lint, and the live CLI parity-drift
  selector pass after the review was added.
- Root regression gate 14.3 remains the passing server/agent/CLI evidence for
  this same tree.

The review is mechanically joined to the live inventories. It therefore
retains the `partial` parity claim and its 12 planned CLI workflows, exactly one
Linux/amd64 DEB, unsigned CI-development publication, no signed output, and
`releaseEligible: false`. No first-release gap, macOS/Windows target, extra
architecture, or extra package format is advertised.

The repository-wide traceability manifest remains `planned` for this change on
purpose: the global advertisement gate must continue to reject production
desktop advertisement while licensing/signing policy and `releaseEligible`
remain unresolved. The final review records the passing implementation evidence
without weakening that distribution guard.
