# Task 5.2 semantic shell red evidence

- Verification IDs: `OS-DFV-001`, `OS-DFV-002`
- Public seam: rendered desktop component/user interaction
- Test layers: Testing Library semantic roles/names, pointer navigation,
  keyboard activation, persistent context, and deterministic minimum viewport
- Command: `pnpm test -- --run src/shell/AppShell.test.tsx`
- Result: intended red (`exit 1`)

Vitest reports that `./AppShell` does not exist. The committed component test
requires one persistent shell for Overview, Endpoints, Fleets, Change requests,
Diagnostics, and Activity; connection profile and Fleet scope must remain
visible while a single main region changes. Navigation must expose the Fleet
management and Operations groups and must not open additional windows.

At the 1100-by-720 logical minimum, every primary navigation control, the
current-page search input, a row-action control, and the topmost overlay close
control must remain visible and enabled. The close control must accept keyboard
focus and activation. Task 5.3 adds only the shell structure, styling, and
overlay behavior needed to make this slice green.
