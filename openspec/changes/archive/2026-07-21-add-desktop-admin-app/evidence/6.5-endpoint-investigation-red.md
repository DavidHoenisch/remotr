# Task 6.5 Endpoint investigation red evidence

- Verification IDs: `OS-DFV-016`, `OS-DFV-017`, `OS-DFV-018`, `OS-DFV-019`
- Public seam: rendered App inventory-to-investigation user flow with an
  injected deterministic Endpoint-detail loader
- Test layers: semantic pointer/keyboard interaction, independently known
  partial evidence, controlled promise ordering, focus restoration, and DOM
  scroll/filter/sort state
- Command: `pnpm exec vitest run src/endpoints/EndpointInvestigation.test.tsx`
- Result: intended red (`1` file, `3` failing tests)

All three flows reach the real Endpoint inventory, then fail because the App
does not yet mount Inspect actions from an injected Endpoint-detail loader.
Consequently no focused detail surface, tab set, close behavior, or obsolete
selection guard exists. The failure is at the user-visible seam rather than a
private hook or helper.

The first controlled detail fixture independently identifies
`endpoint-alpha` with its exact Fleet, compliance, freshness, agent version,
Release ref, and Labels. State and system evidence are ready, Schedule evidence
has a connection-classified failure, and Firewall evidence has an
authorization-classified failure. The assertions require all five labeled tabs
to remain usable and prohibit an Apply mutation control.

The context flow applies search and compliance filters, sorts Fleet, sets a
known horizontal scroll offset, and records the originating Inspect control.
Both the visible close action and Escape must dismiss only the detail surface,
restore focus to that control, and retain the exact query, selector, sort,
count, and scroll position.

The obsolete-selection flow holds `endpoint-alpha`, selects `endpoint-beta`,
resolves beta first, and then resolves alpha late. It requires only beta
identity and beta State evidence to render after both promises settle. Task 6.6
adds the focused overlay and the smallest generation/focus/tab behavior needed
to make this contract green.
