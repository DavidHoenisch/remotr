# 13.4 native Linux smoke gate — red evidence

- Verification ID: `OS-DOA-018`.
- Public seam: the built `remotr-desktop` executable and root native-smoke
  developer/CI command.
- Selected layers: exact embedded-version output, real GTK/WebKit launch with a
  `Remotr Desktop` X11 window, wrong-version/wrong-title/early-exit negatives,
  affected Linux CI ordering, nested desktop regressions, and the root suite.

## Intended red

Command:

`env GOCACHE=/tmp/remotr-gocache GOTMPDIR=/tmp/remotr-gotmp mise exec -- go test ./internal/desktoplayout -run '^TestNativeLinuxSmokeGatesBuiltArtifactAdvertisement$' -count=1`

The test failed because the Make smoke target, embedded-version linker flag,
headless native-launch harness, and CI smoke step were absent. The behavioral
cases could not run because `scripts/desktop-native-smoke.sh` did not exist.
This is the intended red at the native Linux artifact seam.

## Green verification

- The focused smoke-gate contract passed the exact-version/title case and
  rejected a mismatched embedded version, mismatched native title, and a
  process that exited before opening a window.
- `make desktop-smoke DESKTOP_VERSION=0.0.0-smoke.local` built the production
  Linux/amd64 Wails binary with `-X main.version=0.0.0-smoke.local` and the
  `webkit2_41` build tag.
- The exact binary reported `Remotr Desktop 0.0.0-smoke.local`, remained alive
  under isolated Xvfb 21.1.22, and exposed a native X11 window titled
  `Remotr Desktop`; the harness then terminated it and removed its temporary
  log.
- The affected Linux CI job installs Xvfb/Xauth/X11 utilities and runs
  `make desktop-smoke` with a commit-specific development-snapshot version.
  It contains no artifact-upload step, so no Linux package or architecture is
  advertised before the later package-specific evidence slices.
- POSIX shell syntax, the complete root desktop-layout package, the complete
  nested desktop Go suite, and strict OpenSpec validation passed.
- `mise exec -- make test` passed all 37 discovered fuzz seed corpora and the
  complete root Go suite.
