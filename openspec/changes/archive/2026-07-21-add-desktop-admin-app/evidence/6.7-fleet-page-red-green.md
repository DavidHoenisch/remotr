# Task 6.7 Fleet list/detail red-green evidence

- Verification IDs: `OS-DFV-020`, `OS-DFV-021`
- Public seam: rendered App Fleet inventory/detail user flow with an injected
  deterministic Fleet-detail loader
- Test layers: Testing Library semantic list/detail interaction,
  independently known member aggregates, summary-to-member filtering,
  explicit empty evidence, full frontend suite, production build, and
  real-browser behavior/geometry
- Red command: `pnpm exec vitest run src/fleets/FleetPage.test.tsx`
- Red result: intended failure (`1` file, `2` failing tests) because navigation
  mounted only the Fleets placeholder and exposed no `Fleet inventory` region
- Focused green command: `pnpm exec vitest run src/fleets/FleetPage.test.tsx`
- Focused green result: pass (`1` file, `2` tests)
- Regression command: `pnpm exec vitest run src/fleets/FleetPage.test.tsx src/endpoints/EndpointTable.test.tsx src/endpoints/EndpointTable.filtering.test.tsx src/overview/Overview.test.tsx`
- Regression result: pass (`4` files, `12` tests)
- Frontend command: `pnpm test`
- Frontend result: pass (`9` files, `24` tests)
- Production command: `pnpm build`
- Production result: pass (`tsc -b` and Vite production bundle; `1,787`
  modules transformed)

The Fleet inventory retains every server-provided Fleet, including `empty`
with a zero Endpoint count, and presents exact compliance, freshness, and
reported-agent distributions without inventing state. Selecting a Fleet loads
an identity-checked detail result while the list remains visible.

Mixed detail shows three member Endpoints plus compliance, freshness, and
agent-version distributions whose independently supplied counts each total the
same three rows. Compliance and freshness counts are controls that apply the
equivalent selector to the reused semantic Endpoint table; selecting the one
compliant member produces exactly `1 of 3 Endpoints` and only `endpoint-a`.
Opaque agent version identifiers retain their exact casing. The empty Fleet
opens a labeled authenticated-empty state with the service-provided no-
enrollment explanation rather than disappearing or becoming a not-found error.

At a controlled 1100-by-720 browser viewport, the document remained exactly at
the viewport extents and the 813-pixel Fleet surfaces stayed within the
scrolling content frame. The Fleet and Actions columns remained pinned at the
249-pixel left and 1,060-pixel right content edges while the middle summary
columns scrolled. The three distribution panels fit side-by-side in a
167-pixel block; the 1,284-pixel member table stayed inside its 777-pixel scroll
frame. A live compliant-count selection showed exactly `1 of 3 Endpoints`,
`compliant`, and only `endpoint-a`. The browser emitted no errors.
