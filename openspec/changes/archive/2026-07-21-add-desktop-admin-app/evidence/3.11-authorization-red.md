# Task 3.11 authorization-behavior red evidence

- Verification IDs: `OS-DOA-016`, `OS-DOA-017`
- Public seams: authenticated Admin API and desktop connected-session service
- Test layers: real Admin client over a controlled mTLS server, independently
  known read-only identity, server request counts, safe-error canary scan, and
  connected-session snapshot comparison
- Command: `go test ./... -run '^TestServerAuthoritativeForbiddenActionKeepsConnectedSession$' -count=1`
- Result: intended red (`exit 1`)

The compiler reports that the connected-session action boundary, contextual
Git-sync client call, and classified `ActionFailure`/`ActionForbidden` result do
not exist. The controlled test first authenticates exactly once as a read-only
Operator, deliberately invokes the typed action despite the unavailable role
hint, and requires the server's single `403 Forbidden` response to determine
the outcome. The final assertion keeps the complete connected session snapshot
unchanged, forbids alternate-identity retries, and rejects response-body,
state-directory, credential, and bootstrap canaries in the returned failure.

Task 3.12 retains the verified Admin client in the active session, presents the
server identity and roles as hints, sends the typed request with that same
credential, and maps a server `403` to a redacted authorization failure without
disconnecting or retrying.
