# Task 3.9 bridge-security red evidence

- Verification IDs: `OS-DOA-014`, `OS-DOA-015`
- Public seam: actual Wails bound object and release asset middleware
- Test layers: reflected bound-method inventory, safe view-model canary scan,
  and release remote-content request attempts
- Command: `go test ./... -run '^(TestWailsBindingAllowlist|TestBridgeViewModelsExcludeCredentialCanaries|TestBridgeSecurityRejectsRemoteNavigationAndContent)$' -count=1`
- Result: intended red (`exit 1`)

The bound object exposes only `GetApplicationInfo`, while the required
purpose-specific allowlist also includes profile loading/saving, connection,
bootstrap, and native external-link handoff. The same focused run proves that
the current view models do not serialize credential canaries and that the
release middleware rejects HTTP, HTTPS, protocol-relative, file, and data URL
content with `403 Forbidden`.

Task 3.10 adds only the allowlisted application methods, maps their results to
safe bridge view models, and routes validated HTTPS links to the native browser
boundary while retaining the embedded-only release content policy.
