# Task 4.9 Change request and Activity red/green evidence

- Verification IDs: `OS-DFV-022` through `OS-DFV-026`
- Public seams: typed Change request/Activity services and authenticated Admin
  API
- Test layers: real Admin client over controlled mTLS, exact read-only Change
  evidence, binding mutation inventory, cursor/filter query assertion,
  stable-order deduplication, bounded structured detail formatting, secret and
  response canaries, and Activity-local authorization failure
- Red command: `go test ./... -run '^(TestChangeRequestServicePreservesReadOnlyServerEvidence|TestActivityServiceUsesCursorFiltersOrderAndSafeDetails|TestActivityAuthorizationFailureRemainsLocal)$' -count=1 -timeout=30s`
- Red result: intended red (`exit 1`)

The compiler reports that the typed Change request service, Activity service,
and Activity page request contract do not exist. The controlled Change fixture
requires exact server risk, lifecycle, target count, latest update, resource,
target, approval, history, outcome, and warning evidence while the App method
inventory remains free of authorize, pause, resume, revoke, baseline, or
adoption mutations.

The Activity fixture requires the supplied server cursor and filters to appear
unchanged in a bounded request, removes both previously seen and within-page
duplicate IDs without sorting the remaining server order, retains only bounded
non-secret scalar details, and returns the next server cursor. A separate `403`
case must become an authorization-specific empty Activity section while the
permitted Change list remains usable.

## Green

- Green command: `go test ./... -run '^(TestChangeRequestServicePreservesReadOnlyServerEvidence|TestActivityServiceUsesCursorFiltersOrderAndSafeDetails|TestActivityAuthorizationFailureRemainsLocal)$' -count=1 -timeout=30s`
- Green result: pass (`exit 0`)

The implementation maps list and detail evidence into capped typed slices,
preserves server risk/lifecycle/order, validates exact resource identities, and
adds only read methods to the Wails allowlist. No Change lifecycle or baseline
mutation is bound.

Activity parses the optional absolute filters, fixes the page limit at 50,
passes the opaque cursor through unchanged, removes seen and within-page
duplicates without sorting, and returns the server's next cursor. Detail maps
only non-sensitive scalar fields with fixed count/value bounds. A forbidden
response becomes an authorization-specific empty Activity result and does not
affect independently permitted services.
