# Task 3.1 profile-service red evidence

- Verification IDs: `OS-DOA-003`, `OS-DOA-004`, `OS-DOA-005`
- Public seam: desktop profile service
- Test layers: focused native Go service test with real temporary filesystem
  boundaries and synthetic credential/token canaries
- Command: `go test ./... -run '^TestProfileService' -count=1`
- Result: intended red (`exit 1`)

The compiler reported that the expected public profile service and model were
absent (and stopped after the resulting cascade):

```text
undefined: NewProfileService
undefined: ConnectionProfile
```

The committed test specifies independently known behavior for importing the
resolved standard Operator YAML as an implicit `Default` profile, persisting
only the five allowed reference fields at mode `0600`, excluding certificate,
private-key, and token canaries, and rejecting missing names, missing or
non-HTTPS server URLs, relative state directories, and relative CA paths with
field-specific guidance. Task 3.2 implements only enough profile model/store
behavior to make this public slice green.
