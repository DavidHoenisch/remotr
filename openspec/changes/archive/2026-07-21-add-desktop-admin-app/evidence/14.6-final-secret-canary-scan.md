# 14.6 Final credential and secret canary scan

Date: 2026-07-16

## Dynamic security evidence

The focused nested-Go suite reran profile persistence, bootstrap cleanup,
bridge/view-model allowlists, deployment/enrollment token lifecycle, protected
Secret input, RBAC credential output, application-package boundaries, and
diagnostic save boundaries. It passed with local authenticated test-server
access. Its captured verbose output contained none of the 42 synthetic desktop
canaries.

Seven focused frontend files (9 tests) passed for application packages,
deployment and enrollment tokens, RBAC, encrypted Secrets, action error
redaction, and setup/maintenance. The enrollment-token test inspected both
`localStorage` and `sessionStorage` after display, copy, clear, failure, cancel,
and unmount lifecycle paths. Its captured output contained no canary.

The profile tests independently prove that persisted desktop settings contain
only allowlisted references, use owner-only permissions, and exclude certificate
bodies, private keys, bootstrap tokens, and credential-bearing server URLs. The
view-model tests encode every public model and reject credential/PEM/token
fields and canaries.

## Static sink scan

The scan used all 42 exact synthetic canaries discovered from desktop Go and
frontend tests, plus private-key PEM, GitHub token, and AWS access-key
signatures. Zero matches were found in:

- 60 production frontend output files;
- the final native `remotr-desktop` binary;
- the checked release manifest and five files from the extracted final DEB;
- all 10 committed Linux visual-regression screenshots;
- Playwright/browser result output;
- three retained Vagrant failure logs; and
- the workspace security audit log.

The browser result records `passed` with no failed-test artifacts. No generated
desktop failure screenshot, trace, video, or log was present. The final scan did
not read or modify a real Operator credential directory; credential persistence
was verified only with synthetic isolated fixtures.
