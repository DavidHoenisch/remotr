# Task 6.2 Endpoint table green evidence

- Verification IDs: `OS-DFV-006`, `OS-DFV-007`, `OS-DFV-008`
- Public seam: rendered Endpoint inventory and mounted shell page
- Test layers: Testing Library semantic table roles/names, status and evidence
  distinctions, Label-column interaction, authenticated empty recovery, row
  action, full frontend suite, production build, and real-browser geometry
- Focused command: `pnpm exec vitest run src/endpoints/EndpointTable.test.tsx src/overview/Overview.test.tsx`
- Focused result: pass (`2` files, `6` tests)
- Frontend command: `pnpm test`
- Frontend result: pass (`6` files, `15` tests)
- Production command: `pnpm build`
- Production result: pass (`tsc -b` and Vite production bundle)

The semantic table renders all seven canonical compliance values with distinct
text and icons, alongside independent recent, stale, or never-reported
freshness. It exposes reported/desired agent versions, Release ref, configured
Label columns, and deterministic UTC evidence time without inferring online,
offline, healthy, or unhealthy state.

Configured Label keys retain their supplied order regardless of each row's
Label order. The column chooser starts with those keys selected and can remove
or restore a Label column without altering Endpoint rows. Stable Endpoint IDs
key the rows and are passed unchanged to the Inspect action. The visible result
count distinguishes singular/plural inventory, and the zero-row surface uses
the shared authenticated-empty primitive with the optional enrollment-token
workflow rather than a connection error.

At a controlled 1100-by-720 browser viewport, the document remained exactly at
the viewport extents. The 1,260-pixel evidence table scrolled inside an
826-pixel table frame; the first Endpoint identity stayed pinned at the left
edge and its action stayed pinned within the right edge, so both critical
controls remained visible while the middle evidence columns could scroll.
