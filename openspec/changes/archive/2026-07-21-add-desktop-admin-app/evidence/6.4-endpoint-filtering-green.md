# Task 6.4 Endpoint filtering green evidence

- Verification IDs: `OS-DFV-012`, `OS-DFV-013`, `OS-DFV-014`, `OS-DFV-015`
- Public seam: rendered Endpoint inventory and Overview-to-Endpoints navigation
- Test layers: Testing Library semantic search/select/sort interaction,
  independently known intersections and ordering, full frontend suite,
  production TypeScript/Vite build, and real-browser behavior/geometry
- Focused command: `pnpm exec vitest run src/endpoints/EndpointTable.filtering.test.tsx`
- Focused result: pass (`1` file, `4` tests)
- Regression command: `pnpm exec vitest run src/endpoints/EndpointTable.test.tsx src/endpoints/EndpointTable.filtering.test.tsx src/overview/Overview.test.tsx`
- Regression result: pass (`3` files, `10` tests)
- Frontend command: `pnpm test`
- Frontend result: pass (`7` files, `19` tests)
- Production command: `pnpm build`
- Production result: pass (`tsc -b` and Vite production bundle; `1,783`
  modules transformed)

The reusable table-view engine case-folds Endpoint ID, Fleet, username, Label
key, and Label value search fields, then intersects the query with explicit
Fleet, compliance, and freshness selectors. Active filters show filtered and
total counts; clearing resets every query and selector in one action without
changing the mounted shell's Fleet scope or any server state.

Every documented evidence column has a semantic sort control. The selected
column determines primary order while equal values always resolve by Endpoint
identity, independent of input order. Slash and Ctrl/Cmd-K focus the current
page search only when focus is outside an input, editor, select, or modal; slash
inside search remains ordinary query text.

Overview summary navigation now mounts the real Endpoint inventory with its
equivalent selector applied, retains the visible applied-filter summary, and
updates the result count rather than showing a placeholder surface.

At a controlled 1100-by-720 browser viewport, the document stayed exactly
within the viewport. The 794-pixel filter region wrapped into a deliberate
two-row toolbar without overflow, while the 1,328-pixel evidence table remained
inside its 826-pixel scroll frame. A live `prod` search intersected with the
compliant selector to show exactly `1 of 3 Endpoints` and only
`endpoint-alpha`.
