# 12.10 AI integration parity — red evidence

- Verification IDs: OS-DOA-019 and OS-DFA-001 through OS-DFA-004.
- Public seam: typed local AI-integration service backed by `internal/aisetup`, the embedded Remotr agent bundle, opaque native-selected project roots, and a semantic desktop page.
- Selected layers: focused filesystem integration for user/project setup and exact-version upgrade; replacement negatives; traversal and symlink confinement; injected runtime and release-fetch boundaries; missing-runtime recovery; native-path view-model scan; bridge allowlist and parity drift.
- Authority boundary: the backend never invokes the `remotr` executable, accepts only the fixed supported runtimes, and confines writes to the explicit user or selected-project skill root.

## Intended backend red

Command:

`cd desktop && go test ./... -run '^TestAIIntegrationParityConfinesExplicitScopeVersionAndReplacement$'`

The test failed to compile because the typed AI-integration service, options, App injection option, project selection, list, setup, and upgrade methods did not exist. This was the intended failure at the public desktop filesystem seam.

## Intended frontend red

Command:

`cd desktop/frontend && pnpm exec vitest run src/setup/AIIntegrationFlow.test.tsx`

Vitest failed to resolve `./AIIntegrationPage`. This was the intended failure proving the explicit scope, project selection, runtime recovery, replacement, and release-version controls were absent.
