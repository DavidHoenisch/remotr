# Task 7.3 workspace orientation red-green evidence

- Verification IDs: `OS-DFV-031`, `OS-DFV-032`
- Public seam: rendered App navigation from authenticated workspace state and
  rendered selected-profile initial connection failure
- Test layers: Testing Library semantic state/action interaction, exact
  server-classified copy, enrollment/retry/profile callback assertions,
  prohibited fabricated-row assertions, full frontend suite, production build,
  and real-browser behavior/geometry
- Red command: `pnpm exec vitest run
  src/states/WorkspaceOrientation.test.tsx --reporter=dot`
- Red result: intended suite failure (`1` file, `1` passing and `1` failing
  test). The existing authenticated zero-Endpoint behavior passed, while the
  initial failure still rendered the generic `Workspace ready` placeholder,
  exposed no `Connection recovery` region, and reported Connected.
- Focused green command: `pnpm exec vitest run
  src/states/WorkspaceOrientation.test.tsx --reporter=dot`
- Focused green result: pass (`1` file, `2` tests)
- Frontend command: `pnpm test -- --run --reporter=dot`
- Frontend result: pass (`13` files, `33` tests)
- Production command: `pnpm run build`
- Production result: pass (`tsc -b` and Vite production bundle; `1,799`
  modules transformed)

An authenticated empty Endpoint result retains the connected shell, states
that no Endpoints are enrolled in the current Fleet scope, exposes the allowed
Create enrollment token action, and renders no empty semantic table.

An initial classified connection failure now retains the selected Production
profile and server label while explicitly showing `Not connected`. Its labeled
recovery region presents the exact safe message and guidance and exposes only
Retry connection and Choose another profile callbacks. No operational table,
sample row, or demo data is rendered while no successful snapshot exists.

At a controlled 1100-by-720 browser viewport, the recovery panel stayed within
the 248-to-968 horizontal and 160-to-approximately-439 vertical bounds. The App,
body, and document each remained exactly 1,100 by 720 pixels. The semantic
browser snapshot contained only selected-profile recovery content and no
tables or fabricated rows; the browser reported no application errors.
