# 12.6 encrypted Secret parity — red evidence

- Verification IDs: OS-DOA-013, OS-DOA-014, OS-DOA-016, OS-DOA-017, OS-DOA-019, OS-DFA-001 through OS-DFA-004, and OS-DFA-026 through OS-DFA-027.
- Public seam: typed Wails application API with native protected-file input and the authenticated Admin API.
- Selected layers: mTLS protocol integration, live Fleet/Endpoint validation, exact destructive confirmation, rollout-planning metadata, forbidden-response classification, protected-file safety, zeroization and persistence/log canary scans, and a semantic frontend flow.

## Intended backend red

Command:

`cd desktop && go test ./... -run '^TestEncryptedSecretParityProtectsMaterialAndPreservesRolloutPlanning$'`

The test failed to compile because `SecretUploadRequest`, `SecretLifecycleRequest`, `NewSecretService`, and the typed upload/list/activate/revoke application methods did not exist. This was the intended failure at the public desktop seam.

## Intended frontend red

Command:

`cd desktop/frontend && pnpm exec vitest run src/actions/SecretFlow.test.tsx`

Vitest failed to resolve `./SecretPage`. This was the intended failure proving the encrypted Secret management surface and its typed metadata-only actions were absent.
