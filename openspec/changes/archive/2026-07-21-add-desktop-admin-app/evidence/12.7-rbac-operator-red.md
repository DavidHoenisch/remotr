# 12.7 RBAC and Operator parity — red evidence

- Verification IDs: OS-DOA-014, OS-DOA-016, OS-DOA-017, OS-DOA-019, OS-DFA-001 through OS-DFA-004, and OS-DFA-026 through OS-DFA-027.
- Public seam: typed Wails application API with native protected-directory output and the authenticated Admin API.
- Selected layers: mTLS protocol integration, shared role-name and live role/Operator validation, exact destructive confirmation, malformed and forbidden response handling, owner-only credential persistence and cleanup, secret canaries, and a semantic frontend flow with audit refresh.
- Parity scope: the nine canonical `remotr admin credential stamp` and `remotr rbac ...` workflows. Hidden flat compatibility aliases remain outside the desktop parity contract.

## Intended backend red

Command:

`cd desktop && go test ./... -run '^TestRBACOperatorParityUsesCanonicalServerAuthorityAndProtectedCredentialOutput$'`

The test failed to compile because the typed RBAC role, rule, Operator, and credential-stamp request/result types, service, and application methods did not exist. This was the intended failure at the public desktop seam.

## Intended frontend red

Command:

`cd desktop/frontend && pnpm exec vitest run src/actions/RBACOperatorFlow.test.tsx`

Vitest failed to resolve `./RBACOperatorPage`. This was the intended failure proving the canonical security-management surface and its metadata-only actions were absent.
