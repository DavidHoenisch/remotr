# Task 5.4 Overview component red evidence

- Verification IDs: `OS-DFV-003`, `OS-DFV-004`, `OS-DFV-005`
- Public seam: rendered Overview component and its navigation callback
- Test layers: Testing Library semantic roles/names, independently known
  summary counts, authorization-only section failure, and pointer navigation
- Command: `pnpm exec vitest run src/overview/Overview.test.tsx`
- Result: intended red (`exit 1`)

Vitest cannot resolve `./Overview` because the production component does not
exist. The fixture contains four unique Endpoints and four matching State
evidence records so the expected total remains independently fixed at four,
not eight. It also fixes compliance, freshness, two-Fleet, two pending-or-active
Change request, and two recent Activity expectations.

The authorization case changes only the Activity section to an unavailable
authorization result. The test requires the Endpoint and Fleet evidence to
remain visible, the safe recovery guidance to be shown inside Recent activity,
and no whole-connection failure to be invented.

Summary activation must emit an equivalent source-page filter: compliant and
stale counts target Endpoint filters, the Fleet count targets the Fleet page,
and the pending-or-active count targets the pending and authorized Change
request lifecycles. Task 5.5 supplies the minimum component and application
navigation needed to make this contract green.
