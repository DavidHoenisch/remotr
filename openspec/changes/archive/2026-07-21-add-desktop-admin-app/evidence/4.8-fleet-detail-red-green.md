# Task 4.8 Fleet-detail red/green evidence

- Verification IDs: `OS-DFV-020`, `OS-DFV-021`
- Public seams: typed Fleet-detail service and authenticated Admin API
- Test layers: real Admin client over controlled mTLS, mixed-member
  compliance/freshness/version aggregation, visible-member count consistency,
  unrelated-Fleet exclusion, and explicit empty-Fleet behavior
- Red command: `go test ./... -run '^TestFleetDetailServiceAggregatesMixedAndEmptyFleets$' -count=1 -timeout=30s`
- Red result: intended red (`exit 1`)

The compiler reports that the typed Fleet-detail view, service, and injected
clock option do not exist. The mixed fixture independently supplies compliant,
drifted, and missing-State-report members with recent, stale, and
never-reported check-ins plus two reported versions and one absent version.
Each compliance, freshness, and version distribution must total the exact
visible members. An unrelated Endpoint must remain excluded.

The empty case returns a real Fleet with no enrolled Endpoints and an empty
State report. It must remain a valid detail result with zero distributions, an
empty member section, and explicit no-enrollment guidance rather than a missing
Fleet error.

## Green

- Green command: `go test ./... -run '^TestFleetDetailServiceAggregatesMixedAndEmptyFleets$' -count=1 -timeout=30s`
- Green result: pass (`exit 0`)

The implementation loads Fleet existence, members, and current State evidence
through the authenticated client with one shared context. Compliance,
freshness, and reported-version distributions are composed from the same
stable member rows returned in the view, so every distribution total equals
the visible member count. A Fleet present in the Fleet list remains valid when
its member and State evidence are empty and receives the required explicit
no-enrollment result.
