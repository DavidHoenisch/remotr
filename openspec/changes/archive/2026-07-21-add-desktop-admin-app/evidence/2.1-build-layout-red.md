# Task 2.1 build-layout red evidence

- Verification IDs: `OS-DOA-001`, `OS-DOA-002`
- Public seam: clean-checkout standalone desktop build layout
- Command: `go test -mod=vendor ./internal/desktoplayout -run '^TestStandaloneDesktopBuildLayout$' -count=1 -v`
- Result: intended red (`exit 1`)

The focused check reported only the expected missing standalone desktop
artifacts:

- `desktop/go.mod`
- `desktop/wails.json`
- `desktop/main.go`
- `desktop/frontend/dist/index.html`

This proves the isolated desktop module, Wails artifact metadata, embedded
native entrypoint, and production frontend asset were not present before task
2.2. The test remains committed red so the next vertical slice can make these
same public assertions green.
