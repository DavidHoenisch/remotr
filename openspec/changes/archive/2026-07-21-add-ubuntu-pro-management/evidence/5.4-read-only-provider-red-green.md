# Task 5.4 read-only provider evidence

- Verification IDs: OS-UPM-014, OS-UPM-015, OS-UPM-020, OS-UPM-021, OS-UPM-028, OS-UPM-029, OS-UPM-031, OS-UPM-032, OS-UPM-038, and OS-UPM-042.
- Public seam: `executor.Check` on a registry-compatible Ubuntu Pro handler.
- Process seam: Check invokes only literal versioned `/usr/bin/pro api` endpoints. Attachment-only checks use `u.pro.status.is_attached.v1`; declared-service checks additionally use `u.pro.status.enabled_services.v1`.
- Red sequence: structured attachment-state tests first failed to compile because no safe `StateReport` or structured Check existed. Stable contract and native-lock cases then returned generic probe failures. Warning health was incorrectly compliant. Declared services were absent from reports. Availability and entitlement failures were generic probe failures.
- Green state: attached and unattached are bounded enums; only authored service names and their observable enabled/variant state enter the report; undeclared native service details are omitted; and the documented `cis` status alias is normalized to authored `usg`.
- Green failures: invalid and expired contracts are distinct typed failures; native lock is bounded deferred contention; malformed/process failures become sanitized probe failures; stable warnings are unhealthy; unavailable and unentitled requested services are distinct unsupported outcomes. Localized messages, stderr, raw malformed output, account data, and contract data are not copied.
- Focused provider suite: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1` passed.
- Registry-facing regression: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/resourceregistry -run '^TestUbuntuProProviderPreflightPrecedesTokenResolutionAndAttachment$' -count=1` passed.
