# Task 3.3 parser boundary and fuzz evidence

- Verification IDs: OS-UPM-001 through OS-UPM-003, OS-UPM-034 through OS-UPM-035, and OS-UPM-058.
- Public seam: canonical desired-state parser and renderer.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/models -run '^TestParseStateUbuntuPro(Boundaries|CanonicalRoundTripIsDeterministic)$'`
- Independently expected result: invalid enums, alias confusion, cross-service options, duplicate fields, removed fields, and nondeterministic round trips are rejected.
- The retained boundary and deterministic round-trip cases pass after removing the out-of-scope Landscape model.
- Green result: model bounds reject both oversize cases; the complete boundary table and deterministic round-trip test pass.
- Fuzz result: `FuzzParseCanonicalUbuntuPro` passed 38,842 executions in three seconds with 25 total interesting inputs.
