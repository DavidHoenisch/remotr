# Task 8.5 service restoration evidence

- Verification IDs: OS-UPM-023 and OS-UPM-024.
- Public seam: multi-operation Ubuntu Pro Apply, its structured second Check, and a typed post-restoration enabled-services observation.
- Red partial-failure slice: after `esm-apps` enabled, a later stable `usg` failure returned immediately and left the earlier managed change active.
- Green restoration: successful managed changes are snapshotted without secrets, restored in reverse operation order, and checked against their prior enabled/variant state. The final error preserves the original stable failure plus `rollback restored`, `rollback failed`, or `rollback incomplete`.
- Drift slice: when the second Check cannot observe the newly enabled service, Apply disables that declared service and verifies the prior disabled state.
- Unexpected-side-effect slice: if the native response reports an omitted `usg` side effect, Remotr restores only its declared `esm-apps` target, does not mutate omitted `usg`, and reports rollback incomplete/residual rather than expanding ownership.
- Explicit purge is marked non-restorable, so this helper cannot silently upgrade a no-automatic-rollback operation into a transactional claim.
- Full package command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1` passed, including partial-ownership regressions.
