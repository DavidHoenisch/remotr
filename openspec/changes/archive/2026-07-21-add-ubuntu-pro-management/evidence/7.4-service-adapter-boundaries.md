# Task 7.4 service adapter boundary evidence

- Public seam: structured Check for each ordinary authored service name.
- Per-name matrix: `esm-infra`, `esm-apps`, `livepatch`, `usg`, `ros`, `ros-updates`, and `anbox-cloud` each reject stable unavailable and unentitled outcomes, the wrong endpoint schema version, and malformed typed attributes. Bounded unknown members and translated messages are ignored and never projected.
- The common API adversarial table rejects beta/unknown status members and unexpected native side effects; configuration schema tables reject unknown/beta and historical authored names; exact-platform tests reject unsupported releases before any API or secret boundary. Those common gates apply before the shared per-name adapter.
- Stable reason codes are the only failure detail retained. Localized messages and raw response data are absent from Check results.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -run '^TestOrdinaryServiceRowsFailClosedOnStatusBoundaries$' -count=1` passed.
- Broader provider command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1` passed after the minimal enable and disable implementations.
