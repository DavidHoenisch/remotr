# Task 5.6 observable-mode evidence

- Verification IDs: OS-UPM-060 and OS-LPC-022.
- Public seam: `executor.Check` on a resource declaring `enableMode: access-only`, plus the checked-in Ubuntu Pro qualification manifest.
- Independently known limitation: `u.pro.status.enabled_services.v1` reports the enabled service name but does not expose whether the enabling invocation used access-only mode.
- Red: an authored access-only `esm-apps` service was reported compliant solely because the enabled-services endpoint contained `esm-apps`.
- Green: Check reports `unsupported/ubuntu_pro_mode_unobservable` when a requested access-only mode appears enabled but cannot be durably distinguished. It still returns only the safe observable service-name state and performs no mutation.
- Qualification audit: every access-only tuple remains `untested` or `unsupported`, and no target advertises an access-only capability.
- Focused checks: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro ./internal/ubuntuproqualification -count=1` passed.
