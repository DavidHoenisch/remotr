# Task 13.3 traceability reconciliation

Every retained OS-LPC-011-through-020/022 and OS-UPM record now points at the
public configuration, exact-identity, capability-generation, provider,
acceptance, and four pinned VM selectors with `in-process` and `vm-safety`
environments. Passing scenarios have `verified` lifecycle.

OS-UPM-043 deliberately remains `planned`: access-only is recognized and its
protected request contract is tested, but the versioned enabled-services API
cannot durably distinguish that invocation mode. Its exact capability rows
therefore remain unadvertised. OS-UPM-045, OS-UPM-060, and OS-LPC-022 are
verified negative guarantees because they prove capability blocking and
fail-closed behavior for unobservable tuples.

The Ubuntu Pro Godog scenarios carry active verification tags for attachment,
ordinary service convergence/detach, conflict planning, unobservable-mode
blocking, and reboot reporting.

The following audits passed:

- `go run -mod=vendor ./scripts/traceability-lint.go`
- `go test -mod=vendor ./internal/ubuntuproqualification
  ./internal/capabilitydoc ./internal/cicontract ./test/acceptance -count=1`

All VM selectors named in the records passed during tasks 12.3 through 12.8;
none used a live Canonical credential.
