# Tasks 8.1 and 8.2 option observability evidence

- Verification IDs: OS-UPM-043 through OS-UPM-045 and OS-UPM-060.
- Configuration seam: strict catalog validation and capability derivation cover explicit full/access-only, each checked-in real-time-kernel variant, retain/purge, and exact per-tuple capability requirements without downgrade.
- Red mode slices: enabled service names were incorrectly accepted as durable proof first for access-only and then explicit full mode.
- Green mode contract: any explicitly selected enable mode returns `unsupported/ubuntu_pro_mode_unobservable` because the reviewed v32 status API exposes no mode field. Apply rejects the tuple before mutation. Every access-only and explicit-mode qualification tuple remains unadvertised.
- Observable variant contract: `realtime-kernel/raspi` is sent only in typed protected JSON and Apply succeeds only after the versioned status response re-exposes the exact named variant. Unknown or mismatched variants fail closed.
- Disable behavior contract: omitted mode sends `purge:false`; explicit purge sends `purge:true` and is never silently downgraded. Post-operation Check proves the service disabled, while higher-risk package/residual-effect claims remain gated by their VM rows.
- Focused provider command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1` passed. Configuration/capability suites for tasks 3.1–3.5 remain green.
