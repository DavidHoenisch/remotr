# Tasks 7.1 and 7.2 ordinary service convergence evidence

- Verification IDs: OS-UPM-017 through OS-UPM-021, OS-UPM-024, and OS-UPM-042.
- Public seam: Ubuntu Pro provider `Apply`, `executor.Check`, and a second Check against an already attached endpoint.
- Red enable: an authored drifted `esm-apps: enabled` resource returned from Apply without invoking a service endpoint, and the second Check remained drifted.
- Green enable: Apply observes only `u.pro.status.enabled_services.v1`, sends exact typed JSON to `u.pro.services.enable.v1 --data -`, validates the exact returned effect set, and requires a compliant public second Check.
- Red disable: an authored `esm-apps: disabled` resource issued no mutation. Green disable sends exact `{"service":"esm-apps","purge":false}` to `u.pro.services.disable.v1 --data -`, validates the returned disabled set, and passes second Check.
- Partial ownership: enabled but omitted `usg` is neither mutated nor projected into the declared-service report. Disable defaults to retain packages. The adapter rejects undeclared native side effects.
- Read-only provider evidence from task 5.4 covers unavailable/unentitled outcomes, stable warning health, and the checked-in `cis` status alias normalization to `usg`; these paths never report compliance incorrectly.
- Focused suite and registry-facing provider regression passed: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1` and `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/resourceregistry -run '^TestUbuntuProProviderPreflightPrecedesTokenResolutionAndAttachment$' -count=1`.
