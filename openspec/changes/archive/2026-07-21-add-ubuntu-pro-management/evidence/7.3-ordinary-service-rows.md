# Task 7.3 ordinary service row evidence

- Public seam: Ubuntu Pro provider Apply with a required structured second Check.
- Independently exercised rows: `esm-infra`, `esm-apps`, `livepatch`, `usg`, `ros`, `ros-updates`, and `anbox-cloud`.
- Every row begins attached and drifted, invokes exactly one versioned protected enable operation, validates the returned effect set, and succeeds only after the versioned enabled-services API proves the authored name enabled. The `usg` row independently proves the documented native `cis` status representation.
- The first `esm-apps` slice supplied the intended red failure and minimum generic implementation; the remaining names independently exercise that public contract without adding name-specific branches.
- All qualification rows remain `untested`, so this provider-level evidence does not advertise any release/service capability before its pinned VM selector and recovery evidence pass.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -run '^TestOrdinaryServiceRowsUseObservableSecondCheck$' -count=1` passed.
