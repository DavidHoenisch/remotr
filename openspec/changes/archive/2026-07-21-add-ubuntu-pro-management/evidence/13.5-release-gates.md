# Task 13.5 release-gate evidence

All selected Ubuntu Pro implementation, public-workflow, mock-integration, and
credential-free VM checks passed. No live Canonical credential was supplied or
read. The only non-passing repository-wide command is the pre-existing static
analysis backlog described below; the Ubuntu Pro scope is clean.

## Focused and public workflow checks

The following commands passed on the final implementation:

- `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test
  -mod=vendor ./internal/applicators/ubuntupro
  ./internal/ubuntuproqualification -count=1`
- `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test
  -mod=vendor ./cmd/remotr -run '^TestApp_config.*UbuntuPro.*$' -count=1`
- `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test
  -mod=vendor ./internal/agent/facts -run '^TestReadIdentity.*$' -count=1`
- `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test
  -mod=vendor ./internal/applicators/ubuntupro
  -run '^Test(Applicator|APIClient|DecodeEnvelope).*$' -count=1`
- `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test
  -mod=vendor ./internal/ubuntuproqualification ./internal/capabilitydoc
  ./internal/cicontract ./test/acceptance -count=1`
- `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go run
  -mod=vendor ./scripts/traceability-lint.go`
- `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go run
  -mod=vendor ./scripts/ubuntu-qualification-audit.go`
- `openspec validate add-ubuntu-pro-management --strict`

The acceptance suite passed four Ubuntu Pro scenarios and 16 public steps.
Attachment and service operations used a synthetic canary through a stateful
process-boundary double. Public Sync/apply and fleet-state assertions verified
redaction and never exposed the canary.

## Credential-free VM selectors

The following selectors passed outside the filesystem sandbox and cleaned up
their guest secret, test binary, libvirt domain, disk, key, and network:

- `make provider-matrix-vm-ubuntu-pro-20.04`
- `make provider-matrix-vm-ubuntu-pro-22.04`
- `make provider-matrix-vm-ubuntu-pro-24.04`
- `make provider-matrix-vm-ubuntu-pro-26.04`
- `make provider-matrix-vm-ubuntu-pro-negative-identities`
- `make provider-matrix-vm-ubuntu-pro-secret-canary`
- `make provider-matrix-vm-ubuntu-pro-service-fips-24-04`
- `make provider-matrix-vm-ubuntu-pro-variant-realtime-kernel-raspi-24-04`
- `make provider-matrix-vm-ubuntu-pro-enable-mode-esm-infra-full-24-04`

The four base selectors also ran the complete service/default-full/variant
matrix and the high-risk and fault suites described in the task 12 evidence.
The harness rejects live-token environment variables and injects only its own
synthetic token.

## Static analysis

The change-scoped command passed with zero issues:

`env GOCACHE=/tmp/remotr-go-cache PATH=/tmp/remotr-gosec-bin:$PATH mise exec
go@1.26.5 -- gosec -exclude-generated -tests=false
./internal/applicators/ubuntupro ./internal/ubuntuproqualification`

The two scanner false positives introduced by this change are documented at
their source boundaries: the public Ubuntu Pro endpoint identifier containing
the word `token`, and the operator-selected, size-bounded, strictly decoded
qualification manifest path.

The required repository-wide command was also run:

`env GOCACHE=/tmp/remotr-go-cache PATH=/tmp/remotr-gosec-bin:$PATH mise exec
go@1.26.5 -- make gosec`

It exited nonzero with 280 existing repository findings across 435 files and
reported no issue in either Ubuntu Pro package. This is an acknowledged global
baseline, not a passing repository security claim; unrelated suppression or
cleanup was deliberately kept outside this change.

## Final full suite

`env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- make test` first
passed all 59 fuzz seed corpora inside the sandbox, then reached the known
loopback restriction when existing `httptest` packages attempted to bind local
listeners. The exact command was replayed outside the sandbox and passed all
59 seed corpora plus `go test -mod=vendor ./...`.

These results qualify deterministic API-boundary and provider behavior. They
do not claim a live Ubuntu Pro subscription-seat lifecycle or entitled native
package, snap, repository, kernel, boot-artifact, or compliance-tool effects.
