# Task 6.3 attachment fault evidence

- Verification IDs: OS-UPM-012, OS-UPM-015, OS-UPM-024, OS-UPM-029, OS-UPM-030, and OS-UPM-032.
- Public seam: Ubuntu Pro provider `Apply` followed by bounded public `Check` where native outcome may be ambiguous.
- Red lost-response slice: an attach process failure returned immediately after the first status probe, so a possibly successful native attach received no recovery observation.
- Green lost-response slice: non-stable, non-cancellation attach failures trigger exactly one read-only public recovery Check. The provider reports a bounded ambiguous outcome, never retries attachment, and never falls back to ordinary `pro attach`.
- Red resolver slice: resolver denial copied its diagnostic directly into the provider error.
- Green resolver slice: denial becomes stable `Ubuntu Pro token resolution failed`, performs zero protected-input calls, and preserves cancellation/deadline identity without copying provider diagnostics.
- Negative matrix: initial network failure resolves no token; stable Canonical API errors preserve only their validated code and do not trigger ambiguous retry logic; missing attach endpoint receives one recovery Check; pre-probe cancellation crosses no resolver or mutation boundary; and a successful attach with unattached post-state fails with bounded state drift.
- Exact call assertions prove no case performs more than one protected attach invocation. The fake boundary rejects every executable/argv shape other than the literal versioned endpoints.
- Focused and registry regressions passed: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1` and `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/resourceregistry -run '^TestUbuntuProProviderPreflightPrecedesTokenResolutionAndAttachment$' -count=1`.
