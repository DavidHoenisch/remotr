# Task 4.2 Ubuntu Pro token authorization and hash evidence

- Verification IDs: OS-UPM-010 through OS-UPM-013 and OS-UPM-016.
- Public seams: authenticated `POST /v1/secrets/resolve` and registered-resource `ResolveEffectiveHash`.
- Authenticated focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/server -run '^TestResolveUbuntuProTokenAuthorizesExactActiveArtifactScope$' -count=1`.
- Authorization red: the exact active-artifact request returned 403 because `ubuntuPro.tokenRef` had no authorized consumer.
- Authorization green: the exact reference, `ubuntu-pro-token` purpose, mTLS endpoint, registered fleet, active digest, and `ubuntu-pro/primary-subscription` address reach the resolver; wrong purpose, reference, and resource requests return 403 without another provider call. Caller-supplied endpoint/fleet values are replaced by authenticated scope.
- Effective-hash focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/resourceregistry -run '^TestUbuntuProEffectiveHashUsesSafeTokenIdentityAndClearsMaterial$' -count=1`.
- Effective-hash red: the registry rejected `tokenRef` because `ubuntuPro` had no hash purpose.
- Effective-hash green: resolution uses `ubuntu-pro-token` at the exact artifact/resource scope; returned material is cleared; different bearer bytes with the same safe identity produce the same hash; version/activation changes produce a different hash.
- Broader checks: `./internal/secrets`, the existing secret-reference effective-hash regression, and all focused server secret-resolution tests pass.
- Task 4.1 remains open until its process, error, audit, plan, rollback, and report red tests have been added at their corresponding vertical slices.
