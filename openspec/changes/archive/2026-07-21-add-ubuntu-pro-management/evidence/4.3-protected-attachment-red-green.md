# Task 4.3 protected attachment evidence

- Verification IDs: OS-UPM-010, OS-UPM-011, OS-UPM-014, OS-UPM-016, OS-UPM-037, OS-UPM-039, OS-LPC-019, and OS-LPC-020.
- Public seams: registered provider construction/Apply and the Ubuntu Pro Client process boundary.
- Canonical contract checked: Ubuntu Pro Client v32 documents `u.pro.attach.token.full_token_attach.v1`, JSON fields `token` and `auto_enable_services`, the `--data -` stdin form, result fields `enabled` and `reboot_required`, and the common v1 envelope.
- Process-boundary command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -run '^TestAPIClientFullTokenAttachUsesExactProtectedProcessBoundary$' -count=1`.
- Process red: no Ubuntu Pro API client existed.
- Process green: the exact boundary is `/usr/bin/pro api u.pro.attach.token.full_token_attach.v1 --data -`; protected stdin is exactly `{"token":"<canary>","auto_enable_services":false}`; argv contains no token, `--args`, ordinary subcommand, or shell; caller material is cleared.
- Provider command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/resourceregistry -run '^TestUbuntuProProviderPreflightPrecedesTokenResolutionAndAttachment$' -count=1`.
- Provider red: all cases stopped at the unimplemented resource factory.
- Provider green: unsupported derivative identity causes zero runner/resolver calls; already-attached exact Ubuntu causes one typed status call and zero resolver/InputRunner calls; eligible unattached exact Ubuntu resolves once at the exact token scope, invokes protected attachment once, requires no auto-enabled services, clears material, and re-probes attached state.
- Broader checks: all current `./internal/applicators/ubuntupro` tests, `./internal/capabilitymatrix`, and the Ubuntu Pro field-policy/effective-hash/provider focused set pass.
- Tasks 5.x and 6.x remain open for the remaining endpoints, full envelope/error properties, typed outcomes, idempotence, and recovery behavior.
