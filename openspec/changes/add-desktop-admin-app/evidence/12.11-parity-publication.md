# 12.11 desktop CLI parity publication

- Published inventory: `docs/reference/desktop-cli-parity.json`
- Publication date: 2026-07-16
- Public gate: `mise exec -- go test ./cmd/remotr -run '^TestDesktopCLIParityInventoryMatchesCommandTree$'`
- Source command tree: 72 current non-hidden commands with operator-observable actions
- Disposition: 59 implemented, 12 planned, 1 reviewed not-applicable interface mechanic
- Parity claim: partial

The published gate rejects unmapped, stale, and duplicate command rows; malformed dispositions; missing verification IDs; missing planned release targets; implemented workflows without passing public-seam selectors; selectors on planned or not-applicable entries; unclassified not-applicable entries; source-count drift; and a full-parity claim while any planned workflow remains.

## Planned workflows retained without a parity claim

- `remotr change list`
- `remotr change show`
- `remotr endpoint agent upgrade`
- `remotr endpoint label list`
- `remotr endpoint label set`
- `remotr endpoint label unset`
- `remotr endpoint list`
- `remotr endpoint remove`
- `remotr endpoint show`
- `remotr fleet agent upgrade`
- `remotr fleet list`
- `remotr git sync`

These entries retain versioned targets and verification IDs. Their presence keeps the publication partial; it is not evidence of full CLI parity.

## Intended red

Command:

`go test ./internal/desktopparity -run '^TestValidatePublished'`

The test failed to compile because the publication metadata types and `ValidatePublished` gate did not exist. This was the intended failure at the versioned inventory seam before adding false-full-claim, duplicate, stale, and interface-mechanic enforcement.
