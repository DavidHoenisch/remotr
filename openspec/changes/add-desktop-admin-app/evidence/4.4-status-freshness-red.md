# Task 4.4 status and freshness red evidence

- Verification IDs: `OS-DFV-006` through `OS-DFV-011`
- Public seams: typed desktop workspace service and authenticated Admin API
- Test layers: real Admin client over controlled mTLS, table-driven canonical
  status mapping, absent-State-report behavior, deterministic Label projection,
  and injected-clock freshness boundary cases
- Command: `go test ./... -run '^TestWorkspace(CanonicalComplianceAndLabelProjection|FreshnessUsesExactTenMinuteBoundary)$' -count=1 -timeout=30s`
- Result: intended red (`exit 1`)

The controlled workspace supplies every canonical State report status, an
enrolled Endpoint omitted from the State report, Labels in non-display order,
and check-ins within, exactly at, and one nanosecond beyond the default
ten-minute threshold plus an Endpoint that has never checked in.

Every compliance, missing-report, Label-order, exact-boundary, and
never-reported assertion passed. The intended failure was independently known:
`freshness-beyond` was composed as `recent` instead of `stale` even though its
check-in age exceeded ten minutes by one nanosecond. Its compliant State status
remained intact, proving the failure is isolated to freshness classification.
Task 4.5 adds the minimum injected-clock threshold mapping needed to make this
slice green without introducing online/offline presence claims.
