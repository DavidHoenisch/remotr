# 14.4 Final Linux artifact evidence

Date: 2026-07-16

## Wails doctor and prerequisites

Pinned `wails v2.12.0 doctor` reported:

- Ubuntu 26.04, Linux/amd64;
- Go 1.26.5;
- GTK 3 development package 3.24.52 present;
- `pkg-config` 2.5.1 and GCC/build-essential present; and
- its legacy WebKit detector reported `Unknown / Not Found`.

The repository prerequisite contract independently passed and selected
`webkit2_41`. Direct `pkg-config` evidence reported GTK 3.24.52 and
WebKitGTK 4.1 version 2.52.3. The real production build and native launch below
therefore provide the controlling compatibility evidence; the Doctor detector
warning is retained here rather than hidden.

## Production build metadata

- Product/version: `Remotr Desktop 0.0.0-final.14.4`.
- Build mode: production; devtools disabled; embedded frontend; clean binary
  output.
- Target/tags: `linux/amd64`, `desktop,webkit2_41,wv2runtime.download,production`.
- Toolchain/dependency: Go 1.26.5, CGO enabled, Wails v2.12.0.
- Binary: stripped dynamically linked x86-64 ELF, GNU/Linux 3.2.0 baseline,
  22,032,000 bytes, SHA-256
  `cfc8e962f9aa9a1755adec1d4b8560ffb69d8442de3c263cb56dbd6b352f5f64`.
- The binary's `--version` output exactly matched
  `Remotr Desktop 0.0.0-final.14.4`.

## Package lifecycle and manifest

The exact `make desktop-release-check DESKTOP_VERSION=0.0.0-final.14.4`
chain passed build, Debian package creation, package-manager install/remove in
the pinned Debian 13 container, native host launch under Xvfb, embedded identity
and version, manifest generation, and manifest validation.

- Package: `remotr-desktop_0.0.0-final.14.4_amd64.deb`.
- Control identity: `remotr-desktop`, version `0.0.0-final.14.4`, architecture
  `amd64`, section `admin`, priority `optional`.
- Runtime dependencies: `libc6`, GTK 3, and WebKitGTK 4.1.
- Size: 6,955,350 bytes; SHA-256
  `86afc6cc7ea8795501f7c2f3c7718df03c0e856c5e9d9baec6976c28429b20bb`.
- Manifest: exactly one Linux/amd64 DEB with build/install/launch/remove evidence
  marked passed.

The advertised classification remains truthful: unsigned seven-day CI
development artifact, `releaseEligible: false`, with no signed release output,
macOS artifact, Windows artifact, additional architecture, or additional
package format.
