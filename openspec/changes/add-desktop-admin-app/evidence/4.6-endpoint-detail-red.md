# Task 4.6 Endpoint-detail red evidence

- Verification IDs: `OS-DFV-016` through `OS-DFV-019`
- Public seams: typed Endpoint-detail service and authenticated Admin API
- Test layers: real Admin client over controlled mTLS, exact request inventory,
  bounded fan-out, partial-tab classification, structured-content canary scan,
  and channel-controlled obsolete-selection cancellation
- Command: `go test ./... -run '^TestEndpointDetailService(PreservesPartialEvidence|CancelsObsoleteSelection)$' -count=1 -timeout=30s`
- Result: intended red (`exit 1`)

The compiler reports that the typed Endpoint-detail service, option, view, and
obsolete-selection error contract do not exist. The committed partial-evidence
case independently supplies one exact Endpoint record plus State and bounded
system evidence while Schedules returns `503` and Firewall returns `403`. It
requires the usable sections to remain ready, each failed tab to retain its own
safe classified state, four detail requests to overlap within a bound of four,
and response/raw-system/credential canaries to remain outside the view model.

The cancellation case blocks every request for `endpoint-slow`, selects
`endpoint-fast`, and requires the old request contexts to be canceled with no
slow identity or evidence returned for rendering. Task 4.7 adds only the lazy,
identity-checked, bounded loader and safe projections needed to make these cases
green. UI-owned close/focus restoration remains covered by the later Endpoint
detail user-flow slice.
