# 12.8 setup and maintenance parity — red evidence

- Verification IDs: OS-DOA-001, OS-DOA-003 through OS-DOA-005, OS-DOA-008 through OS-DOA-015, OS-DOA-018 through OS-DOA-019, and OS-DFA-008 through OS-DFA-011.
- Public seam: typed Wails setup API, owner-only profile and credential references, authenticated mTLS identity verification, fixed native documentation handoff, and an injected stable-release check.
- Selected layers: focused filesystem and mTLS integration, invalid-profile/no-request evidence, bootstrap and enrollment secret canaries, Linux platform/update-state validation, bridge allowlist, and a semantic Setup-page flow.
- Update safety: this build checks the stable release channel but keeps in-app installation disabled until an exact Linux architecture and artifact format has matching native evidence.

## Intended backend red

Command:

`cd desktop && go test ./... -run '^TestSetupMaintenanceParityUsesProtectedProfilesClassifiedDoctorAndTruthfulLinuxUpdates$'`

The test failed to compile because the typed setup view, doctor report, release-check service, application option, and purpose-specific setup methods did not exist. This was the intended failure at the public desktop seam.

## Intended frontend red

Command:

`cd desktop/frontend && pnpm exec vitest run src/setup/SetupMaintenanceFlow.test.tsx`

Vitest failed to resolve `./SetupMaintenancePage`. This was the intended failure proving the profile/bootstrap, doctor, docs, version, update, and enrollment-entry surface was absent.
