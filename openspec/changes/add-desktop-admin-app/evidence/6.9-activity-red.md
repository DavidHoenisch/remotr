# Task 6.9 Activity contract red evidence

- Verification IDs: `OS-DFV-024`, `OS-DFV-025`, `OS-DFV-026`
- Public seam: rendered App Activity navigation, filter, pagination, detail,
  and authorization-local user flows with deterministic page results
- Test layers: Testing Library semantic filter/paging interaction, exact
  request arguments, independently supplied duplicate IDs, exact server row
  order, inert structured-detail values, connected-shell preservation, and a
  permitted Endpoint regression surface
- Red command: `pnpm exec vitest run src/activity/ActivityPage.test.tsx
  --reporter=dot`
- Red result: intended failure (`1` file, `2` failing tests) because Activity
  still mounted only its placeholder, exposing neither the labeled filter
  inputs nor the authorization-local `Activity evidence` region

The paging contract first applies exact since, until, action, and actor-type
filters with an empty cursor and seen-ID set. Its second request must retain
those filters, send the server-provided `cursor-2`, and send the IDs already
visible. The independently supplied second page deliberately repeats
`event-3`; the final rows must remain in server order as `event-2`, `event-3`,
and `event-4`, with one occurrence of the repeated ID.

The detail contract supplies script-shaped and URL-shaped scalar values. It
requires both to remain visible literal text inside a structured-data region,
with no link, image, or executed global side effect. The authorization case
starts from a server-classified Activity section failure, requires the exact
safe explanation and recovery guidance inside the Activity page, keeps the
connection visibly connected, and then verifies that a permitted Endpoint
inventory remains usable.
