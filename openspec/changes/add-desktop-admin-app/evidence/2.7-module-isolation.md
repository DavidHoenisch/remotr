# Task 2.7 module-isolation evidence

- Verification IDs: `OS-DOA-001`, `OS-DOA-002`
- Public seam: root configuration/CLI build and explicit nested desktop module
- Environment: every displayed `go` command was run through `mise exec --`
  with `GOCACHE=/tmp/remotr-go-cache`,
  `GOMODCACHE=/tmp/remotr-go-modcache`, and
  `GOTMPDIR=/tmp/remotr-go-tmp`; root dependency resolution used the committed
  vendor tree

## Static isolation guard

Command:

```text
go test -mod=vendor ./internal/desktoplayout \
  -run '^TestRootModuleRemainsIndependentOfDesktopToolchain$' -count=1
```

Result: pass. The guard verifies that root `go.mod`, root
`vendor/modules.txt`, and every non-test production Go import outside the
nested module contain no Wails dependency.

## Root module and Admin CLI

Commands and results:

```text
go env GOMOD
/home/dhoenisch/code/remotr/go.mod

go list -mod=vendor ./...
pass; the root package list contains no github.com/DavidHoenisch/remotr/desktop package

go test -mod=vendor ./cmd/remotr -count=1
ok github.com/DavidHoenisch/remotr/cmd/remotr

go build -mod=vendor -o /tmp/remotr-task-2.7 ./cmd/remotr
pass
```

The CLI test was rerun with local-loopback permission because its existing
doctor tests use `httptest` listeners. No Wails or frontend dependency was
needed by the root list, test, or build.

## Nested desktop module

Commands and results:

```text
cd desktop
go env GOMOD
/home/dhoenisch/code/remotr/desktop/go.mod

go test ./... -count=1
ok github.com/DavidHoenisch/remotr/desktop
```

The distinct `GOMOD` paths and successful explicit commands demonstrate that
the desktop tests belong to the nested module and are not silently included in
the root Go surface.
