# 13.2 affected pull-request desktop CI — red evidence

- Verification IDs: OS-DOA-014, OS-DOA-015, OS-DOA-018, and OS-DOA-019.
- Public seam: the affected pull-request GitHub Actions workflow and the commands it publishes as required Linux desktop checks.
- Selected layers: static workflow contract, nested desktop Go suite, dedicated bridge canaries, frontend type/lint/unit checks, real-browser visual regression, production frontend build, CLI parity drift, and retained root `make test`.

## Intended red

Command:

`mise exec -- go test ./internal/desktoplayout -run '^TestAffectedPullRequestsRunCompleteLinuxDesktopGate$' -count=1`

The test failed because `.github/workflows/desktop.yml` did not exist. This is the intended red proving affected pull requests had no Linux desktop gate and the frontend manifest had no independently invokable type-check or lint contract.

## Green verification

- The focused workflow contract passed and rejects non-Linux or floating-host-browser runners.
- pnpm 11.7.0 passed the TypeScript check, the focused Biome lint baseline, 33 unit/component files containing 68 tests, and the production Vite build.
- Playwright 1.61.1 installed its pinned Chromium and passed all 10 browser/visual cases at 1440x900 and 1100x720 on an unchanged second run. The baselines were refreshed for the current navigation and locked full-Chromium renderer; no system Chrome executable is selected.
- The complete nested desktop Go suite passed. The dedicated Wails allowlist, view-model credential-canary, and remote-content rejection selectors passed separately.
- `TestDesktopCLIParityInventoryMatchesCommandTree` passed against the current non-hidden Admin CLI tree.
- `mise exec -- openspec validate add-desktop-admin-app --strict` passed.
- `mise exec -- make test` passed all 37 discovered fuzz seed corpora and the complete root Go suite.
