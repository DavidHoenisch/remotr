# 13.6 desktop release-manifest gate — red evidence

- Verification ID: `OS-DOA-018`.
- Public seam: the exact desktop artifact set uploaded by CI and its
  machine-readable release manifest.
- Selected layers: manifest generation/check behavior, integrity binding,
  Windows/macOS/unevidenced-target/evidence-status/undeclared-file negatives,
  CI check-before-upload ordering, exact package lifecycle, and broader suites.

## Intended red

Command:

`env GOCACHE=/tmp/remotr-gocache GOTMPDIR=/tmp/remotr-gotmp mise exec -- go test ./internal/desktoplayout -run '^TestDesktopReleaseManifestRejectsUnevidencedAndNonLinuxArtifacts$' -count=1`

The focused test failed because no release-manifest generator/checker, Make
gate, checked CI command, or uploaded manifest existed. This is the intended
red before the artifact advertisement boundary can reject non-Linux or
unevidenced outputs.

## Green verification

- The focused generator/checker test bound a controlled artifact to its exact
  SHA-256 and size, then rejected Windows, macOS, arm64, RPM, failed launch
  evidence, a digest mismatch, and an undeclared DMG.
- `make desktop-release-check DESKTOP_VERSION=0.0.0-deb-smoke.local` passed the
  complete CI-equivalent dependency chain: production Wails build, root-owned
  DEB build, digest-pinned Debian package-manager install/purge, native launch,
  manifest generation, and manifest validation.
- The checked 6,955,002-byte Linux/amd64 DEB had SHA-256
  `e2527fd3e2b02b3d3fae6df6c8f4bbc9ba81a0db6e0d377307d1e9f19c4f8ede`.
  Its manifest records only `linux`/`amd64`/`deb`, the exact embedded/package
  version, unsigned CI-development classification, `releaseEligible: false`,
  and passed build/install/launch/remove evidence.
- The checker constrains the manifest and artifact to regular files in the
  upload directory, rejects duplicates and unlisted upload entries, and accepts
  only targets present in the native-evidence inventory.
- Affected Linux CI runs `make desktop-release-check` before uploading the exact
  DEB and checked `release-manifest.json` together.
- Python syntax compilation, the complete root desktop-layout package, the
  complete nested desktop Go suite, and strict OpenSpec validation passed.
- `mise exec -- make test` passed all 37 discovered fuzz seed corpora and the
  complete root Go suite.
