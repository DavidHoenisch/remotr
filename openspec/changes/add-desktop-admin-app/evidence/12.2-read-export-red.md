# 12.2 read/export parity — intended RED

- Mapped CLI workflows: `remotr inventory`, `remotr endpoint state report`, `remotr endpoint cron report`, `remotr fleet state report`, `remotr fleet cron report`, `remotr firewall logs`, `remotr firewall report`, `remotr firewall export`, `remotr logs list`, `remotr logs export-info`, and `remotr diagnostics collect`.
- Verification IDs: OS-DOA-019, OS-DFV-003, OS-DFV-006, OS-DFV-007, OS-DFV-016, OS-DFV-017, OS-DFV-020, OS-DFV-021, OS-DFV-024, OS-DFV-025, OS-DFV-026, and OS-DFA-018 through OS-DFA-022.
- Public seam: purpose-specific Remotr Desktop/Wails application methods backed by a controlled mutual-TLS Admin API fixture and native save destinations.
- Focused command: `go test -run TestReadExportParityUsesBoundedTypedEvidenceAndNativeDestinations ./...` in `desktop/`.
- Intended RED: compilation failed because `LoadAssetInventory`, `SaveAssetInventory`, `LoadFleetOperationalReports`, `LoadFirewallReport`, `SaveFirewallReport`, `LoadAuditExportInfo`, `LoadDiagnosticRequest`, and their typed view models did not exist.
- Rendered-flow command: `pnpm exec vitest run src/reports/ReadExportFlow.test.tsx` in `desktop/frontend/`.
- Rendered-flow RED: the test could not find a `Reports` navigation control because no read/export workspace was rendered.

The green implementation must return bounded structured evidence, preserve exact server identities and timestamps, write exports only after a native destination choice with owner-only permissions, and return metadata rather than file bytes to the frontend.

## Green evidence

- Focused native seam, parser boundary/fuzz seeds, and bridge allowlist passed.
- The rendered Reports workflow, bridge adapter, and first-release authority-boundary tests passed.
- All 25 frontend test files (58 tests), the production frontend build, the complete desktop Go suite, and repository `make test` passed.
- The parity-drift inventory maps all eleven workflows to `parity-read-export` with public-seam selectors.
- Strict OpenSpec validation passed before and after checklist completion.
