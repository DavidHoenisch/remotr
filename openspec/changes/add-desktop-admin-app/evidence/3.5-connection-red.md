# Task 3.5 connection-service red evidence

- Verification IDs: `OS-DOA-008`, `OS-DOA-009`, `OS-DOA-010`
- Public seam: typed desktop connection service backed by the real Admin client
- Test layers: focused native Go integration test over controlled HTTPS/mTLS
  sockets with fixed certificate-validity dates and synthetic secret canaries
- Command: `go test ./... -run '^TestConnectionService' -count=1`
- Result: intended red (`exit 1`)

The compiler reported that the expected desktop connection seam was absent,
including `NewConnectionService`, `ConnectionView`,
`ConnectionFailureKind`, and the classified failure constants.

The committed test requires an independently known `/v1/admin/me` identity on
success and distinct safe results for missing credentials, an unknown CA, an
expired client credential, an unreachable server, and a forbidden identity.
The controlled server requires a verified client certificate. Failure
assertions reject the Operator state directory, private-key path or PEM marker,
bootstrap-token canary, and forbidden response-body canary. Task 3.6 adds the
minimum real-client mapping needed to make this matrix green.
