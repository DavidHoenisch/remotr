# Task 5.1 visual theme red/green evidence

- Public seam: frontend CSS custom-property theme contract
- Test layers: deterministic asset contract, local font references, minimum
  geometry, focus visibility, reduced motion, and frontend type/unit suites
- Red command: `pnpm test -- --run src/styles/theme.contract.test.ts`
- Red result: intended red (`exit 1`), `theme.css` was absent
- Green command: `pnpm test -- --run src/styles/theme.contract.test.mjs`
- Green result: pass (`exit 0`)

The theme is a light, refined industrial operations ledger. Its single token
contract defines warm near-white content, cool stone navigation, charcoal text,
cobalt interaction, restrained mint/amber/red evidence, a 4-pixel spacing
rhythm, one-pixel borders, minimal elevation, 224-pixel navigation, and the
1100-by-720 logical-window floor. IBM Plex Sans and IBM Plex Mono are bundled
through local package imports; no remote URL or decorative gradient is present.

The contract also requires tabular numeric evidence, a visible global focus
ring, brief semantic motion tokens, and a reduced-motion override. The adjacent
theme documentation defines component usage rules so later shell and page work
does not create competing palettes, spacing systems, status semantics, or
elevation layers.
