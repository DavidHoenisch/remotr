# Task 5.5 Overview component green evidence

- Verification IDs: `OS-DFV-003`, `OS-DFV-004`, `OS-DFV-005`
- Public seam: rendered Overview component and mounted shell navigation
- Test layers: Testing Library semantic roles/names, independently known
  aggregation, partial authorization, pointer navigation, full frontend suite,
  production TypeScript/Vite build, and real-browser geometry
- Focused command: `pnpm exec vitest run src/overview/Overview.test.tsx`
- Focused result: pass (`4` tests)
- Frontend command: `pnpm test`
- Frontend result: pass (`4` files, `11` tests)
- Production command: `pnpm build`
- Production result: pass (`tsc -b` and Vite production bundle)

Overview derives compliance and freshness exactly once from unique Endpoint
rows; it does not add the matching State-evidence or per-Fleet aggregates back
into the total. Pending and authorized Change requests form the open-change
count, while revoked requests remain available in the detailed summary without
inflating that count.

Each Fleet, Endpoint/State, Change request, and Activity section evaluates its
own state. A forbidden Activity result renders safe authorization guidance in
the Recent activity region while the summary strip, posture distributions,
Fleets, and Change requests remain usable. Ready data is capped to concise
recent lists without mutating or inventing server evidence.

Compliance, freshness, Fleet, and Change summary controls emit explicit page
and filter targets. The mounted application keeps those filters when switching
the single shell main region; a component integration assertion verifies the
compliant summary opens Endpoints with `compliance: compliant` visibly applied.

At a controlled 1100-by-720 browser viewport, the document stayed exactly at
the viewport extents, the navigation rail remained 224 pixels, and the content
frame had no horizontal overflow. Its dense two-column Overview required only
29 pixels of internal vertical scrolling; Operational posture/Fleets and Recent
activity/Change requests aligned in paired rows with all summary controls
visible.
