# Task 6.10 Activity implementation green evidence

- Verification IDs: `OS-DFV-024`, `OS-DFV-025`, `OS-DFV-026`
- Public seam: rendered App Activity navigation, filter, pagination, detail,
  and authorization-local user flows with deterministic page results
- Test layers: Testing Library semantic filter/paging interaction, exact
  request arguments, independent duplicate-ID input, exact row ordering,
  inert structured-detail rendering, permitted-page regression, full frontend
  suite, production build, and real-browser behavior/geometry
- Focused command: `pnpm exec vitest run src/activity/ActivityPage.test.tsx
  --reporter=dot`
- Focused result: pass (`1` file, `2` tests)
- Frontend command: `pnpm test -- --run --reporter=dot`
- Frontend result: pass (`11` files, `28` tests)
- Production command: `pnpm run build`
- Production result: pass (`tsc -b` and Vite production bundle; `1,794`
  modules transformed)

Applying Activity filters sends the exact since, until, action, and actor-type
fields with an empty cursor and seen-ID set. Loading the next page sends the
server-provided cursor, preserves the applied filter snapshot even if draft
fields later differ, and sends the currently visible IDs. Incoming events are
appended in server order while empty and repeated IDs are discarded without a
client sort.

The semantic Activity table displays exact event ID, time, actor, action,
resource type/identity, status, and request identity. Its focused detail uses
ordinary definition-list text nodes for the already bounded backend detail
fields. Script-shaped and URL-shaped values remain literal text and create no
links, images, markup, or code side effects. A classified audit authorization
failure remains inside the `Activity evidence` region while connection context
and permitted Endpoint navigation remain usable.

At a controlled 1100-by-720 browser viewport, the document remained exactly at
the viewport extents. All four filter fields and Apply control fit within an
826-by-76-pixel filter bar with matching client and scroll extents. The
1,120-pixel audit table stayed inside its 826-pixel horizontal scroller with
Event ID and Actions pinned. The detail overlay occupied the expected 380 to
1,100 horizontal and 48 to 720 vertical bounds; its 596-pixel content area had
an equal scroll height for the controlled record. The browser reported no
application errors.
