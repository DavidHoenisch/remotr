# Task 4.2 workspace-load red evidence

- Verification IDs: `OS-DFV-003`, `OS-DFV-004`
- Public seams: typed desktop workspace service and authenticated Admin API
- Test layers: real Admin client over controlled mTLS, exact request inventory,
  deterministic fan-out overlap, safe workspace mapping, section-local
  authorization failure, and credential/response canary scan
- Command: `go test ./... -run '^TestWorkspaceServiceLoadsCompleteAndSectionForbiddenResults$' -count=1`
- Result: intended red (`exit 1`)

The compiler reports that `NewWorkspaceService` does not exist. The committed
test independently supplies one Operator identity, two Fleets, two Endpoints,
per-Fleet State reports, one Change request, and one cursor-paged audit event.
It requires every top-level and dependent request exactly once, concurrent
top-level work bounded between two and four active requests, stable safe output,
and internally consistent resource identities.

The second case returns a response-body canary with `403 Forbidden` only from
Activity. Endpoint, Fleet, State, and Change request results must remain ready,
while Activity alone becomes authorization-unavailable with no rows or cursor.
Neither result may expose the response canary, credential state path, PEM data,
unrelated bootstrap token, audit actor fingerprint, or client IP. Task 4.3 adds
the minimum cancellable bounded loader and safe mappings needed to make both
cases green.
