# 13.1 root desktop targets — red evidence

- Verification IDs: OS-DOA-001 and OS-DOA-018.
- Public seam: the root `desktop-test`, `desktop-dev`, and `desktop-build` developer commands and their Linux prerequisite diagnostics.
- Selected layers: focused Make command-contract checks, negative Linux/GTK/WebKit prerequisite cases, nested-module Go tests, frozen-lockfile frontend tests, a production frontend build, and the root regression suite.

## Intended red

Command:

`mise exec -- go test ./internal/desktoplayout -run '^(TestDesktopRootTargetsUsePinnedNestedModuleWorkflow|TestDesktopLinuxPrerequisitesSelectSupportedWebKit)$' -count=1`

The command-contract test reported that all three root targets, the nested desktop/frontend paths, frozen-lockfile setup, pinned-module Wails invocations, and test commands were absent. Its dry runs also failed because Make had no matching rules. The Linux prerequisite cases failed because the prerequisite selector did not exist. This is the intended red at the public developer-command seam.

## Green verification

- The focused command and prerequisite suite passed, including WebKitGTK 4.1 and 4.0 selection plus non-Linux, missing GTK, and missing WebKitGTK negatives.
- `make desktop-test` installed from the frozen lockfile with pnpm 11.7.0, passed the nested desktop Go suite, and passed 33 frontend test files containing 68 tests.
- `make desktop-build` ran Wails v2.12.0 with the `webkit2_41` tag and produced the native Linux/amd64 `remotr-desktop` binary after a production frontend build.
- `mise exec -- openspec validate add-desktop-admin-app --strict` passed.
- `mise exec -- make test` passed all 37 discovered fuzz seed corpora and the complete root Go suite.
