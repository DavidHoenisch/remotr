# Task 4.1 safe view-model red evidence

- Verification IDs: foundational model contract for `OS-DFV-003` through
  `OS-DFV-026` and `OS-DFA-001` through `OS-DFA-004`
- Public seam: typed desktop Wails/JSON view-model boundary
- Test layers: exact status-vocabulary table, recursive type-shape inventory,
  stable JSON-tag inspection, and credential-field/canary scan
- Command: `go test ./... -run '^TestDesktopViewModelsAreTypedBoundedAndCredentialFree$' -count=1`
- Result: intended red (`exit 1`)

The compiler reported that the required Operator, section, Endpoint, Fleet,
State, Change request, Activity, action-result, classified-error, and snapshot
types did not exist. The committed contract rejects raw byte slices, maps,
interfaces, functions, channels, credential/certificate/fingerprint-shaped
fields, raw diagnostic content, and client IPs. It also fixes the documented
compliance and freshness vocabulary without inferring online/offline presence.

The green implementation contains only concrete, purpose-specific fields and
stable JSON names. Full desktop tests, static analysis, nested-module integrity,
and module verification pass without adding a new Wails-bound method.
