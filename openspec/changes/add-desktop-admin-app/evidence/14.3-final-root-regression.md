# 14.3 Final root regression and dependency-independence gate

Date: 2026-07-16

The root gate ran from a fresh `/tmp/remotr-14.3-gocache` build cache:

`make test`

Results:

- all 37 discovered root fuzz seed corpora passed;
- `go test -mod=vendor ./...` passed the complete server, agent, Admin CLI,
  provider, acceptance, and testing-support tree; and
- no package in the root suite required the nested desktop dependency graph.

As a separate dependency check, `go list -deps -mod=vendor ./...` resolved 568
root dependencies. None used the `github.com/wailsapp/` namespace or the nested
`github.com/DavidHoenisch/remotr/desktop` module path. This confirms that routine
root verification remains independent of Wails, Node, and native WebView build
dependencies.
