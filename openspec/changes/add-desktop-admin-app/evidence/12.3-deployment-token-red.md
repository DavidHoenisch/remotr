# 12.3 deployment-token parity — intended RED

- Mapped CLI workflows: `remotr deployment create`, `remotr deployment list`, `remotr deployment show`, and `remotr deployment revoke`.
- Verification IDs: OS-DOA-013, OS-DOA-016, OS-DOA-017, OS-DOA-019, OS-DFA-001 through OS-DFA-004, and OS-DFA-026 through OS-DFA-027.
- Public seam: purpose-specific Remotr Desktop/Wails application methods backed by a controlled mutual-TLS Admin API fixture, native clipboard, and native protected save destination.
- Test layers: focused native integration, rendered action flow, malformed/unauthorized/destructive negative cases, secret canaries, persistence/cleanup evidence, and bridge allowlist.
- Focused command: `go test -run TestDeploymentTokenParityProtectsOneTimeSecretAndUsesServerMetadata ./...` in `desktop/`.
- Intended RED: compilation must fail because the typed create/list/show/revoke methods, one-time secret lifecycle, and protected native save boundary do not exist yet.
- Rendered-flow command: `pnpm exec vitest run src/actions/DeploymentTokenFlow.test.tsx` in `desktop/frontend/`.
- Rendered-flow RED: the test could not find a `Deployment tokens` navigation control because no reusable-token workspace was rendered.

## Green evidence

- The focused mutual-TLS seam passed create/list/show/revoke, malformed-input, authorization, exact confirmation, native clipboard, owner-only atomic persistence, server-error redaction, profile-switch cleanup, and shutdown cleanup assertions.
- The rendered action flow passed one-active-secret, explicit review, copy/save/clear, metadata inspection, exact destructive confirmation, server-authoritative refresh, and Activity refresh assertions.
- The Wails binding allowlist permits the one-time secret only on the exact create result and rejects credential-bearing fields on every other output.
- All 26 frontend test files (59 tests), the production frontend build, the complete desktop Go suite, repository `make test`, parity drift, and strict OpenSpec validation passed.
