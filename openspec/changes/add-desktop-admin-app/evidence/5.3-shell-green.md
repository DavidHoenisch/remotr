# Task 5.3 visual shell green evidence

- Verification IDs: `OS-DFV-001`, `OS-DFV-002`
- Public seam: rendered desktop component and mounted frontend application
- Test layers: Testing Library semantic roles/names and user interaction,
  TypeScript production build, full frontend unit suite, and real-browser
  minimum-viewport geometry
- Focused command: `pnpm exec vitest run src/shell/AppShell.test.tsx`
- Focused result: pass (`2` tests)
- Frontend command: `pnpm test`
- Frontend result: pass (`3` files, `7` tests)
- Production command: `pnpm build`
- Production result: pass (`tsc -b` and Vite production bundle)

The mounted shell provides one compact top connection bar, a grouped primary
navigation rail, one main page heading and content frame, and an in-window
topmost detail overlay. Profile, server, Operator, and Fleet context remain in
the same window while the active page changes. The disconnected application
entry state is explicit and does not fabricate fleet data.

At a controlled 1100-by-720 browser viewport, the connection bar measured 48
pixels and the navigation rail measured 224 pixels. The document client and
scroll extents were both exactly 1100 by 720, so the shell introduced no page
overflow. The Fleet-scope control and all six navigation controls remained
inside the viewport; selecting Activity replaced the level-one heading and
content region in place while its navigation control became current.

The first focused green attempt also exposed missing test cleanup between the
two committed red cases. Adding deterministic cleanup removed the duplicate
render without changing the behavioral assertions; the isolated focused run
then passed.
