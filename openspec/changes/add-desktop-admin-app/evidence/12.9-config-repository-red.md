# 12.9 Configuration repository parity — red evidence

- Verification IDs: OS-DOA-014, OS-DOA-019, and OS-DFA-028.
- Public seam: opaque native-selected working-tree handles backed by the shared `scaffold`, `configrepo`, `configcompose`, and `hubcatalog` Go packages.
- Selected layers: focused filesystem integration for init, validate, discover, render, native save, and Hub import; traversal and symlink-escape negatives; bounded frontend preview; bridge allowlist; parity inventory drift.
- Authority boundary: local operations never shell out to `remotr`, register server configuration, mutate server desired state, or stage, commit, push, merge, or apply repository content.

## Intended backend red

Command:

`cd desktop && go test ./... -run '^TestConfigRepositoryParityConfinesLocalSharedPackageWorkflows$'`

The test failed to compile because `ConfigRepositoryService`, its options, the App injection option, and the typed choose/init/validate/discover/render/save/Hub methods did not exist. This was the intended failure at the public desktop working-tree seam.

## Intended frontend red

Command:

`cd desktop/frontend && pnpm exec vitest run src/configuration/ConfigRepositoryFlow.test.tsx`

Vitest failed to resolve `./ConfigRepositoryPage`. This was the intended failure proving the semantic selection, validation, discovery, preview/save, Hub import, and initialization surface was absent.
