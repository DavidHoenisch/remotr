# Task 4.4 Landscape secret and transport evidence

- Verification IDs: OS-UPM-054 through OS-UPM-056.
- Public seams: active-artifact authorization, registered-resource effective hashing, registered provider preflight, and the Ubuntu Pro qualification inventory.
- Authorization command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/secrets -run '^TestArtifactAuthorizesExactLandscapeSecretPurposes$' -count=1`.
- Authorization red: neither Landscape reference was an authorized artifact consumer.
- Authorization green: registration-key and CA references authorize only `landscape-registration-key` and `landscape-ca`, respectively, at the exact resource address; swapped purposes, the Ubuntu Pro token purpose, and a sibling address are denied.
- Hash command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/resourceregistry -run '^TestUbuntuProEffectiveHashScopesLandscapeSecretIdentities$' -count=1`.
- Hash red: `landscape.registrationKeyRef` had no safe hash purpose.
- Hash green: token, registration key, and CA resolve as three exact-purpose safe identities; every returned canary buffer is cleared and the effective hash remains canonical.
- Native-boundary red: the provider reached the read-only Ubuntu Pro process probe before rejecting an unavailable Landscape transport.
- Native-boundary green: any Landscape block is rejected during provider preflight before a runner, InputRunner, or resolver call because no qualified protected native secret transport exists.
- Unadvertised evidence: the checked-in `landscape` service and SaaS/self-hosted environment rows remain `untested`; isolated base-row promotion advertises only `resource:ubuntu-pro`, as proven by `TestBaseAttachmentCannotManufactureServiceCapabilities`.
- Broader checks: all `./internal/secrets` tests and the focused field-policy, effective-hash, provider-preflight, and qualification-boundary sets pass.
