# Task 4.1 token-canary cross-layer evidence

- Verification IDs: OS-UPM-010 through OS-UPM-013 and OS-UPM-016.
- Authenticated authorization and scope: `TestResolveUbuntuProTokenAuthorizesExactActiveArtifactScope` admits only the endpoint/fleet, active artifact digest, exact resource address, selected version, and `ubuntu-pro-token` purpose; sibling scope and stale artifacts fail.
- Resolution and hash: the registered field policy treats token and Landscape references as credential references, never inline values. `TestUbuntuProEffectiveHashUsesSafeTokenIdentityAndClearsMaterial` hashes safe version identity and clears resolved bytes.
- Process/error boundary: attachment tests require the literal versioned full-token endpoint with protected stdin, reject argv/environment/shell fallback, clear request and resolver buffers, and prove localized/process/token canaries do not enter errors.
- Plan and audit: `TestUbuntuProDynamicRiskAndSafePlan` excludes token fields and identity from approval evidence. `TestUbuntuProChangeControlAuditOmitsTokenIdentity` carries that projection through change-request creation and target-outcome audit history and serializes no token, contract, or account detail.
- Rollback and report: restart tests decrypt the strict rollback payload and scan the store filesystem without finding token reference/material; secret-bearing snapshot fields fail closed. Public Check reports only bounded health, declared state, apply outcome, rollback/residual class, and reboot state.
- Focused and full server-secret, secret policy, registry, Ubuntu Pro applicator, and change-control suites passed throughout their red-green slices.
