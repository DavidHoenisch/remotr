# Task 10.5 separate-capability evidence

- Verification IDs: OS-UPM-058 and OS-UPM-059.
- Configuration seam red: the strict decoder rejected client settings and one-shot maintenance only as unknown YAML, without explaining their separate ownership. Green parsing gives bounded guidance for APT News, proxy, refresh, telemetry, fix, upgrade, hardening, unattended-upgrade, contract-refresh, and reboot-execution fields while retaining strict known-field decoding.
- Provider seam: a service-enable response with `reboot_required: true` produces only the standard reboot-required activation and a bounded declared-service report. Its exact process fake accepts only `/usr/bin/pro api` and observes only the typed enable endpoint, proving the provider does not execute package upgrades, fixes, hardening, shells, or reboot commands.
- Secret and output boundary: the report contains only attachment and the declared `usg` state; it carries no client settings, maintenance commands, native text, contract data, or undeclared state.
- Focused tests and the full Ubuntu Pro applicator and model suites pass.
