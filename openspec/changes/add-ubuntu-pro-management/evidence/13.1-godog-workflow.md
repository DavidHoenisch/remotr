# Task 13.1 public Godog workflow evidence

`test/acceptance/features/ubuntu_pro_management.feature` uses only operator,
authenticated Sync/apply, endpoint, and fleet-state vocabulary. Its four
scenarios cover:

- active synthetic-token attachment and redacted fleet state;
- ordinary `esm-apps` convergence and idempotent resync;
- explicit destructive detachment;
- owned FIPS-to-Livepatch conflict ordering;
- authenticated capability blocking for unadvertised access-only mode; and
- FIPS reboot reporting without a reboot execution boundary.

Step implementations parse canonical schema-1 YAML through the public model
seam, use the exact capability qualification inventory, execute the public
provider through `executor.Apply`/`executor.Check`, and keep Canonical behind a
stateful deterministic process-boundary double. Only the protected attachment
request sees the synthetic canary; public Apply/Check and fleet reports are
asserted redacted.

Intended red: all 16 steps were initially undefined. Green:
`env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor
./test/acceptance -count=1` passed all acceptance scenarios.

This workflow does not use or imply a live Canonical token or entitled native
effect.
