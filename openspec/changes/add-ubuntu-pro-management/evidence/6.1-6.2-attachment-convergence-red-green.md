# Tasks 6.1 and 6.2 attachment convergence evidence

- Verification IDs: OS-UPM-010, OS-UPM-011, OS-UPM-014, and OS-UPM-024.
- Public seam: registry-compatible Ubuntu Pro provider `Apply`, `executor.Check`, and second `Apply`.
- Red: after native attachment succeeded, a post-attachment `is_attached: true` response carrying a stable warning was accepted; Apply was not using the health-aware public Check contract.
- Green: attachment invokes only `/usr/bin/pro api u.pro.attach.token.full_token_attach.v1 --data -` with typed protected JSON, `auto_enable_services: false`, and an empty returned enabled set. Token and marshaled request buffers are cleared on every return path.
- Green convergence: an unattached endpoint resolves once, attaches once, passes a structured second Check, and a later explicit Check plus second Apply remain compliant without further resolution or mutation. An already-attached endpoint resolves no token. A stable invalid-token failure remains observably unattached and is not retried or replaced.
- Warning regression: post-attach warnings fail the second Check using only the stable reason and do not copy localized messages.
- Focused suite: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1` passed.
- Registry-facing regression: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/resourceregistry -run '^TestUbuntuProProviderPreflightPrecedesTokenResolutionAndAttachment$' -count=1` passed.
