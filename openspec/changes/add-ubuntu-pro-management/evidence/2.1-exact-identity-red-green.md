# Task 2.1 exact identity red-green evidence

## Exact Ubuntu identity

- Verification IDs: OS-UPM-005 and OS-LPC-012.
- Public seam: `facts.ReadIdentity` with filesystem and process boundaries supplied by the caller.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/agent/facts -run '^TestReadIdentityPreservesExactUbuntuAndFamilyFacts$'`
- Independently expected result: matching exact Ubuntu source records and exact `dpkg-vendor --query Vendor` evidence preserve `ID`, `ID_LIKE`, source consistency, vendor, release, and Debian-family compatibility.
- Intended red observed: build failed with `undefined: facts.ReadIdentity`.
- Green result: the focused test passes; exact ID/version/source-consistency/vendor facts coexist with normalized Ubuntu/Debian-family facts, and vendor evidence uses only `/usr/bin/dpkg-vendor --query Vendor`.

## Pop!_OS derivative boundary

- Verification IDs: OS-UPM-004 and OS-LPC-011.
- Public seam: `facts.ReadIdentity` plus `Facts.ExactUbuntu`.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/agent/facts -run '^TestReadIdentityKeepsPopOSInDebianFamilyWithoutExactUbuntuIdentity$'`
- Independently expected result: Pop!_OS remains Debian-family compatible while exact Ubuntu identity stays false even with Ubuntu lineage and matching vendor text.
- Intended red observed: build failed because `Facts.ExactUbuntu` did not exist.
- Green result: both the exact-Ubuntu regression and Pop!_OS boundary test pass; derivative family compatibility is preserved while the exact predicate remains false.

## Second derivative diagnostic

- Verification IDs: OS-UPM-004 and OS-LPC-011.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/agent/facts -run '^TestReadIdentityRejectsSecondUbuntuDerivativeWithBoundedReason$'`
- Independently expected result: Linux Mint remains Debian-family compatible and returns a stable exact-ID rejection reason.
- Intended red observed: build failed because `Facts.ExactUbuntuReason` did not exist.
- Green result: the Linux Mint test passes with the bounded exact-ID reason while generic Debian-family facts remain available.

## Conflicting os-release sources

- Verification IDs: OS-UPM-005 and OS-LPC-012.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/agent/facts -run '^TestReadIdentityFailsClosedWhenOSReleaseSourcesConflict$'`
- Independently expected result: both sources are observably considered, inconsistent ID/release evidence cannot satisfy exact Ubuntu, and the reason is bounded.
- Intended red observed: build failed because `Facts.OSReleaseSourceCount` did not exist.
- Green result: the conflicting-source test and all prior identity regressions pass; the facts record two observed sources while refusing exact Ubuntu eligibility.

## Malformed and duplicate keys

- Verification IDs: OS-UPM-005 and OS-LPC-012.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/agent/facts -run '^TestReadIdentityClassifiesMalformedAndDuplicateOSRelease$'`
- Independently expected result: malformed quoted values and duplicate exact keys fail closed through a stable ambiguity category.
- Intended red observed: build failed because `facts.ErrAmbiguousOSRelease` did not exist.
- Green result: malformed and duplicate fixtures pass through `errors.Is(..., ErrAmbiguousOSRelease)`, and the earlier identity cases remain green.

## Boundary table and bounded fuzz property

- Verification IDs: OS-UPM-004, OS-UPM-005, OS-LPC-011, and OS-LPC-012.
- Public seam: `facts.ReadIdentity`.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/agent/facts`
- Independently expected result: single-source identity, exact vendor comparison, duplicate-key and missing-release boundaries behave deterministically; any bounded parser input returns bounded diagnostics and never panics.
- Intended red observed: fuzz seed 2 produced a 624-byte error, exceeding the 512-byte diagnostic bound.
