# Task 11.3 explicit detach evidence

- Verification IDs: OS-UPM-025 through OS-UPM-027.
- Public seam: destructive lifecycle ApplyResult followed by public Check where needed.
- Red: lifecycle `detached` failed immediately as unimplemented.
- Green: an attached endpoint invokes only `/usr/bin/pro api u.pro.detach.v1`, proves `is_attached: false`, reports the typed reboot result through the standard activation, and returns rollback class `none`.
- Already-detached and absent desired ownership are no-change paths with only one read-only status call. They perform no detach, resolution, or service mutation.
- Stable native detach failure preserves only its validated code, claims no rollback, and a separate follow-up Check determines the endpoint remains drifted/attached. A success response followed by attached post-state is an explicit failure with no automatic reattach.
- Exact fake boundaries reject ordinary `pro detach`, shells, or any non-versioned argv. Focused and full Ubuntu Pro applicator suites passed.
