# Task 13.5 release-gate evidence (partial)

Task 13.5 remains open because the authenticated fleet workflow, Ubuntu Pro
and Landscape VM selectors, and repository-wide security gate have not all
passed. No qualification row was promoted from this partial evidence.

## Passing checks

- Focused public configuration acceptance:
  `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./cmd/remotr -run '^TestApp_config(ValidateAcceptsTypedUbuntuProResource|ValidateAcceptsCompleteUbuntuProCatalogAndLandscape|UbuntuProRepositoryWorkflow|ValidateRejectsUnsafeUbuntuProAuthoring)$' -count=1`.
- Traceability audit:
  `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go run -mod=vendor ./scripts/traceability-lint.go`.
- Provider qualification audit:
  `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go run -mod=vendor ./scripts/ubuntu-qualification-audit.go`.
- Strict OpenSpec validation:
  `openspec validate add-ubuntu-pro-management --strict`.
- Final local suite after the last implementation and mutation changes:
  `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- make test`.
  The sandboxed attempt failed only because `httptest` could not bind a local
  loopback socket; the same revision passed with loopback permission, including
  all 59 discovered fuzz seed corpora and `go test -mod=vendor ./...`.

## Unmet gates

- `make gosec` completed against the repository and reported 282 existing
  findings. The focused Ubuntu Pro packages reported seven findings, including
  low-confidence endpoint/purpose string detections and established process or
  file-boundary findings. No suppression or unrelated security cleanup was
  introduced, so the security gate remains failing.
- The disposable Vagrant home has no `vagrant-libvirt` plugin, the current
  session cannot access `qemu:///system`, and `/dev/kvm` is unavailable.
  Therefore the negative-identity, release-specific, specialized-service,
  failure, and Landscape selectors could not be executed on this host.
- No Ubuntu Pro token was supplied through the protected qualification input,
  so the subscription-seat lifecycle and authenticated end-to-end attachment
  workflow could not be exercised.
- Landscape remains deliberately unadvertised: its protected native secret
  transport and externally observable convergence seam have not been proven.
