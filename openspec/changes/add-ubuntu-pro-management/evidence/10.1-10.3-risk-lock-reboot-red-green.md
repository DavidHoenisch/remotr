# Tasks 10.1 through 10.3 risk, lock, and reboot evidence

- Verification IDs: OS-UPM-033 and OS-UPM-050 through OS-UPM-053.
- Red risk/plan: every resource was statically sensitive and every plan claimed no rollback. Green descriptors keep attach/ordinary enablement sensitive, elevate FIPS/FIPS Updates/real-time-kernel to boot risk, classify disable/purge/Landscape unenrollment/detach as destructive, and honor only authored escalation. Plans contain no token, reference, account, or contract material and distinguish best-effort from no-automatic-rollback operations.
- Red locks: snap, boot, and Landscape selections retained only the two base locks. Green registry wiring always preserves `ubuntu-pro` and `package-manager:apt`, adds `package-manager:snap`, `boot`, or `landscape` where cataloged, and retains additional authored locks. The engine's shared native-lock contract supplies serialization/cancellation; task 5.4 proves stable native Pro contention classification.
- Red reboot: a successful enable result with `reboot_required: true` produced a legacy changed result with no activation. Green structured Apply emits only the standard `reboot-required` activation and `required` flag.
- Typed reboot-status coverage accepts exactly `no`, `yes`, and `yes-kernel-livepatches-applied`; unknown values fail closed. Exact process assertions prove the provider invokes only versioned Pro API endpoints and no reboot command.
- Focused suites passed: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/resourceregistry -run 'TestUbuntuPro(DynamicRiskAndSafePlan|CatalogedLockDomains)' -count=1` and the full Ubuntu Pro applicator package.
