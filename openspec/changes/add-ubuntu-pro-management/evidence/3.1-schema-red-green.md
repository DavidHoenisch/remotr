# Task 3.1 Ubuntu Pro schema red-green evidence

## Valid attached resource

- Verification ID: OS-UPM-001.
- Public seam: operator `config validate` and `config render`, followed by the agent parser.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./cmd/remotr -run '^TestApp_configValidateAcceptsTypedUbuntuProResource$'`
- Independently expected result: a typed attached resource with a versioned `tokenRef` and unique catalog services validates, renders deterministically, and preserves only the unresolved reference.
- Intended red observed: build failed because `Configuration.UbuntuPro` and `UbuntuProAttached` did not exist.
- Green result: the public CLI validates and renders the typed resource; the agent parser recovers the attached lifecycle, unresolved token reference, and two declared services.

## Complete stable catalog

- Verification ID: OS-UPM-035.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./cmd/remotr -run '^TestApp_configValidateAcceptsCompleteUbuntuProCatalog$'`
- Independently expected result: all ten stable services validate with typed modes, variants, and disable behavior.
- Intended red observed: config validation rejected `esm-apps` because the initial minimal catalog contained only `esm-infra` and `livepatch`.
- Green result: the full stable service list validates and service options are checked against the catalog.

## Unsafe and inconsistent authoring

- Verification IDs: OS-UPM-002, OS-UPM-003, OS-UPM-034, and OS-UPM-058.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./cmd/remotr -run '^TestApp_configValidateRejectsUnsafeUbuntuProAuthoring$'`
- Independently expected result: lifecycle/token inconsistencies, duplicate/unknown/historical services, raw arguments, provider options, client settings, maintenance events, and option/state cross-products are rejected with field-specific diagnostics.
- Intended red observed: `enableMode` on a disabled service and `disableMode` on an enabled service were both accepted; every other negative case already failed closed.
- Green result: both option/state cross-products now fail, and all valid/negative Ubuntu Pro config-CLI tests pass together.
