# Task 3.1 Ubuntu Pro schema red-green evidence

## Valid attached resource

- Verification ID: OS-UPM-001.
- Public seam: operator `config validate` and `config render`, followed by the agent parser.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./cmd/remotr -run '^TestApp_configValidateAcceptsTypedUbuntuProResource$'`
- Independently expected result: a typed attached resource with a versioned `tokenRef` and unique catalog services validates, renders deterministically, and preserves only the unresolved reference.
- Intended red observed: build failed because `Configuration.UbuntuPro` and `UbuntuProAttached` did not exist.
