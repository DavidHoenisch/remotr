# Tasks 9.1 and 9.2 specialized-service evidence

- Verification IDs: OS-UPM-050 through OS-UPM-053.
- Catalog red/green: specialized rows had risk and locks embedded in registry switches and no typed observation, activation, recovery, or incompatibility metadata. The checked-in catalog now exposes copy-safe metadata for every service row; registry risk/locks and public plan rollback consume it directly.
- USG is normalized from native `cis` only into declared `usg` service/tooling state. Its public ApplyResult/Check regression proves no hardening, security-fix, package-upgrade, shell, or reboot execution is smuggled into the provider.
- FIPS, FIPS Updates, and real-time-kernel rows carry boot risk/lock metadata and native reboot-result activation. Full high-impact control changes advertise rollback `none`; fault injection proves a later failure triggers only a read-only residual-state check and never an unsafe generic disable inverse.
- All six enabled pairs among Livepatch, FIPS, FIPS Updates, and real-time kernel produced the intended red acceptance, then became catalog-driven configuration failures containing both conflicting service names before provider creation or secret resolution.
- Real-time variants are typed and durably observed; access-only/explicit modes remain unsupported where the versioned API cannot distinguish them; explicit purge remains destructive with rollback `none`.
- Focused and full model, Ubuntu Pro applicator, and registry suites passed. Platform qualification, package/kernel/boot effect claims, and capability advertisement remain gated by tasks 9.3 and 12.
