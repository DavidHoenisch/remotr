# Task 3.5 capability requirements and config workflow evidence

- Verification IDs: OS-UPM-001, OS-UPM-034 through OS-UPM-036, and OS-UPM-043 through OS-UPM-045.
- Public seams: `capabilitymatrix.Requirements` and the operator `config discover`, `config validate`, and `config render` commands.
- Focused requirement command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/capabilitymatrix -run '^TestUbuntuProRequirementsAreExactAndCatalogDerived$' -count=1`.
- Requirement red: the resource emitted only `resource:ubuntuPro` and `schema:1`; it omitted every service, option, variant, disable-behavior, and Landscape requirement and did not use the normalized base capability identity.
- Requirement green: the exact sorted set includes normalized `resource:ubuntu-pro`, each authored service, default and explicit options, the selected variant, default and explicit disable behaviors, and the selected self-hosted Landscape environment, with no sibling capability uplift.
- Focused workflow command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./cmd/remotr -run '^TestApp_configUbuntuProRepositoryWorkflow$' -count=1`.
- Workflow red: the checked-in source-only Ubuntu Pro repository did not exist.
- Workflow green: public discovery reports the exact requirement set; validation succeeds; two public renders are byte-identical; the rendered parser retains `ubuntu-pro.primary-subscription`; and the repository walk finds no `desired.yaml` or `crons.yaml`.
- Broader checks: the complete `./internal/capabilitymatrix` suite and all `./cmd/remotr` tests prefixed `TestApp_config` pass.
