# Tasks 8.3 and 8.4 dependency graph evidence

- Verification IDs: OS-UPM-046 through OS-UPM-049.
- Public seam: Ubuntu Pro provider Apply before its first service mutation.
- Red ownership slice: `usg` with a disabled, omitted `esm-apps` dependency crossed directly into enablement instead of reading and enforcing the versioned graph.
- Green ownership: Apply reads bounded `u.pro.services.dependencies.v1`, rejects unknown catalog members, and blocks when a disabled dependency is neither already enabled nor explicitly declared enabled. An enabled omitted conflict is likewise blocked.
- Red ordering slice: authoring `usg` before declared `esm-apps` attempted the target first. Green planning topologically enables dependencies first, pre-disables explicitly owned active conflicts, orders targets deterministically, and processes remaining disables afterward.
- Boundary matrix proves already-satisfied dependencies remain outside ownership, declared conflicts disable before targets enable, cycles fail before mutation, unknown graph members fail closed, and unexpected native enabled/disabled response members are rejected.
- Reconciliation uses the checked-in service catalog and alias contract as the only accepted graph vocabulary; relation titles are discarded and only bounded stable codes survive parsing.
- Focused and broader commands passed: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -run 'TestApplicator(BlocksOmittedDisabledDependency|OrdersDeclaredDependenciesBeforeTargets|DependencyAndConflictPlanningBoundaries)' -count=1` and the full package suite.
