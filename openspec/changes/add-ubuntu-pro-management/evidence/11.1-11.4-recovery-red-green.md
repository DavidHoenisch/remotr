# Tasks 11.1 and 11.4 recovery evidence

- Verification IDs: OS-UPM-022 through OS-UPM-024, OS-UPM-048, and OS-UPM-057.
- Apply-local fault evidence covers post-attachment entitlement failure, later ordinary-service failure, reverse dependency restoration, unexpected undeclared side effects, post-rollback observation, rollback failure, and cataloged no-automatic inverse behavior. A new attachment is detached only when the failed Apply created it; a pre-existing attachment is never detached.
- High-impact FIPS/FIPS Updates/real-time rows use catalog recovery `none`. After partial work, the provider performs a read-only residual-state check, reports possible native effects and rollback `none`, and makes no generic disable inverse.
- Durable red/green: attachment and ordinary-service crash tests initially had no rollback-store seams or service record. The provider now reserves the shared protected store, arms a strict versioned non-secret snapshot before mutation, reconstructs recovery after restart, restores only catalog-restorable declared state in reverse order, verifies it, and never resolves enrollment secrets during recovery.
- Snapshot payloads contain only version, prior attachment state, and bounded declared service name/enabled/variant fields. Version-1 records containing token or contract fields fail closed before API access and remain armed for investigation.
- Successful convergence and successful recovery leave acknowledged/rolled-back lifecycle metadata but no loadable payload. Filesystem scanning finds no plaintext token canary. Malformed recovery leaves its payload armed rather than deleting evidence.
- Focused, full, and race-sensitive Ubuntu Pro applicator checks pass. The attachment and service recovery contract completes task 11.2 without claiming filesystem transactionality.
