# Task 3.3 parser boundary and fuzz evidence

- Verification IDs: OS-UPM-001 through OS-UPM-003, OS-UPM-034 through OS-UPM-035, OS-UPM-054, OS-UPM-056, and OS-UPM-058.
- Public seam: canonical desired-state parser and renderer.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/models -run '^TestParseStateUbuntuPro(Boundaries|CanonicalRoundTripIsDeterministic)$'`
- Independently expected result: invalid enums, alias confusion, cross-service options, duplicate fields, more than 32 Landscape tags, strings over 256 bytes, and nondeterministic round trips are rejected.
- Intended red observed: 33 Landscape tags and a 257-byte computer title were accepted; all other boundary cases and deterministic round-trip evidence passed.
