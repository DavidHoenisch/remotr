# Tasks 12.3 through 12.6 service-row evidence (partial)

The public Ubuntu Pro provider seam ran on each pinned Ubuntu amd64 guest with
the stateful, deterministic `pro api` double and a synthetic token. Each
catalog service, default `full` enable behavior, and realtime-kernel variant was
exercised independently as a Go subtest. Every row proved the exact protected
enable request, observable compliant Check, idempotent second Apply, and boot
signal where catalog risk requires it.

- `make provider-matrix-vm-ubuntu-pro-20.04` — all service/full-mode/variant
  subtests passed on `cloud-image/ubuntu-20.04@20250624.0.0`.
- `make provider-matrix-vm-ubuntu-pro-22.04` — all service/full-mode/variant
  subtests passed on `cloud-image/ubuntu-22.04@20260711.0.0`.
- `make provider-matrix-vm-ubuntu-pro-24.04` — all service/full-mode/variant
  subtests passed on `cloud-image/ubuntu-24.04@20260705.0.0`.
- `make provider-matrix-vm-ubuntu-pro-26.04` — all service/full-mode/variant
  subtests passed on `cloud-image/ubuntu-26.04@20260720.0.0`.
- `make provider-matrix-vm-ubuntu-pro-service-fips-24-04` and
  `make provider-matrix-vm-ubuntu-pro-variant-realtime-kernel-raspi-24-04`
  separately proved tuple-selector routing.
- `make provider-matrix-vm-ubuntu-pro-enable-mode-esm-infra-full-24-04`
  proved the default-full selector and exact `access_only: false` request.

The manifest promotes only service, default `full` behavior, and variant rows.
The passing `full` capability covers the omitted default and its exact
`access_only: false` request; it does not make explicit invocation-mode state
observable. Explicit mode assertions and both disable-behavior rows remain
unadvertised because enabled-state observation cannot recover the prior mode,
and disabled-state observation cannot distinguish retained packages from
purge. A successful invocation is not substituted for durable state evidence.

No live Canonical token, subscription attachment, or entitled package, snap,
repository, kernel, boot artifact, or compliance-tool effect was exercised or
inferred. Tasks 12.3 through 12.6 remain open pending their drift/recovery and
explicit unobservable-option dispositions.
