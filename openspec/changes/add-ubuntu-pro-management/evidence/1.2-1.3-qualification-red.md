# Tasks 1.2–1.3 qualification inventory red evidence

- Verification IDs: OS-UPM-006 through OS-UPM-009, OS-UPM-036, OS-UPM-043 through OS-UPM-045, OS-UPM-060, OS-LPC-013 through OS-LPC-020, and OS-LPC-022.
- Public seam: qualification manifest and capability advertisement.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/ubuntuproqualification`
- Independently expected result: the checked-in inventory starts with exactly four Ubuntu LTS amd64 API-v32 attachment rows, keeps every service/option tuple untested, records derivative and out-of-scope non-claims, and cannot derive a service capability from a passing base row.
- Intended red observed: `no non-test Go files in .../internal/ubuntuproqualification`; the manifest loader and advertisement boundary did not yet exist.
- Green result: the focused package passes with four exact untested base rows, 220 independent untested service/option rows, required negative/non-claim inventory, and isolated attachment-only advertisement after a base-row promotion.
- Broader checks: traceability lint passes and `openspec validate add-ubuntu-pro-management --strict` reports the change valid.
