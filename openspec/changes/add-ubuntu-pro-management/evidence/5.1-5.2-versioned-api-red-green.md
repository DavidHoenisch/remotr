# Tasks 5.1 and 5.2 versioned API boundary evidence

- Verification IDs: OS-UPM-032, OS-UPM-033, OS-UPM-037 through OS-UPM-040, OS-UPM-043, OS-UPM-044, OS-UPM-046, OS-LPC-019, and OS-LPC-020.
- Public seam: the shell-free Ubuntu Pro Client process boundary.
- Canonical contract checked: the Ubuntu Pro Client v32 API reference documents the common `_schema_version: v1` envelope and all nine endpoint names, attributes, and parameter shapes implemented here.
- Focused suite: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1`.
- Red sequence: each endpoint test failed independently because its API method and typed result did not yet exist; no batch of endpoint implementations preceded its test.
- Green read-only/no-argument boundaries: `/usr/bin/pro api u.pro.version.v1`, `u.pro.status.is_attached.v1`, `u.pro.status.enabled_services.v1`, `u.pro.services.dependencies.v1`, `u.pro.security.status.reboot_required.v1`, and `u.pro.detach.v1`.
- Green parameterized boundaries: `/usr/bin/pro api <endpoint> --data -` for `u.pro.attach.token.full_token_attach.v1`, `u.pro.services.enable.v1`, and `u.pro.services.disable.v1`, with typed bounded JSON and no `--args`, token argv, shell, or ordinary `pro` subcommand.
- Typed results cover installed/client version, attachment, enabled services and variants, stable-code dependency/incompatibility relations, attachment/service transitions, detach effects, and reboot state. The documented `cis` status alias is normalized to `usg`; localized dependency titles and enable messages are discarded.
- Output and input are bounded; collections are capped and validated against the checked-in catalog. Task 5.3 remains open for adversarial envelope tables and fuzzing.
