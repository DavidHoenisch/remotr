# Tasks 12.3 through 12.6 base-row evidence (partial)

Only the base attachment rows are qualified here. Tasks 12.3 through 12.6
remain open because their service, mode, variant, and disable-behavior rows
must still run independently. Every capability row remains `untested`; no
service capability is advertised from this evidence.

All commands used the credential-free synthetic API double, verified exact
Ubuntu amd64/APT identity, exercised attach, public Check, second Apply, and
detach, and completed guest-secret cleanup plus libvirt teardown:

- `make provider-matrix-vm-ubuntu-pro-20.04` —
  `cloud-image/ubuntu-20.04@20250624.0.0`: pass.
- `make provider-matrix-vm-ubuntu-pro-22.04` —
  `cloud-image/ubuntu-22.04@20260711.0.0`: pass.
- `make provider-matrix-vm-ubuntu-pro-24.04` —
  `cloud-image/ubuntu-24.04@20260705.0.0`: pass.
- `make provider-matrix-vm-ubuntu-pro-26.04` —
  `cloud-image/ubuntu-26.04@20260720.0.0`: pass.

The shared negative-identity and secret-canary selectors also passed on the
pinned 24.04 guest. These base rows therefore move to `passing`, while all
service and option tuples stay unadvertised. No live Canonical subscription or
entitled native effect was exercised or inferred.
