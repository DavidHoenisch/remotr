# Task 2.3 Ubuntu Pro capability gate evidence

- Verification IDs: OS-UPM-006, OS-UPM-008, OS-UPM-009, OS-LPC-013, and OS-LPC-014.
- Public seam: generated endpoint capability document.
- Focused command: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/capabilitydoc -run '^TestGeneratorPublishesOnlyExactPassingUbuntuProRelease$'`
- Independently expected result: an isolated passing Ubuntu 26.04 LTS amd64 API-v32 base row advertises only `resource:ubuntu-pro`; an unrelated 24.04 row, untested service row, interim release, future LTS, and derivative identity remain absent.
- Intended red observed: build failed because `NewDefaultGeneratorWithUbuntuProQualification` did not exist.
- Green result: the focused test passes; only exact Ubuntu identity plus the exact passing release/architecture/API row advertises the resource, with no base-to-service or cross-resource uplift.
- Broader checks: `./internal/capabilitydoc`, `./internal/ubuntuproqualification`, `./internal/agent/facts`, and traceability lint pass.
