# Task 5.3 API envelope evidence

- Verification IDs: OS-UPM-039 and OS-UPM-040.
- Public seam: the bounded Ubuntu Pro Client common envelope and each typed endpoint adapter.
- Red sequence: the first envelope test failed to compile because a stable-code `APIError` did not exist. The endpoint-attribute table then failed because an attach response could report an unknown enabled service without rejection.
- Green behavior: the decoder requires the v1 common-envelope members, validates result and stable issue codes, rejects duplicate members at every depth, trailing JSON, excessive nesting, excessive collections, oversized output, missing members, and malformed endpoint attributes, while tolerating bounded unknown fields for forward compatibility.
- Localized error messages and dependency titles are not copied into errors or control flow; only validated stable codes survive.
- Focused checks: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -run 'TestDecodeEnvelopeBoundariesAndStableErrors|TestAPIClientRejectsInvalidEndpointAttributes' -count=1` passed.
- Bounded fuzz check: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -run '^$' -fuzz '^FuzzDecodeEnvelopeBounded$' -fuzztime=3s` passed after 936,178 executions with 209 interesting inputs.
- Broader adapter and provider checks: `env GOCACHE=/tmp/remotr-go-cache mise exec go@1.26.5 -- go test -mod=vendor ./internal/applicators/ubuntupro -count=1` and the focused Ubuntu Pro provider preflight test passed.
