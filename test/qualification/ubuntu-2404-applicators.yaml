version: 1
change: qualify-ubuntu-2404-applicators
platform:
  distribution: ubuntu
  release: "24.04"
  architecture: amd64
external_qualifications:
  - capability_ids: [package, aptSigningKey, aptRepository]
    change: complete-core-package-providers
    reason: "APT package, signing-key, and repository qualification is owned by the sibling package-provider change."
future_roadmap:
  - id: UHF-000
    title: Complete the high-risk execution foundation
    disposition: future-roadmap
    reason: "This is a shared safety dependency, not an applicator row; qualification cannot replace its durable rollback, redaction, and authorization gates."
  - id: UHF-001
    title: Add typed Hub import parameters and review
    disposition: future-roadmap
    reason: "No typed bounded Hub-parameter contract or reviewed substitution workflow exists in the applicator registry."
  - id: UHF-002
    title: Add conditional applicability for optional host facilities
    disposition: future-roadmap
    reason: "The registry has no bounded applicability contract for optional packages, services, schemas, hardware, or configuration owners."
  - id: UHF-100
    title: Local password-aging policy
    disposition: future-roadmap
    reason: "The user contract does not own shadow password-aging fields or distribution defaults."
  - id: UHF-101
    title: MFA and smart-card authentication providers
    disposition: future-roadmap
    reason: "No demand-backed MFA or smart-card enrollment, factor-health, and recovery provider is registered."
  - id: UHF-102
    title: Automatic logout and session termination policy
    disposition: future-roadmap
    reason: "sessionPolicy does not define portable graphical, shell, SSH, and logind termination semantics."
  - id: UHF-103
    title: First-class GDM policy
    disposition: future-roadmap
    reason: "Interactive-user dconf providers do not own the GDM system database, greeter lifecycle, or recovery boundary."
  - id: UHF-104
    title: Expand browser policy catalogs and providers
    disposition: future-roadmap
    reason: "Edge and other browsers, user scope, Firefox recommended policy, unknown policy names, types, and levels, and expanded versioned policy catalogs are not registered or qualified."
  - id: UHF-105
    title: Authoritative repository governance
    disposition: future-roadmap
    reason: "Named APT fragments do not provide a bounded authoritative repository-set ownership and cleanup contract."
  - id: UHF-106
    title: Structured remote log forwarding
    disposition: future-roadmap
    reason: "journald local forwarding does not define remote destination, trust, queue, retry, or delivery-health ownership."
  - id: UHF-107
    title: Chrony time-synchronization provider
    disposition: future-roadmap
    reason: "The only registered timeSync backend is systemd-timesyncd; Chrony has no exact provider row."
  - id: UHF-108
    title: Authoritative default-application cleanup
    disposition: future-roadmap
    reason: "Authoritative default application cleanup is not safely portable across desktop MIME databases, so sessionPolicy default applications remain merge-only."
  - id: UHF-200
    title: Dynamic filesystem posture and bounded remediation
    disposition: future-roadmap
    reason: "Explicit file and directory resources do not define bounded discovery or remediation of dynamically found objects."
  - id: UHF-201
    title: ACL, extended-attribute, and Linux capability resources
    disposition: future-roadmap
    reason: "The filesystem contracts do not accept or observe ACL, extended-attribute, or Linux capability state."
  - id: UHF-202
    title: System and interactive-user umask policy
    disposition: future-roadmap
    reason: "No typed system, shell, service, and desktop umask ownership contract is registered."
  - id: UHF-203
    title: USB device and HID policy
    disposition: future-roadmap
    reason: "kernelModule blacklisting is not a USB device inventory, authorization, or recovery contract."
  - id: UHF-204
    title: Destructive storage and LUKS lifecycle
    disposition: future-roadmap
    reason: "Mount and swap contracts do not own partitioning, LVM, RAID, LUKS formatting, enrollment, or crypttab."
  - id: UHF-205
    title: TPM enrollment and key-protection providers
    disposition: future-roadmap
    reason: "No TPM enrollment, PCR policy, key lifecycle, or hardware recovery provider is registered."
  - id: UHF-206
    title: Ubuntu FIPS lifecycle
    disposition: future-roadmap
    reason: "Package presence cannot prove Ubuntu entitlement, FIPS enablement, approved boot state, or effective crypto modules."
  - id: UHF-207
    title: Scoped TLS and cryptographic policy
    disposition: future-roadmap
    reason: "No provider-scoped TLS and cryptographic policy ownership contract exists across heterogeneous consumers."
  - id: UHF-208
    title: System-wide AppArmor posture
    disposition: future-roadmap
    reason: "A named appArmorProfile does not define system-wide inventory, attachment, unconfined-process, or package-profile posture."
  - id: UHF-300
    title: Security-tool health and finding telemetry
    disposition: future-roadmap
    reason: "The agent has no typed security-tool health, version, finding, and bounded telemetry contract."
  - id: UHF-301
    title: Falco runtime-monitoring integration
    disposition: future-roadmap
    reason: "No Falco installation, rule, kernel-driver, service-health, and finding provider is registered."
  - id: UHF-302
    title: Backup and restore provider contract
    disposition: future-roadmap
    reason: "Endpoint schedules do not define backup destination, encryption, retention, restore testing, or recovery evidence."
  - id: UHF-303
    title: Compliance scanner execution and result ingestion
    disposition: future-roadmap
    reason: "Generic commands cannot safely model scanner profiles, execution bounds, signed results, or ingestion semantics."
  - id: UHF-304
    title: Control mapping and evidence export
    disposition: future-roadmap
    reason: "Technical endpoint state does not provide scoped control mappings, evidence objects, authorization, or satisfied-control claims."
rows:
  - capability_id: file
    backend: posix
    contract_revision: file-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: container
    risk: ordinary
    accepted_fields: ["authorizationGroup","content","dependsOn[]","enforce","group","kind","lifecycle","lockDomains[]","mode[]","name","notifications[].target","notifications[].type","owner","ownership","path","policy","preApplyValidation[]","providerOptions.*.*","replaceRegx","risk","updateExisting","validation[].command[]","withRegx"]
    composed_address: m1-filesystem/managed-file
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-097","OS-AEC-102"]
    selectors: ["make:provider-matrix-containers"]
    disposition: qualified
    reason: "The exact file/posix contract passed its provider contract and pinned Ubuntu 24.04 amd64 container selector."
    tdd:
      governing_id: OS-AEC-097
      public_seam: provider-contract
      expected_result: "A drifted file/posix resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-container","capability-advertisement"]
      phase: verified
      red_failure: "TestApplicator_ConformsForMetadataOnlyLifecycle observed compliant for a missing metadata-only file; expected drifted before creation."
      green_result: "The file provider now treats a missing metadata-only target as drifted, creates an empty regular file with the declared mode and real UID/GID, and reports a compliant second Check."
      broader_checks: ["go-test:./internal/applicators/files", "make:provider-matrix-containers on pinned Ubuntu 24.04 amd64"]
      final_disposition: qualified
  - capability_id: download
    backend: https
    contract_revision: download-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: container
    risk: ordinary
    accepted_fields: ["authenticationRef","authorizationGroup","checksum","dependsOn[]","dest","enforce","group","kind","lifecycle","lockDomains[]","mode[]","name","notifications[].target","notifications[].type","notifySystemd","owner","ownership","policy","preApplyValidation[]","providerOptions.*.*","redirectPolicy","reloadExec[]","risk","signature","timeout","trustedSigner","url","validation[].command[]"]
    composed_address: m1-filesystem/verified-download
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-097","OS-AEC-102"]
    selectors: ["make:provider-matrix-containers"]
    disposition: qualified
    reason: "The exact download/https contract passed its provider, composed authentication, authenticated authorization, and pinned Ubuntu 24.04 amd64 container selector."
    tdd:
      governing_id: OS-AEC-097
      public_seam: provider-contract
      expected_result: "A drifted download/https resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-container","capability-advertisement"]
      phase: verified
      red_failure: "TestApplicator_authenticationFailureIsRedactedAndClean first exposed the synthetic authentication canary; the accepted-field audit then found both that TestRegistryDownloadProviderResolvesScopedAuthentication had no composed resolver and that TestResolveSecretAuthorizesEndpointArtifactResourceAndPurpose denied the active artifact's download authentication reference."
      green_result: "Authentication failures are typed and redacted; composed execution sends the exact scoped request to the endpoint resolver; and authenticated resolution now authorizes only the active artifact's matching download reference, address, and download-authentication purpose before a compliant second Check."
      broader_checks: ["go-test:./internal/applicators/downloads", "go-test:./internal/resourceregistry", "go-test:./internal/secrets and ./internal/server", "make:provider-matrix-containers on pinned Ubuntu 24.04 amd64"]
      final_disposition: qualified
  - capability_id: directory
    backend: posix
    contract_revision: directory-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: container
    risk: ordinary
    accepted_fields: ["allowTypeReplacement","authorizationGroup","crossFilesystem","dependsOn[]","enforce","exclusions[]","group","kind","lifecycle","lockDomains[]","maxDepth","maxEntries","mode[]","name","notifications[].target","notifications[].type","owner","ownership","path","policy","preApplyValidation[]","providerOptions.*.*","purge","recursive","risk","validation[].command[]"]
    composed_address: m2-filesystem/managed-directory
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-097","OS-AEC-102"]
    selectors: ["make:provider-matrix-containers"]
    disposition: qualified
    reason: "The exact directory/posix contract passed its provider contract and pinned Ubuntu 24.04 amd64 container selector, including a real tmpfs mount boundary."
    tdd:
      governing_id: OS-AEC-097
      public_seam: provider-contract
      expected_result: "A drifted directory/posix resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-container","capability-advertisement"]
      phase: verified
      red_failure: "Qualification exposed three focused gaps in sequence: rejected purge mutated metadata, bounded recursive absence could not remove a non-empty tree, and TestDirectoryProviderContract reported changed for every already-compliant Apply."
      green_result: "Directory Apply now preserves state on rejected cleanup, supports bounded recursive absence without crossing mounts, and returns no-change whenever its public Check is already compliant."
      broader_checks: ["go-test:./internal/applicators/directories", "make:provider-matrix-containers on pinned Ubuntu 24.04 amd64 with tmpfs mount boundary"]
      final_disposition: qualified
  - capability_id: link
    backend: posix
    contract_revision: link-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: container
    risk: ordinary
    accepted_fields: ["allowTypeReplacement","authorizationGroup","dependsOn[]","enforce","group","kind","lifecycle","linkType","lockDomains[]","name","notifications[].target","notifications[].type","owner","ownership","path","policy","preApplyValidation[]","providerOptions.*.*","risk","target","validation[].command[]"]
    composed_address: m2-filesystem/managed-link
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-097","OS-AEC-102"]
    selectors: ["make:provider-matrix-containers"]
    disposition: qualified
    reason: "The exact link/posix contract passed symbolic and hard-link provider contracts plus the pinned Ubuntu 24.04 amd64 container selector."
    tdd:
      governing_id: OS-AEC-097
      public_seam: provider-contract
      expected_result: "A drifted link/posix resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-container","capability-advertisement"]
      phase: verified
      red_failure: "Qualification exposed replacement-before-validation for symbolic ownership and missing hard-link sources; after those fixes, TestHardLinkCrossFilesystemFailurePreservesActiveDestination showed linkat still removed the active destination before returning EXDEV."
      green_result: "Symbolic and hard links are now fully prepared under randomized same-directory staging names and atomically renamed, so validation, ownership, source, and cross-filesystem failures preserve the active destination and clean staging artifacts."
      broader_checks: ["go-test:./internal/applicators/links", "make:provider-matrix-containers on pinned Ubuntu 24.04 amd64"]
      final_disposition: qualified
  - capability_id: group
    backend: shadow
    contract_revision: group-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-access
    risk: access
    accepted_fields: ["allowGIDReassignment","authorizationGroup","dependsOn[]","enforce","gid","group","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","system","validation[].command[]"]
    composed_address: m2-access/managed-group
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-user-safety"]
    disposition: qualified
    reason: "The exact group/shadow contract passed provider and pinned Ubuntu 24.04 amd64 VM account-database and recovery evidence."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted group/shadow resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","access-recovery","capability-advertisement"]
      phase: verified
      red_failure: "Qualification exposed malformed group lookup as false absence, a Debian-default VM selector, and a missing exact group advertisement after the pinned VM contract passed."
      green_result: "Malformed native group records fail closed; the pinned Ubuntu VM proves fixed ordinary class, GID reassignment, real native account-database mutation, removal, recovery-group preservation, idempotent Apply, and compliant second Checks."
      broader_checks: ["go-test:./internal/applicators/groups", "make:provider-matrix-vm-user-safety on pinned Ubuntu 24.04 amd64"]
      final_disposition: qualified
  - capability_id: user
    backend: shadow
    contract_revision: user-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-access
    risk: access
    accepted_fields: ["allowUIDReassignment","authorizationGroup","comment","createHome","dependsOn[]","enforce","expiry","forceRemoval","home","kind","lifecycle","lockDomains[]","locked","name","notifications[].target","notifications[].type","ownership","passwordHashRef","policy","preApplyValidation[]","present","primaryGroup","providerOptions.*.*","removeHome","risk","shell","supplementaryGroupsMode","supplementaryGroups[]","system","uid","username","validation[].command[]"]
    composed_address: m2-access/managed-user
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-user-safety"]
    disposition: qualified
    reason: "The exact user/shadow contract passed provider and pinned Ubuntu 24.04 amd64 VM account-database and access-recovery evidence."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted user/shadow resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","access-recovery","capability-advertisement"]
      phase: verified
      red_failure: "Qualification exposed creation before passwordHashRef resolution, a Debian-default VM selector, a shadow lock-marker second-Check drift, and a missing exact user advertisement after the pinned VM contract passed."
      green_result: "Secret prerequisites now resolve before account creation and shadow lock state is independent of password material; the pinned Ubuntu VM proves UID/class, groups, home, shell, password, lock, expiry, protected recovery identity, removal, idempotent Apply, and compliant second Checks."
      broader_checks: ["go-test:./internal/applicators/users", "make:provider-matrix-vm-user-safety on pinned Ubuntu 24.04 amd64 with recovery principal"]
      final_disposition: qualified
  - capability_id: authorizedKey
    backend: openssh
    contract_revision: authorizedKey-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-access
    risk: access
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","entries[].comment","entries[].expiresAt","entries[].fingerprint","entries[].key","entries[].principals[]","entries[].restrictions[]","entries[].type","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","recoveryPrincipals[]","risk","user","validation[].command[]"]
    composed_address: m2-access/managed-authorized-key
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-user-safety"]
    disposition: qualified
    reason: "The exact authorizedKey/openssh contract passed provider and pinned Ubuntu 24.04 amd64 VM home/access-recovery evidence."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted authorizedKey/openssh resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","access-recovery","capability-advertisement"]
      phase: verified
      red_failure: "Qualification exposed silent omission of entries[].expiresAt and, after the pinned access VM passed, a missing exact authorizedKey advertisement."
      green_result: "RFC3339 expiry values render as canonical OpenSSH restrictions; the pinned Ubuntu VM proves fingerprint validation, restrictions/principals, merge and authoritative ownership, revocation, unmanaged-key preservation, malicious-home symlink rejection, recovery-principal use, idempotent Apply, and second Checks."
      broader_checks: ["go-test:./internal/models and ./internal/applicators/authorizedkeys", "make:provider-matrix-vm-user-safety on pinned Ubuntu 24.04 amd64 with recovery principal"]
      final_disposition: qualified
  - capability_id: knownHost
    backend: openssh
    contract_revision: knownHost-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: container
    risk: ordinary
    accepted_fields: ["authorizationGroup","comment","dependsOn[]","enforce","fingerprint","hashing","hosts[]","key","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","replaceExisting","risk","scope","type","user","validation[].command[]"]
    composed_address: m2-access/managed-known-host
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-097","OS-AEC-102"]
    selectors: ["make:provider-matrix-containers"]
    disposition: qualified
    reason: "The exact knownHost/openssh contract passed provider and pinned Ubuntu 24.04 amd64 container evidence."
    tdd:
      governing_id: OS-AEC-097
      public_seam: provider-contract
      expected_result: "A drifted knownHost/openssh resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-container","capability-advertisement"]
      phase: verified
      red_failure: "Qualification exposed repeated salt-driven rewrites, a hashed conflict bypassing replacement policy, and a missing exact knownHost advertisement after the pinned container passed."
      green_result: "Apply is idempotent before new salt generation and conflict detection verifies hashed tokens; fingerprint, plain/hashed policy, merge-safe editing, explicit replacement, absence, preservation, rollback, and second Checks pass in the pinned Ubuntu container."
      broader_checks: ["go-test:./internal/models and ./internal/applicators/knownhosts", "make:provider-matrix-containers on pinned Ubuntu 24.04 amd64"]
      final_disposition: qualified
  - capability_id: sudo
    backend: sudoers
    contract_revision: sudo-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-access
    risk: access
    accepted_fields: ["authorizationGroup","commands[]","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","recoveryPrincipals[]","risk","runAs[]","subjects[]","tags[]","validation[].command[]"]
    composed_address: m2-access/managed-sudo
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-user-safety"]
    disposition: qualified
    reason: "The exact sudo/sudoers contract passed provider and pinned Ubuntu 24.04 amd64 native visudo/access-recovery evidence."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted sudo/sudoers resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","access-recovery","capability-advertisement"]
      phase: verified
      red_failure: "Qualification exposed unsafe ownership compliance, leaked validation canaries, missing Ubuntu @includedir support, and a missing exact sudo advertisement after the corrected VM passed."
      green_result: "Sudo enforces root:root 0440 metadata, stages both include syntaxes, and redacts validation failures; native Ubuntu visudo, managed/recovery grants, invalid-candidate preservation, transactional rollback, idempotent Apply, and second Checks pass."
      broader_checks: ["go-test:./internal/applicators/sudo", "make:provider-matrix-vm-user-safety on pinned Ubuntu 24.04 amd64 with native visudo and recovery grant"]
      final_disposition: qualified
  - capability_id: userFile
    backend: posix
    contract_revision: userFile-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-access
    risk: access
    accepted_fields: ["authorizationGroup","content","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","mode[]","name","notifications[].target","notifications[].type","ownership","path","policy","preApplyValidation[]","providerOptions.*.*","replaceRegx","risk","selector.mode","selector.usernames[]","updateExisting","users","validation[].command[]","withRegx"]
    composed_address: m2-access/managed-user-file
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-user-safety"]
    disposition: qualified
    reason: "Pinned Ubuntu 24.04 VM evidence passed real interactive-user resolution, per-user observations, lifecycle and metadata convergence, target-user access, no-follow isolation, preservation, and second Checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted userFile/posix resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","access-recovery","capability-advertisement"]
      phase: verified
      red_failure: "Qualification exposed first-failure fan-out abort, configuration validation that rejected lifecycle: absent unless meaningless content was supplied, root-owned 0750 parent directories that made a managed file unreadable by its target user on Ubuntu, and a missing exact user-file advertisement after the corrected VM passed."
      green_result: "Apply isolates and aggregates per-user failures while persisting successes; absent lifecycle validates without content and rejects contradictory metadata; safe-path creation owns new parent directories for target-user access while preserving existing directories; the exact qualified row now advertises userFile-v1."
      broader_checks: ["go-test:./internal/applicators/files ./internal/applicators/userfiles ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-user-safety on pinned Ubuntu 24.04 amd64 with real passwd resolution, target-user reads, metadata repair, absence, no-follow traversal, failure isolation, preservation, and second Checks"]
      final_disposition: qualified
  - capability_id: sysctl
    backend: procfs
    contract_revision: sysctl-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: ordinary
    accepted_fields: ["activation","authorizationGroup","dependsOn[]","enforce","key","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","persistent","policy","preApplyValidation[]","providerOptions.*.*","risk","runtime","validation[].command[]","value"]
    composed_address: m3-host/kernel-sysctl
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-system-safety"]
    disposition: qualified
    reason: "Pinned Ubuntu 24.04 system-safety evidence passed independent runtime/persistent state, owned metadata, unsupported keys, native reload, next-boot signaling, preservation, and second Checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted sysctl/procfs resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The focused provider-contract check accepted a mode-0600 persistent sysctl drop-in as compliant because it compared content without owned-file metadata, and the corrected passing VM contract still lacked its exact resource and kernel-provider advertisements."
      green_result: "Persistent checks distinguish read failures from drift and require a regular mode-0644 owned fragment; pinned Ubuntu VM evidence passes independent runtime/persistent scopes, root ownership, unsupported keys, native reload, next-boot signaling, preservation, idempotence, second Checks, and exact resource/provider advertisement."
      broader_checks: ["go-test:./internal/applicators/sysctl ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-system-safety on pinned Ubuntu 24.04 amd64 with native sysctl, real procfs, controlled reboot, and reconstructed recovery checks"]
      final_disposition: qualified
  - capability_id: kernelModule
    backend: kmod
    contract_revision: kernelModule-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: kernel-security
    accepted_fields: ["authorizationGroup","blacklisted","dependsOn[]","enforce","kind","lifecycle","loaded","lockDomains[]","module","name","notifications[].target","notifications[].type","ownership","parameters.*","persistent","policy","preApplyValidation[]","protectedModules[]","providerOptions.*.*","risk","validation[].command[]"]
    composed_address: m3-host/kernel-module
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-kernel-module-safety"]
    disposition: qualified
    reason: "Pinned Ubuntu 24.04 kernel-safety evidence passed built-in and dynamic loaded state, real parameters, boot persistence, blacklist lifecycle, protected unloads, native failure recovery, next-boot signaling, preservation, and second Checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted kernelModule/kmod resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "A synthetic modprobe activation failure left newly staged fragments in place, a persistence-only change omitted next-boot, pinned Ubuntu exposed built-in loop support that /proc/modules-only checks missed, its dummy module lacked the initial parameter fixture, and the corrected brd VM contract still lacked exact resource and kernel-provider advertisements."
      green_result: "Activation failure restores prior fragments, persistence-only changes report next-boot, loaded-state observation combines /proc/modules with /sys/module, and pinned Ubuntu passes real brd/rd_nr load, persistence, blacklist, declared/root/network protection, native failure recovery, preservation, idempotence, second Checks, and exact resource/provider advertisement."
      broader_checks: ["go-test:./internal/applicators/kernelmodules ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-kernel-module-safety on pinned Ubuntu 24.04 amd64 with built-in loop observation and real brd lifecycle"]
      final_disposition: qualified
  - capability_id: hostname
    backend: systemd-hostnamed
    contract_revision: hostname-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: ordinary
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","static","transient","validation[].command[]"]
    composed_address: m3-host/hostname
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-system-safety"]
    disposition: qualified
    reason: "The exact Ubuntu 24.04 amd64 systemd-hostnamed row passes the public provider seam, pinned system-safety VM, recovery gate, and capability-advertisement checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted hostname/systemd-hostnamed resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "A missing hostnamectl/systemd-hostnamed boundary was reported as a generic failed probe instead of an explicit unsupported provider mismatch; after that correction, pinned Ubuntu proved that a configured static hostname takes precedence over a distinct transient request, leaving the resource drifted instead of converged; after the corrected VM contract passed, the exact resource:hostname/hostname-v1 and provider:host/hostnamectl/1 advertisements were absent while the matrix row remained untested."
      green_result: "Hostname Check reports an absent hostnamectl boundary as unsupported, distinct transient state shadowed by a static hostname is rejected before mutation, pinned Ubuntu passes equal static/active convergence, supported transient-only state, immediate static activation, /etc/hosts preservation, idempotence, cleanup, and compliant second Checks, and the exact resource and hostnamectl-provider capabilities are advertised."
      broader_checks: ["go-test:./internal/applicators/hostname ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-system-safety on pinned Ubuntu 24.04 amd64 with controlled reboot and recovery verification"]
      final_disposition: qualified
  - capability_id: hostLocale
    backend: systemd-localed
    contract_revision: hostLocale-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: ordinary
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","keymap","kind","lifecycle","locale.*","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","timezone","validation[].command[]"]
    composed_address: m3-host/locale
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-host-locale"]
    disposition: qualified
    reason: "The exact Ubuntu 24.04 amd64 host-locale row passes the public provider seam, stock-image VM safety fixture, and capability-advertisement checks through systemd and Ubuntu console-setup boundaries."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted hostLocale/systemd-localed resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The host-locale VM selector inherited the default Debian guest and lacked the exact pinned Ubuntu 24.04 amd64 box, hostname, and guest OS assertion required for qualification; after pinning passed, a later native locale failure left an earlier timezone mutation active instead of restoring the observed timezone; after reverse-order rollback was corrected, the expanded pinned VM contract found that the Ubuntu cloud image's systemd-localed boundary had no native keymap catalog (`localectl list-keymaps` failed with no such file or directory), while Check still treated a keymap request as ordinary drift instead of unsupported; after unsupported handling passed on the stock image, `kbd` and `console-setup-linux` provided no catalog, and `console-data` installed `.kmap.gz` files under `/usr/share/keymaps` while Ubuntu systemd scanned `/usr/share/kbd/keymaps` for `.map.gz` files; after the fixture exposed exact `.map.gz` aliases, `localectl set-keymap` still failed natively with `Setting X11 and console keymaps is not supported in Debian`, proving that the accepted keymap field cannot converge through the current command boundary; the focused OS-KHB-008/OS-AEC-100 provider correction then failed to compile because Applicator had no injectable Ubuntu keyboard configuration boundary; after the corrected stock-image VM selector passed, exact resource:host-locale/hostLocale-v1 and provider:host/localectl/1 advertisements were still absent while the matrix row remained untested."
      green_result: "Host-locale Check and Apply independently manage timezone, declared locale variables, and Ubuntu's XKB console layout while preserving omitted state; ckbcomp rejects invalid layouts before mutation; atomic /etc/default/keyboard replacement preserves unrelated settings, mode, and owner; later native failures restore earlier timezone/locale mutations; the stock pinned Ubuntu VM passes immediate/logout/reboot activation, idempotence, cleanup, and compliant second Checks; and the exact resource and localectl-provider capabilities are advertised."
      broader_checks: ["go-test:./internal/applicators/hostlocale ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-host-locale on stock pinned Ubuntu 24.04 amd64 with native ckbcomp validation and host-state preservation"]
      final_disposition: qualified
  - capability_id: timeSync
    backend: systemd-timesyncd
    contract_revision: timeSync-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: system-service
    accepted_fields: ["authorizationGroup","dependsOn[]","enabled","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","pools[]","preApplyValidation[]","provider","providerOptions.*.*","risk","servers[]","validation[].command[]"]
    composed_address: m3-host/time-sync
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-time-sync"]
    disposition: qualified
    reason: "The exact Ubuntu 24.04 amd64 systemd-timesyncd row passes the public provider seam, pinned VM safety fixture, transactional failure checks, and capability-advertisement gate."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted timeSync/systemd-timesyncd resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The time-sync VM selector inherited the default guest and lacked the exact pinned Ubuntu 24.04 amd64 box, hostname, and guest OS assertion required for qualification; after pinning, Check treated an absent systemd-timesyncd unit as a generic failed timedatectl probe instead of an explicit unsupported backend mismatch; after backend selection was corrected, Check reported compliant when generic NTP state was yes but the selected systemd-timesyncd unit was inactive, conflating configured and effective state; after configured/effective probes were corrected, a server-fragment persistence failure occurred only after timedatectl had enabled NTP, leaving a partial host mutation; after persistence was ordered first, a later native enablement failure left the newly written server fragment in place instead of restoring its prior content and mode; after the expanded pinned VM selector passed, exact resource:time-sync/timeSync-v1 and provider:time-sync/systemd-timesyncd/1 advertisements were still absent while the matrix row remained untested."
      green_result: "Time-sync Check rejects an absent or masked systemd-timesyncd backend as unsupported, distinguishes configured unit state from active generic NTP state, independently manages enablement and exact NTP/FallbackNTP drop-in fields, reports restart activation for fragment changes, orders persistence before enablement, restores prior fragment content and mode after native enablement failure, preserves unrelated configuration, and passes compliant/no-change second passes in the pinned Ubuntu VM with exact resource/provider advertisement."
      broader_checks: ["go-test:./internal/applicators/timesync ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-time-sync on pinned Ubuntu 24.04 amd64 with real systemd-timesyncd enablement, configuration preservation, activation, and cleanup"]
      final_disposition: qualified
  - capability_id: mount
    backend: util-linux
    contract_revision: mount-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: storage
    accepted_fields: ["authorizationGroup","dependsOn[]","dump","enforce","filesystemType","kind","lifecycle","lockDomains[]","mounted","name","notifications[].target","notifications[].type","options[]","ownership","pass","persistent","policy","preApplyValidation[]","providerOptions.*.*","risk","source","target","unmountMode","validation[].command[]"]
    composed_address: m3-storage/managed-mount
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-mount"]
    disposition: qualified
    reason: "The exact Ubuntu 24.04 amd64 util-linux mount row passes the public provider seam, pinned VM boot/storage fixture, transactional failure checks, and capability-advertisement gate."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted mount/util-linux resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The mount VM selector inherited the default guest and lacked the exact pinned Ubuntu 24.04 amd64 box, hostname, and guest OS assertion required for boot/storage qualification; after pinning, a persistent-only resource wrote an unsupported local filesystem type into fstab because source/filesystem preflight ran only for immediate runtime mounts; after persistent boot preflight was corrected, a bind-style mount sourced from the live Remotr state tree reached the native mount boundary instead of being rejected as a protected source; after source protection was added, combined runtime/persistent Apply mounted the live target before discovering that fstab persistence had failed, leaving a partial runtime mutation; after persistence was ordered first, a native mount failure left the newly added fstab declaration and changed mode in place instead of restoring the prior file exactly; after the expanded pinned VM selector passed, exact resource:mount/mount-v1 and provider:storage/mount/1 advertisements were still absent while the matrix row remained untested."
      green_result: "Mount Apply independently manages real runtime tmpfs state and one precisely owned fstab entry, preflights persistent as well as runtime sources and filesystems, rejects protected target/source overlaps and unauthorized forced unmounts before mutation, orders persistence before activation, restores exact prior fstab content and mode after native activation failure, preserves unrelated declarations, passes native findmnt boot verification, and reports compliant/no-change second passes in the pinned Ubuntu VM with exact resource/provider advertisement."
      broader_checks: ["go-test:./internal/applicators/mounts ./internal/models ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-mount on pinned Ubuntu 24.04 amd64 with real tmpfs activation, independent scope removal, native fstab verification, preservation, recovery, cleanup, and second Apply"]
      final_disposition: qualified
  - capability_id: swap
    backend: util-linux
    contract_revision: swap-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: storage
    accepted_fields: ["active","allowRemove","authorizationGroup","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","path","persistent","policy","preApplyValidation[]","priority","providerOptions.*.*","risk","sizeBytes","type","validation[].command[]"]
    composed_address: m3-storage/managed-swap
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-swap"]
    disposition: qualified
    reason: "The exact Ubuntu 24.04 amd64 util-linux swap row passes the public provider seam, pinned VM boot/storage fixture, transactional failure checks, and capability-advertisement gate."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted swap/util-linux resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The swap row depended on the broad system-safety selector, which compiled and ran no public swap provider contract; the required bounded pinned Ubuntu 24.04 amd64 swap VM function, exact hostname, guest assertion, and TestSwapProviderVM execution were absent; after the selector was isolated, a regular file declared as a device reached the native swapon boundary because the provider did not validate block-device identity; after device identity was enforced, a declared swap-file symlink was followed to its target and reached swapon instead of failing closed before mutation; after file identity was bound, an impossibly large zero-filled swap-file request reached dd instead of being rejected against available filesystem capacity; after capacity preflight was added, mkswap failure left Remotr's newly created file behind as partial storage state; after creation cleanup was added, combined active/persistent Apply completed dd, mkswap, and swapon before discovering that fstab persistence had failed; after persistence was ordered first, swapon failure removed the new file but left the newly added fstab declaration and changed mode in place; after transactional recovery was corrected, Check reported compliant for an active swap entry whose effective priority differed from the declared priority; after priority observation was corrected, Apply returned no-change for the same drift and could not converge it to a compliant second Check; after priority convergence was implemented, failure to activate the new priority left the previously active swap disabled instead of recovering its prior priority; after priority recovery was added, existing regular files with the wrong declared size or unsafe mode still reached swapon instead of failing identity preflight; after the expanded pinned VM selector passed, exact resource:swap/swap-v1 and provider:storage/swap/1 advertisements were still absent while the matrix row remained untested."
      green_result: "Swap Apply validates no-follow file identity, exact size/mode and block-device identity, preflights filesystem capacity before zero-fill, independently manages active/persistent scopes and exact priority, persists before activation, cleans newly created files on failure, restores exact prior fstab state after activation failure, recovers the prior active priority after failed priority change, preserves unrelated declarations, and passes compliant/no-change second passes for real swap-file and loop-device state in the pinned Ubuntu VM with exact resource/provider advertisement."
      broader_checks: ["go-test:./internal/applicators/swaps ./internal/models ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-swap on pinned Ubuntu 24.04 amd64 with real zero-filled swap-file and loop-device activation, exact priorities, independent scope removal, native fstab verification, recovery, cleanup, and second Apply"]
      final_disposition: qualified
  - capability_id: endpointSchedule
    backend: cron
    contract_revision: endpointSchedule-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: container
    risk: ordinary
    accepted_fields: ["argv[]","authorizationGroup","backend","dependsOn[]","enforce","environment[].name","environment[].secretRef","environment[].value","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","overlap","ownership","persistent","policy","preApplyValidation[]","providerOptions.*.*","risk","schedule","shell","timeout","user","validation[].command[]","workingDirectory"]
    composed_address: m4-services/cron-schedule
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-097","OS-AEC-102"]
    selectors: ["make:provider-matrix-containers"]
    disposition: qualified
    reason: "The exact Ubuntu 24.04 amd64 cron row passes the public provider seam, native pinned-container execution, lifecycle/ownership checks, and capability-advertisement gate."
    tdd:
      governing_id: OS-AEC-097
      public_seam: provider-contract
      expected_result: "A drifted endpointSchedule/cron resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-container","capability-advertisement"]
      phase: verified
      red_failure: "The cron provider created its shared launcher state directory as root-only 0750, so a launcher and environment file correctly owned by the declared non-root schedule user were still unreachable at execution time; after traversal was corrected, overlap-forbid launchers still depended on out-of-band creation of their run directory and lock file; after local state was self-contained, the pinned Ubuntu container installed no cron backend and compiled, mounted, and executed no cron provider contract; once the real contract ran, Ubuntu GNU timeout treated the generated post-duration -- token as the command name and every timed launcher failed with exit 127; after the corrected pinned container selector passed, exact resource:endpoint-schedule/endpointSchedule-v1 and provider:schedule/cron/1 advertisements were still absent while the cron row remained untested."
      green_result: "Cron schedules now create a protected but user-traversable state directory and an owned overlap lock before activation, preserve exact argv or shell boundaries, working directory, public/secret environment separation, timeout and non-overlap behavior, independently support present/disabled/absent lifecycle, preserve unrelated fragments, execute offline as the declared non-root user on Ubuntu GNU cron/coreutils, and pass compliant/no-change second passes with exact resource/provider advertisement."
      broader_checks: ["go-test:./internal/applicators/endpointschedules/cron ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-containers on pinned Ubuntu 24.04 amd64 with native cron backend, non-root offline launcher execution, protected environment/lock state, timeout, lifecycle, preservation, and second Check"]
      final_disposition: qualified
  - capability_id: endpointSchedule
    backend: systemd-timer
    contract_revision: endpointSchedule-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: system-service
    accepted_fields: ["argv[]","authorizationGroup","backend","dependsOn[]","enforce","environment[].name","environment[].secretRef","environment[].value","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","overlap","ownership","persistent","policy","preApplyValidation[]","providerOptions.*.*","risk","schedule","shell","timeout","user","validation[].command[]","workingDirectory"]
    composed_address: m4-services/systemd-timer
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-systemd-timer"]
    disposition: qualified
    reason: "The exact Ubuntu 24.04 amd64 systemd-timer row passes paired-unit transaction recovery, native systemd syntax and manager lifecycle, missed-run telemetry, second checks, and capability-advertisement gates."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted endpointSchedule/systemd-timer resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The systemd-timer row depended on the broad system-safety selector, which compiled and ran no public paired-unit provider contract; the required bounded pinned Ubuntu 24.04 amd64 function, exact hostname, guest assertion, and TestSystemdTimerProviderVM execution were absent. After the selector was isolated, a failed daemon reload left both newly installed units active instead of restoring the prior pair; after reload recovery was added, a failed timer start left the new definitions and successful enablement behind; after transactional activation recovery was added, the VM target could still pass with no matching Go test because its selector did not verify the vmsafety source; the first real VM run then exposed an incorrect fixture expectation for native quoted EnvironmentFile values; after the corrected real lifecycle passed, exact provider:schedule/systemd-timer/1 and provider:init/systemd/1 advertisements were still absent while the evidence row remained untested."
      green_result: "The dedicated pinned Ubuntu VM compiles and selects a non-empty root-only provider test, verifies paired service/timer syntax through native systemd-analyze, transactionally restores files and prior state after reload or activation failure, converges enablement and active state through the real manager, reports persistent catch-up and non-persistent skip policy after actual oneshot execution, retains disabled units, removes absent units and protected environment state, passes compliant/no-change second passes, and advertises only the exact qualified schedule and observed init capabilities."
      broader_checks: ["go-test:./internal/applicators/endpointschedules/systemdtimer ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-systemd-timer on pinned Ubuntu 24.04 amd64 with real systemd-analyze, manager lifecycle, missed-run telemetry, removal, cleanup, and second Check"]
      final_disposition: qualified
  - capability_id: service
    backend: systemd
    contract_revision: service-state-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: system-service
    accepted_fields: ["active","authorizationGroup","dependsOn[]","enabled","enforce","kind","lifecycle","linger","lockDomains[]","masked","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","provider","providerOptions.*.*","risk","scope","service","users","validation[].command[]"]
    composed_address: m4-services/managed-service
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-service"]
    disposition: qualified
    reason: "The exact provider-neutral systemd service row passes structured probes, transactional state recovery, real Ubuntu manager lifecycle and failure semantics, preservation, second checks, and capability-advertisement gates."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted service/systemd resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The provider-neutral service Check discarded a failed native systemctl state probe and reported a declared inactive service as compliant even though its state was unobservable; after probe failures were preserved, a failed start following successful enablement left the previously disabled service enabled instead of restoring its prior state; after transactional state recovery was added, the exact service row still depended on the broad system-safety selector and had no bounded pinned Ubuntu provider-neutral VM fixture; the first real VM fixture declared enablement for units with no [Install] target, so native systemd correctly kept them static; after real enablement was exercised, failed-start rollback disabled and stopped the unit but left systemd's native failed-state latch set instead of restoring inactive state; after rollback recovery passed in the real VM, Check still treated a latched failed unit as equivalent to a healthy declared inactive state; after failure-state convergence was corrected, the exact resource:service/service-state-v1 advertisement was still absent while the evidence row remained untested; the final VM rerun then showed unconditional reset-failed on a healthy inactive unit aborting otherwise valid mask convergence."
      green_result: "Provider-neutral service construction flows through ResourceKindService and the registry's explicit systemd selection; Check preserves native probe failures and distinguishes failed from healthy inactive state; Apply snapshots declared state, recovers enablement/activity/masking and clears only failure latches created or observed at the appropriate boundary; the dedicated pinned Ubuntu VM proves enable/start, independent active-only preservation, mask/unmask, real failing-start recovery to disabled/inactive, unmanaged unit preservation, compliant/no-change second passes, and cleanup; only resource:service/service-state-v1 is newly advertised, while OpenRC, SysV, and legacy systemd forms remain unadvertised."
      broader_checks: ["go-test:./internal/applicators/systemd ./internal/applicators/services ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification ./internal/resourceregistry", "make:provider-matrix-vm-service on pinned Ubuntu 24.04 amd64 with provider-neutral registry selection, real systemd enablement/activity/masking, failing-start recovery, preservation, and second Check"]
      final_disposition: qualified
  - capability_id: service
    backend: openrc
    contract_revision: service-state-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: system-service
    accepted_fields: ["active","authorizationGroup","dependsOn[]","enabled","enforce","kind","lifecycle","linger","lockDomains[]","masked","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","provider","providerOptions.*.*","risk","scope","service","users","validation[].command[]"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "OpenRC is a known service contract but is not supported on the Ubuntu 24.04 qualification target."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for service/openrc and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: service
    backend: sysv
    contract_revision: service-state-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: system-service
    accepted_fields: ["active","authorizationGroup","dependsOn[]","enabled","enforce","kind","lifecycle","linger","lockDomains[]","masked","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","provider","providerOptions.*.*","risk","scope","service","users","validation[].command[]"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "SysV is a known service contract but is not advertised for Ubuntu 24.04."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for service/sysv and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: systemd
    backend: systemd-legacy
    contract_revision: systemd-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: system-service
    accepted_fields: ["active","authorizationGroup","dependsOn[]","enabled","enforce","kind","lifecycle","lockDomains[]","masked","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","unit","validation[].command[]"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "The legacy systemd compatibility kind cannot broaden the provider-neutral service capability claim."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for systemd/systemd-legacy and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: systemdUser
    backend: systemd-user-legacy
    contract_revision: systemdUser-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-desktop
    risk: desktop-session
    accepted_fields: ["active","authorizationGroup","dependsOn[]","enabled","enforce","kind","lifecycle","linger","lockDomains[]","masked","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","unit","unitPath","users","validation[].command[]"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "The legacy systemdUser compatibility kind requires separate evidence and cannot imply desktop/session qualification."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for systemdUser/systemd-user-legacy and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: systemdUnit
    backend: systemd
    contract_revision: systemdUnit-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: system-service
    accepted_fields: ["authorizationGroup","content","dependsOn[]","dropIn","enforce","group","kind","lifecycle","lockDomains[]","mode[]","name","notifications[].target","notifications[].type","owner","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","unit","validation[].command[]"]
    composed_address: m4-services/managed-unit
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-systemd-unit"]
    disposition: qualified
    reason: "The dedicated pinned Ubuntu 24.04 amd64 VM passes the complete-unit and named drop-in systemd contract through the resource registry with native staged verification, atomic replacement, coalesced daemon reload/restart activation, failure and unrelated-state preservation, removal, and second Checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted systemdUnit/systemd resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The systemdUnit row depended on the broad system-safety selector, which compiled and ran no public systemd-unit provider contract; the required bounded pinned Ubuntu 24.04 amd64 function, exact hostname, guest assertion, and TestSystemdUnitProviderVM execution were absent. The first real VM run also showed that the fixture's malformed-section drop-in was only a non-fatal systemd warning, so it could not prove failure preservation; a native relative ExecStart regression was added and observed to fail before activation."
      green_result: "The dedicated registry-backed VM contract passes complete-unit and named drop-in creation, atomic update, exact native systemd-analyze rejection before activation, prior-byte and unrelated-file preservation, deduplicated daemon-reload/restart execution, named drop-in and unit removal, compliant second Checks, no-change replay, guest cleanup, and exact capability advertisement."
      broader_checks: ["go-test:./internal/applicators/systemdunits ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "go-test:./internal/applicators/systemdunits with providerintegration outside the sandbox syscall restriction", "make:provider-matrix-vm-systemd-unit on pinned Ubuntu 24.04 amd64 with the real systemd manager, native verifier, activation, removal, preservation, and second Check"]
      final_disposition: qualified
  - capability_id: reboot
    backend: systemd
    contract_revision: reboot-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: boot
    accepted_fields: ["authorizationGroup","deadline","delay","dependsOn[]","enforce","generation","kind","lifecycle","lockDomains[]","maintenanceWindow.duration","maintenanceWindow.start","maintenanceWindow.weekdays[]","name","notifications[].target","notifications[].type","onlyIfRequired","ownership","policy","preApplyValidation[]","providerOptions.*.*","requireACPower","risk","timeout","userInhibition","validation[].command[]","workloadInhibition"]
    composed_address: m4-services/coordinated-reboot
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-system-safety"]
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 system-safety VM passes maintenance and real shutdown-inhibitor preflight, durable intent and acknowledgement state, attempt generation, same-boot timeout and terminal no-loop behavior, a controlled reboot with changed boot-ID completion, observable outcomes, second Check, and teardown."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted reboot/systemd resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The focused system probe treated every block inhibitor as a reboot blocker, so a sleep-only or hardware-key lock falsely deferred coordinated reboot; after shutdown-scoped parsing was corrected, the expanded pinned Ubuntu VM contract showed that a same-boot timed-out generation could be prepared again and arm another reboot; after terminal generation handling passed the VM, exact resource:reboot/reboot-v1 capability publication was still absent while the matrix row remained untested."
      green_result: "The corrected probe parses real systemd inhibitor rows and defers only shutdown-covering block locks; the durable store makes timed-out generations terminal while preserving idempotence for live generations; the pinned Ubuntu VM proves an excluded maintenance window and a pipe-synchronized real shutdown inhibitor prepare no intent, deterministic same-boot timeout is observable and non-replayable, authenticated acknowledgement increments the durable attempt generation, one controlled VM reload changes the kernel boot ID, reconciliation completes the exact generation, the second Check is compliant, no loop is possible, and teardown succeeds; the exact reboot capability is advertised."
      broader_checks: ["go-test:./internal/agent/rebootstate ./internal/applicators/reboots ./cmd/remotr-agent ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-system-safety on pinned Ubuntu 24.04 amd64 with real shutdown inhibitor, durable timeout/no-loop, controlled reboot, changed boot ID, second Check, and teardown"]
      final_disposition: qualified
  - capability_id: hostsEntry
    backend: hosts-file
    contract_revision: hostsEntry-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: connectivity
    accepted_fields: ["address","aliases[]","authorizationGroup","canonicalHost","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","validation[].command[]"]
    composed_address: m4-network/managed-host
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-network-recovery"]
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 network VM passes the real hosts-file provider lifecycle, effective resolver observation, protected control-host preflight, independent connectivity rollback, authenticated acknowledgement, second Check, and teardown."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted hostsEntry/hosts-file resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","network-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The focused provider test showed Check inspected only the configured /etc/hosts line and falsely reported compliance when effective resolution disagreed; the fixture contract showed the generic Debian recovery VM did not execute the hosts provider on pinned Ubuntu; the first real Ubuntu run showed Go's shared resolver cache retained the prior address after atomic replacement; after switching to a fresh getent process, the recovery peer advertised the host LAN address that the isolated guest could not reach; after advertising the guest gateway, blackholing only the default route did not sever the directly connected control path."
      green_result: "Check uses an exact getent ahosts process boundary to compare fresh effective addresses with the desired address; the pinned Ubuntu VM exercises the registry seam against real /etc/hosts, preserves unrelated bytes and metadata, proves marked create/update/absent restoration, blocks mutation of the active Remotr control host, reports effective state, reaches a control peer through the guest's actual gateway, proves deterministic host-specific route plus DNS/firewall/profile rollback, authenticates acknowledgement, reports a compliant second Check, and tears down cleanly; the exact hostsEntry capability is advertised."
      broader_checks: ["go-test:./internal/applicators/hostsentries ./internal/resourceregistry ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-network-recovery on pinned Ubuntu 24.04 amd64 with real hosts lifecycle, four independent connectivity recoveries, authenticated acknowledgement, second Check, and teardown"]
      final_disposition: qualified
  - capability_id: dnsResolver
    backend: network-manager
    contract_revision: dnsResolver-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: connectivity
    accepted_fields: ["authorizationGroup","configured","dependsOn[]","effective","enforce","interface","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","provider","providerOptions.*.*","risk","searchDomains[]","servers[]","validation[].command[]"]
    composed_address: m4-network/managed-dns
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-network-recovery"]
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 network VM passes the exact NetworkManager DNS provider contract, configured/effective observation, control-path preflight, real checkpoint rollback, stable connection recovery, authenticated acknowledgement, second Check, and teardown."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted dnsResolver/network-manager resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","network-recovery","capability-advertisement"]
      phase: verified
      red_failure: "Focused provider tests exposed NetworkManager enforcement crossing into resolvectl, mutation without a durable checkpoint/control-path preflight, registry loss of state and sync context, non-transactional planning, hidden acknowledgement state, and untyped backend rejection. The first Ubuntu fixture lacked NetworkManager and a real provider contract; successive real runs exposed an unmanaged dummy device, a missing dummy link, Ubuntu's strict-unmanaged package policy, an incomplete per-device-only override, the wrong CheckpointCreate D-Bus signature, an invalid IPv6 search-domain mutation for disabled IPv6, the wrong effective search-domain field, and asynchronous checkpoint rollback reported before the prior connection was stable."
      green_result: "The registered NetworkManager provider observes and mutates only configured and effective DNS state through nmcli, assigns search domains to an active address family, rejects unsupported backends with a typed reason, resolves and routes the active Remotr control host before mutation, persists an exact device/connection recovery handle, creates a real NetworkManager checkpoint before mutation, rolls back on failure or timeout, waits for the restored profile, requires authenticated acknowledgement to destroy the checkpoint, reports redacted transaction outcomes, and returns transactional no-change on its compliant second Apply. The fresh pinned Ubuntu VM proves the real package policy exception, dummy-device lifecycle, address preservation, timed rollback to prior DNS, four independent connectivity recoveries, authenticated sync, and complete teardown."
      broader_checks: ["go-test:./internal/applicators/networkresources ./internal/resourceregistry ./internal/executor ./internal/agent/networkstate ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification", "make:provider-matrix-vm-network-recovery on a fresh pinned Ubuntu 24.04 amd64 guest with real NetworkManager checkpoint create/rollback/destroy, stable reconnection, configured/effective second Check, four connectivity recoveries, authenticated acknowledgement, and teardown"]
      final_disposition: qualified
  - capability_id: route
    backend: network-manager
    contract_revision: route-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: connectivity
    accepted_fields: ["authorizationGroup","configured","dependsOn[]","destination","effective","enforce","gateway","interface","kind","lifecycle","lockDomains[]","metric","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","provider","providerOptions.*.*","risk","table","validation[].command[]"]
    composed_address: m4-network/managed-route
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-network-recovery"]
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 network VM passes the exact NetworkManager route provider contract, configured/effective observation, protected-control-path validation, real checkpoint rollback, stable connection recovery, authenticated acknowledgement, second Check, and teardown."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted route/network-manager resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","network-recovery","capability-advertisement"]
      phase: verified
      red_failure: "Focused provider tests exposed untyped backend rejection, raw ip-route mutation instead of the declared NetworkManager backend, mutation without durable checkpoint/control-path preflight, registry loss of state and sync context, non-transactional planning, and substring route identity that falsely treated different destinations and metrics as compliant. The initial VM fixture did not run the registered route provider; real Ubuntu runs then exposed absent custom FIB tables as observation errors, an invalid comma before NetworkManager's table route attribute, and configured/kernel table representations that caused false post-Apply drift."
      green_result: "The registered NetworkManager route provider observes and mutates configured and effective route scopes through exact structured identities, queries all kernel tables, rejects unsupported backends and protected control destinations before mutation, resolves and routes the active Remotr control host, persists the exact connection/interface recovery handle, creates a native checkpoint before mutation, rolls back on failure or timeout with stable reconnection, and requires authenticated acknowledgement to destroy the checkpoint. The fresh pinned Ubuntu VM proves exact table, metric, gateway, interface, and destination convergence, address preservation, timed rollback to the prior route, effective-only scope isolation, authenticated reporting without secrets, a transactional no-change second Apply, four independent connectivity recoveries, and complete teardown."
      broader_checks: ["go-test:./internal/applicators/networkresources ./internal/resourceregistry ./internal/agent/networkstate ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification ./internal/traceability", "make:provider-matrix-vm-network-recovery on a fresh pinned Ubuntu 24.04 amd64 guest with real NetworkManager route Apply/rollback/reapply/checkpoint destroy, stable reconnection, configured/effective second Check, four connectivity recoveries, authenticated acknowledgement, and teardown"]
      final_disposition: qualified
  - capability_id: networkProfile
    backend: network-manager
    contract_revision: networkProfile-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: connectivity
    accepted_fields: ["addresses[]","audit","authorizationGroup","autoConnect","credentialRef","dependsOn[]","enforce","ipv4Method","ipv6Method","kind","lifecycle","lockDomains[]","mtu","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","profileName","profileType","provider","providerOptions.*.*","risk","rollbackTimeout","selector.name","selector.permanentMAC","selector.type","ssid","validation[].command[]"]
    composed_address: m4-network/managed-profile
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-network-recovery"]
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 network VM passes the exact NetworkManager profile contract, configured/effective observation, exact selector, guarded activation, real checkpoint rollback, stable fallback reconnection, authenticated acknowledgement, second Check, and teardown."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted networkProfile/network-manager resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","network-recovery","capability-advertisement"]
      phase: verified
      red_failure: "Focused tests exposed the invalid NetworkManager CheckpointCreate D-Bus signature, missing durable interface/original-connection recovery identity, and acknowledgement state absent from Check reports. The initial Ubuntu provider fixture was missing; real runs then exposed a fixture peer name beyond Linux's 15-byte interface bound, profile observation that requested settings from both Ethernet and Wi-Fi types, and Ubuntu NetworkManager 1.46 rejecting absent user.data as an invalid selected field. Probe failures initially discarded the exact bounded backend diagnostic."
      green_result: "The registered NetworkManager provider rejects ambiguous selectors and unauthorized enforcement before mutation, observes exact configured/effective state with profile-type-specific fields, isolates credential fingerprint metadata without requesting --show-secrets, retains bounded diagnostics, creates a valid native checkpoint before activation, persists the exact interface and prior connection, reconnects the stable fallback after timeout rollback, reports authenticated acknowledgement without credential material, and returns transactional no-change after its compliant second Check. The fresh pinned Ubuntu VM proves real Ethernet profile drift, activation, fallback preservation, timeout rollback, reapply, checkpoint destruction, four independent connectivity recoveries, and teardown. Credential-bearing Netplan enforcement and deferred systemd-networkd device classes remain explicitly unadvertised."
      broader_checks: ["go-test:./internal/applicators/networkmanager ./internal/agent/networkstate ./internal/resourceregistry ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification ./internal/traceability", "make:provider-matrix-vm-network-recovery on a fresh pinned Ubuntu 24.04 amd64 guest with real NetworkManager profile activation/checkpoint rollback/stable fallback reactivation/reapply/destroy, configured/effective second Check, four connectivity recoveries, authenticated acknowledgement, and teardown"]
      final_disposition: qualified
  - capability_id: networkProfile
    backend: netplan
    contract_revision: networkProfile-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: connectivity
    accepted_fields: ["addresses[]","audit","authorizationGroup","autoConnect","credentialRef","dependsOn[]","enforce","ipv4Method","ipv6Method","kind","lifecycle","lockDomains[]","mtu","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","profileName","profileType","provider","providerOptions.*.*","risk","rollbackTimeout","selector.name","selector.permanentMAC","selector.type","ssid","validation[].command[]"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "Credential-bearing file-backed Netplan enforcement lacks the required activation and rollback qualification."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for networkProfile/netplan and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: networkProfile
    backend: systemd-networkd
    contract_revision: networkProfile-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: connectivity
    accepted_fields: ["addresses[]","audit","authorizationGroup","autoConnect","credentialRef","dependsOn[]","enforce","ipv4Method","ipv6Method","kind","lifecycle","lockDomains[]","mtu","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","profileName","profileType","provider","providerOptions.*.*","risk","rollbackTimeout","selector.name","selector.permanentMAC","selector.type","ssid","validation[].command[]"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "systemd-networkd device classes and credential-bearing enforcement remain deferred."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for networkProfile/systemd-networkd and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: firewall
    backend: nftables-audit
    contract_revision: firewall-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: firewall
    accepted_fields: ["action","audit","authorizationGroup","backend","chain","cleanupLimit","dependsOn[]","destinations[]","enforce","family","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","ports[]","preApplyValidation[]","protectRemotr","protocol","providerOptions.*.*","risk","rollbackTimeout","rule","rules[].action","rules[].destinations[]","rules[].name","rules[].ports[]","rules[].protocol","rules[].rule","rules[].services[]","rules[].sources[]","services[]","sources[]","table","validation[].command[]","zones[]"]
    composed_address: m4-network/nftables-audit
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-network-recovery"]
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 network VM passes the exact nftables audit contract as a persistent structured plan and proves Check, Apply, and second Check do not mutate packet-filter state."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A firewall/nftables-audit resource remains a structured audit plan across Check, Apply, and second Check without mutating packet-filter state."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","network-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The qualification gate showed that the pinned network-recovery VM did not execute the registered nftables audit provider, so unit-only structured-plan coverage could not satisfy the exact Ubuntu row."
      green_result: "The registered provider resolves only the endpoint-advertised nftables backend, returns a structured non-enforcing plan by default, writes only its mode-0600 audit record on Apply, remains an audit plan on second Check, and emits no nftables mutation command. The fresh pinned Ubuntu VM proves the real backend boundary alongside all four independent connectivity recoveries and complete teardown."
      broader_checks: ["go-test:./internal/applicators/firewall ./internal/agent/networkstate ./internal/resourceregistry ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification ./internal/traceability", "make:provider-matrix-vm-network-recovery on a fresh pinned Ubuntu 24.04 amd64 guest with registered nftables audit Check/Apply/second Check, zero packet-filter mutations, four connectivity recoveries, authenticated acknowledgement, and teardown"]
      final_disposition: qualified
  - capability_id: firewall
    backend: nftables-enforcement
    contract_revision: firewall-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: firewall
    accepted_fields: ["action","audit","authorizationGroup","backend","chain","cleanupLimit","dependsOn[]","destinations[]","enforce","family","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","ports[]","preApplyValidation[]","protectRemotr","protocol","providerOptions.*.*","risk","rollbackTimeout","rule","rules[].action","rules[].destinations[]","rules[].name","rules[].ports[]","rules[].protocol","rules[].rule","rules[].services[]","rules[].sources[]","services[]","sources[]","table","validation[].command[]","zones[]"]
    composed_address: m4-network/nftables-enforcement
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-network-recovery"]
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 network VM passes the exact guarded nftables enforcement contract, protected control-path planning, durable ruleset rollback across process reconstruction, authenticated acknowledgement, second Check, and teardown."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted firewall/nftables-enforcement resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","network-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The qualification gate showed that the pinned network-recovery VM did not execute the registered nftables enforcement provider or its durable timeout recovery, so focused transaction tests could not satisfy the exact Ubuntu row."
      green_result: "The registered nftables provider covers named-rule lifecycle plus owned fragment/authoritative rule-set semantics and bounded cleanup at its process boundary, rejects unavailable or mismatched backends, plans resolved control destinations, routes, ports, established traffic, and DNS dependencies before mutation, durably protects the exact prior ruleset, restores it after timeout in a reconstructed process, reapplies, records authenticated acknowledgement, and reports a compliant second Check. The fresh pinned Ubuntu VM also proves independent firewall connectivity recovery and teardown."
      broader_checks: ["go-test:./internal/applicators/firewall ./internal/agent/networkstate ./internal/resourceregistry ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification ./internal/traceability", "make:provider-matrix-vm-network-recovery on a fresh pinned Ubuntu 24.04 amd64 guest with registered nftables Apply/process-restart timeout rollback/reapply/authenticated acknowledgement/second Check, four connectivity recoveries, and teardown"]
      final_disposition: qualified
  - capability_id: firewall
    backend: firewalld-audit
    contract_revision: firewall-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: firewall
    accepted_fields: ["action","audit","authorizationGroup","backend","chain","cleanupLimit","dependsOn[]","destinations[]","enforce","family","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","ports[]","preApplyValidation[]","protectRemotr","protocol","providerOptions.*.*","risk","rollbackTimeout","rule","rules[].action","rules[].destinations[]","rules[].name","rules[].ports[]","rules[].protocol","rules[].rule","rules[].services[]","rules[].sources[]","services[]","sources[]","table","validation[].command[]","zones[]"]
    composed_address: m4-network/firewalld-audit
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-network-recovery"]
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 network VM passes the exact firewalld audit contract as a persistent structured plan and proves Check, Apply, and second Check do not mutate packet-filter state."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A firewall/firewalld-audit resource remains a structured audit plan across Check, Apply, and second Check without mutating packet-filter state."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","network-recovery","capability-advertisement"]
      phase: verified
      red_failure: "The qualification gate showed that the pinned network-recovery VM did not execute the registered firewalld audit provider. The first real run then proved capability gating correctly rejected the fixture because it declared nftables endpoint facts for the firewalld resource."
      green_result: "The exact firewalld audit fixture declares firewalld endpoint facts, reaches the registered provider without cross-backend fallback, returns a structured non-enforcing plan by default, writes only its mode-0600 audit record on Apply, remains an audit plan on second Check, and emits no firewalld mutation or reload command. The fresh pinned Ubuntu VM proves the real backend boundary alongside all four independent connectivity recoveries and complete teardown."
      broader_checks: ["go-test:./internal/applicators/firewall ./internal/resourceregistry ./internal/capabilitydoc ./internal/providermatrix ./internal/ubuntuqualification ./internal/traceability", "make:provider-matrix-vm-network-recovery on a fresh pinned Ubuntu 24.04 amd64 guest with exact firewalld capability facts, registered audit Check/Apply/second Check, zero packet-filter mutations, four connectivity recoveries, authenticated acknowledgement, and teardown"]
      final_disposition: qualified
  - capability_id: firewall
    backend: firewalld-enforcement
    contract_revision: firewall-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-network
    risk: firewall
    accepted_fields: ["action","audit","authorizationGroup","backend","chain","cleanupLimit","dependsOn[]","destinations[]","enforce","family","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","ports[]","preApplyValidation[]","protectRemotr","protocol","providerOptions.*.*","risk","rollbackTimeout","rule","rules[].action","rules[].destinations[]","rules[].name","rules[].ports[]","rules[].protocol","rules[].rule","rules[].services[]","rules[].sources[]","services[]","sources[]","table","validation[].command[]","zones[]"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "Firewalld enforcement lacks the exact Ubuntu rollback and authenticated recovery evidence."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for firewall/firewalld-enforcement and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: certificate
    backend: pem-files
    contract_revision: certificate-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: sensitive
    accepted_fields: ["authorizationGroup","certificateMode[]","certificatePath","certificateRef","chainRefs[]","dependsOn[]","enforce","fingerprint","group","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","owner","ownership","policy","preApplyValidation[]","privateKeyMode[]","privateKeyPath","privateKeyRef","providerOptions.*.*","renewBefore","renewalPolicy","risk","sans[]","subject","validation[].command[]"]
    composed_address: m5-security/managed-certificate
    governing_ids: ["OS-LSM-025","OS-LSM-026","OS-LSM-028","OS-LSM-031","OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102","OS-AEC-099"]
    selectors:
      - make:provider-matrix-vm-system-safety
      - go-test:./internal/resourceregistry:^TestResourceSafeSummaryProjectsClassifiedFieldsAndOmitsSecretCanary$
      - go-test:./cmd/remotr:^TestSecretUploadReadsProtectedInputFileAndNeverAcceptsArgvMaterial$
      - make:test-e2e
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 system-safety VM passes the registered PEM-file certificate lifecycle, purpose-scoped material resolution, policy and metadata observation, transactional activation/removal, protected secret recovery across reboot, preservation, cleanup, and second Checks."
    tdd:
      governing_id: OS-LSM-025
      public_seam: system-safety-recovery
      expected_result: "A drifted certificate/pem-files resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement","secret-canary"]
      phase: verified
      red_failure: "The system-safety selector ran direct certificate rollback recovery but no registered certificate provider lifecycle; it did not prove purpose-scoped public/key resolution, lifecycle removal, or the exact resource:certificate advertisement."
      green_result: "The registered provider resolves certificate-public and certificate-private-key material independently, installs a matching policy-compliant pair with exact root ownership and modes, reports only safe fingerprint/expiry metadata, preserves an unmanaged sibling, transactionally removes only the managed pair, and produces compliant second Checks. Across the controlled reboot, a reconstructed provider restores the exact prior canary-bearing key from encrypted rollback state, prevents replay, and permits recovery abandonment only through authorization."
      broader_checks: ["go-test:./internal/applicators/certificates ./internal/resourceregistry ./internal/providermatrix ./internal/ubuntuqualification ./internal/capabilitydoc ./internal/traceability", "make:provider-matrix-vm-system-safety on a fresh pinned Ubuntu 24.04 amd64 guest with registered certificate lifecycle, reboot reconstruction, exact secret rollback, authorized-only abandonment, second Checks, and teardown"]
      final_disposition: qualified
  - capability_id: trustAnchor
    backend: update-ca-certificates
    contract_revision: trustAnchor-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: sensitive
    accepted_fields: ["anchorRef","authorizationGroup","dependsOn[]","enforce","fingerprint","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","validation[].command[]"]
    composed_address: m5-security/managed-trust-anchor
    governing_ids: ["OS-LSM-027","OS-LSM-028","OS-LSM-031","OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102","OS-AEC-099"]
    selectors:
      - make:provider-matrix-vm-system-safety
      - go-test:./internal/resourceregistry:^TestResourceSafeSummaryProjectsClassifiedFieldsAndOmitsSecretCanary$
      - go-test:./cmd/remotr:^TestSecretUploadReadsProtectedInputFileAndNeverAcceptsArgvMaterial$
      - make:test-e2e
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 system-safety VM passes the registered named-anchor lifecycle, fingerprint gate, purpose-scoped resolution, coalesced native update-ca-certificates activation, effective OpenSSL trust, preservation, reconstructed rollback, and second Checks."
    tdd:
      governing_id: OS-LSM-027
      public_seam: system-safety-recovery
      expected_result: "A drifted trustAnchor/update-ca-certificates resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement","secret-canary"]
      phase: verified
      red_failure: "The system-safety selector did not build or execute a registered trustAnchor provider, so in-process lifecycle and refresh-coalescing tests could not prove the exact Ubuntu trust directory, native refresh, effective trust, reconstructed recovery, or capability advertisement."
      green_result: "The registered Ubuntu provider rejects a mismatched fingerprint before mutation, resolves only the ca-trust-anchor purpose, transactionally installs the named root under /usr/local/share/ca-certificates, emits a coalesced Debian trust refresh, passes native update-ca-certificates and OpenSSL verification, preserves an unrelated file, restores prior state through a reconstructed provider, removes only its named anchor, and reaches compliant second Checks."
      broader_checks: ["go-test:./internal/applicators/trustanchors ./internal/agent/engine ./internal/resourceregistry ./internal/providermatrix ./internal/ubuntuqualification ./internal/capabilitydoc ./internal/traceability", "make:provider-matrix-vm-system-safety on a fresh pinned Ubuntu 24.04 amd64 guest with registered trust-anchor lifecycle, native trust refresh and verification, reconstructed rollback, second Checks, the existing combined recovery suite, and teardown"]
      final_disposition: qualified
  - capability_id: appArmorProfile
    backend: apparmor-parser
    contract_revision: appArmorProfile-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: kernel-security
    accepted_fields: ["authorizationGroup","content","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","mode","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","profile","providerOptions.*.*","risk","validation[].command[]"]
    composed_address: m5-security/apparmor-profile
    governing_ids: ["OS-LSM-003","OS-LSM-031","OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102","OS-AEC-099"]
    selectors:
      - make:provider-matrix-vm-system-safety
      - go-test:./internal/resourceregistry:^TestResourceSafeSummaryProjectsClassifiedFieldsAndOmitsSecretCanary$
      - go-test:./cmd/remotr:^TestSecretUploadReadsProtectedInputFileAndNeverAcceptsArgvMaterial$
      - make:test-e2e
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 system-safety VM passes the registered AppArmor provider against the native parser and kernel effective-state interface for enforce, complain, disabled, invalid-profile preservation, unrelated-profile preservation, re-enable, redacted diagnostics, and second Checks."
    tdd:
      governing_id: OS-LSM-003
      public_seam: system-safety-recovery
      expected_result: "A drifted appArmorProfile/apparmor-parser resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement","secret-canary"]
      phase: verified
      red_failure: "The system-safety selector only reported whether AppArmor appeared available and ran no registered appArmorProfile provider, so mocked parser tests could not prove staged native validation, loaded/effective modes, invalid-profile preservation, or capability advertisement."
      green_result: "The registered provider uses Ubuntu's real apparmor_parser and /sys/kernel/security/apparmor/profiles state, converges enforce, complain, disabled, and re-enabled modes, keeps the disable link consistent, rejects invalid staged content without changing the active profile or exposing its canary, preserves an unrelated loaded profile throughout, and reports compliant idempotent second Checks. The pinned guest fails closed if AppArmor kernel enforcement or effective state is unavailable."
      broader_checks: ["go-test:./internal/applicators/apparmor ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/ubuntuqualification ./internal/capabilitydoc ./internal/traceability", "make:provider-matrix-vm-system-safety on a fresh pinned Ubuntu 24.04 amd64 guest with native AppArmor parser/kernel state, all declared modes, invalid-profile preservation, unrelated-profile preservation, the existing combined recovery suite, and teardown"]
      final_disposition: qualified
  - capability_id: auditRules
    backend: auditd
    contract_revision: auditRules-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: kernel-security
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","rules[]","validation[].command[]"]
    composed_address: m5-security/audit-rules
    governing_ids: ["OS-LSM-004","OS-LSM-031","OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102","OS-AEC-099"]
    selectors:
      - make:provider-matrix-vm-system-safety
      - go-test:./internal/resourceregistry:^TestResourceSafeSummaryProjectsClassifiedFieldsAndOmitsSecretCanary$
      - go-test:./cmd/remotr:^TestSecretUploadReadsProtectedInputFileAndNeverAcceptsArgvMaterial$
      - make:test-e2e
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 system-safety VM passes the registered auditd fragment lifecycle, staged syscall validation, persistent and effective state, mutable loading, immutable next-boot reporting, failure preservation, unrelated-rule preservation, cleanup, and second Checks."
    tdd:
      governing_id: OS-LSM-004
      public_seam: system-safety-recovery
      expected_result: "A drifted auditRules/auditd resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement","secret-canary"]
      phase: verified
      red_failure: "The system-safety selector ran no registered auditRules provider. The first real Ubuntu run then proved that augenrules --check only detected merge changes: an invalid staged syscall replaced the persistent fragment and failed later during live load. A focused public-provider regression reproduced that unsafe success before production correction."
      green_result: "The provider now resolves every declared staged syscall through the native ausyscall lookup before persistent replacement or live load, while retaining augenrules merge checking. The fresh pinned VM proves registered mutable lifecycle, exact persistent and auditctl-loaded state, canary-safe invalid-syscall rejection before mutation, non-destructive immutable-mode persistence with reboot-required reporting, restoration, named removal, unrelated-rule preservation, idempotence, and compliant second Checks."
      broader_checks: ["go-test:./internal/applicators/auditrules ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/ubuntuqualification ./internal/capabilitydoc ./internal/traceability", "make:provider-matrix-vm-system-safety on a fresh pinned Ubuntu 24.04 amd64 guest with native auditctl, augenrules, and ausyscall boundaries, mutable lifecycle, invalid-stage preservation, immutable reporting, unrelated-rule preservation, the existing combined recovery suite, and teardown"]
      final_disposition: qualified
  - capability_id: accountLimit
    backend: pam-limits
    contract_revision: accountLimit-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-access
    risk: access
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","entries[].domain","entries[].item","entries[].type","entries[].value","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","validation[].command[]"]
    composed_address: m5-auth/account-limits
    governing_ids: ["OS-LIA-012","OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102","OS-AEC-099"]
    selectors:
      - make:provider-matrix-vm-user-safety
      - go-test:./internal/resourceregistry:^TestResourceSafeSummaryProjectsClassifiedFieldsAndOmitsSecretCanary$
      - go-test:./cmd/remotr:^TestSecretUploadReadsProtectedInputFileAndNeverAcceptsArgvMaterial$
      - make:test-e2e
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 user-safety VM passes the registered pam-limits provider for typed lifecycle, complete limits.conf and limits.d validation before mutation, native per-login limits, logout activation, transactional recovery, recovery-principal access, preservation, cleanup, and second Checks."
    tdd:
      governing_id: OS-LIA-012
      public_seam: system-safety-recovery
      expected_result: "A drifted accountLimit/pam-limits resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","access-recovery","capability-advertisement","secret-canary"]
      phase: verified
      red_failure: "The user-safety selector had no registered accountLimit provider fixture. Once added, the first corrected Ubuntu PAM run proved that Apply replaced the managed fragment even while a malformed sibling made the complete limits.d tree invalid; a focused provider regression reproduced that unsafe mutation. Native range boundary coverage then proved malformed UID ranges were also accepted before correction."
      green_result: "The provider now validates the complete effective limits.conf plus limits.d configuration in memory with its desired named fragment substituted or removed before rollback is armed or disk is mutated. The fresh pinned VM proves canary-safe invalid-tree rejection, documented native syntax compatibility, exact root-owned typed fragments, real pam_limits soft/hard session state for managed and recovery users, logout activation, reconstructed transactional rollback, reapply, named removal, unrelated recovery-fragment preservation, idempotence, and compliant second Checks."
      broader_checks: ["go-test:./internal/applicators/accountlimits ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/ubuntuqualification ./internal/capabilitydoc ./internal/traceability plus bounded account-limit parser fuzzing", "make:provider-matrix-vm-user-safety on a fresh pinned Ubuntu 24.04 amd64 guest with registered account-limit lifecycle, complete native configuration validation, secret-canary failure preservation, managed and recovery-user PAM sessions, reconstructed rollback, the existing access-provider suite, and teardown"]
      final_disposition: qualified
  - capability_id: loginPolicy
    backend: pam-auth-update
    contract_revision: loginPolicy-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-access
    risk: authentication
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","priority","provider","providerOptions.*.*","recoveryPrincipals[]","risk","rules[].arguments[]","rules[].control","rules[].module","rules[].section","validation[].command[]"]
    composed_address: m5-auth/login-policy
    governing_ids: ["OS-AEC-052","OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102","OS-AEC-099"]
    selectors:
      - make:provider-matrix-vm-login-policy-safety
      - go-test:./internal/resourceregistry:^TestResourceSafeSummaryProjectsClassifiedFieldsAndOmitsSecretCanary$
      - go-test:./cmd/remotr:^TestSecretUploadReadsProtectedInputFileAndNeverAcceptsArgvMaterial$
      - make:test-e2e
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 login-policy VM passes the registered pam-auth-update provider for supported password-quality, password-history, and lockout rules, safely rejects the unavailable pam_lastlog module, validates the complete active PAM tree, preserves recovery authentication, reconstructs rollback, removes only its owned profile, and passes second Checks."
    tdd:
      governing_id: OS-AEC-052
      public_seam: system-safety-recovery
      expected_result: "A drifted loginPolicy/pam-auth-update resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","access-recovery","capability-advertisement","secret-canary"]
      phase: verified
      red_failure: "The login-policy selector defaulted to Debian 12 and covered only a benign direct-provider session rule. The first pinned registered-provider VM proved that pam-auth-update silently accepted an unavailable authored pam_lastlog.so module. After module validation, native composition proved that Remotr omitted Password-Type and Account-Type headers, so the authored password-history rule never reached common-password. Complete-tree validation then contradicted the focused fixture by rejecting Noble's stock optional pam_lastlog hook; focused public-provider regressions captured each failure before correction."
      green_result: "The provider now resolves every authored module before rollback or mutation, validates required modules throughout the staged active PAM service tree while honoring native optional and ignored-module semantics, renders the required per-family pam-auth-update type headers, and rejects an unrepresentable mix of global and interactive-only session scopes. Fresh pinned VM evidence proves canary-safe pam_lastlog rejection on Noble, native pwquality/pwhistory/faillock composition, exact owned profile state, failed-login recovery authentication, reconstructed transactional rollback, reapply, named removal, unrelated recovery-profile preservation, idempotence, and compliant second Checks."
      broader_checks: ["go-test:./internal/models ./internal/applicators/loginpolicy ./internal/resourceregistry ./internal/providermatrix ./internal/ubuntuqualification ./internal/capabilitydoc ./internal/traceability plus 65,595 bounded canonical login-policy fuzz executions", "make:provider-matrix-vm-login-policy-safety on fresh pinned Ubuntu 24.04 amd64 guests with registered provider construction, native pam-auth-update composition, pamtester authentication, unsupported last-login rejection, stock optional-module compatibility, durable rollback, lifecycle removal, recovery preservation, and teardown"]
      final_disposition: qualified
  - capability_id: journald
    backend: systemd-journald
    contract_revision: journald-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: sensitive
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","forwardToConsole","forwardToKernelBuffer","forwardToSyslog","forwardToWall","kind","lifecycle","lockDomains[]","maxRetention","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","rateLimitBurst","rateLimitInterval","risk","runtimeMaxUseBytes","storage","systemMaxUseBytes","validation[].command[]"]
    composed_address: m5-logging/journald
    governing_ids: ["OS-AEC-053","OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102","OS-AEC-099"]
    selectors:
      - make:provider-matrix-vm-system-safety
      - go-test:./internal/resourceregistry:^TestResourceSafeSummaryProjectsClassifiedFieldsAndOmitsSecretCanary$
      - go-test:./cmd/remotr:^TestSecretUploadReadsProtectedInputFileAndNeverAcceptsArgvMaterial$
      - make:test-e2e
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 system-safety VM passes the registered systemd-journald provider for complete staged syntax validation, storage, retention, disk, rate, and local-forwarding fields, native restart and local-record operation, transactional recovery, lifecycle removal, unrelated-drop-in preservation, and second Checks without claiming remote delivery health."
    tdd:
      governing_id: OS-AEC-053
      public_seam: system-safety-recovery
      expected_result: "A drifted journald/systemd-journald resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement","secret-canary"]
      phase: verified
      red_failure: "The system-safety selector ran no registered journald provider fixture. The first fresh Ubuntu VM run then proved that systemd-analyze cat-config only concatenated the staged tree: a malformed canary-bearing sibling was accepted and the managed drop-in changed. A focused ApplyResult regression reproduced that unsafe mutation before correction."
      green_result: "The provider now retains native systemd-analyze composition and additionally validates the complete staged main/drop-in tree for NUL bytes, section placement, and directive syntax before rollback or mutation. Fresh pinned VM evidence proves all declared fields, exact root-owned drop-in state, canary-safe malformed-tree rejection, native systemd-journald restart and local record retrieval, reconstructed transactional rollback, reapply, named removal, unrelated-drop-in preservation, idempotence, and compliant second Checks; it makes no remote forwarding-health claim."
      broader_checks: ["go-test:./internal/applicators/journald ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/ubuntuqualification ./internal/capabilitydoc ./internal/traceability plus 27,195 bounded canonical journald fuzz executions", "make:provider-matrix-vm-system-safety on a fresh pinned Ubuntu 24.04 amd64 guest with registered journald lifecycle, complete native and structural validation, secret-canary failure preservation, service restart, local record retrieval, reconstructed rollback, the existing recovery suite, controlled reboot, and teardown"]
      final_disposition: qualified
  - capability_id: logrotate
    backend: logrotate
    contract_revision: logrotate-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: sensitive
    accepted_fields: ["authorizationGroup","cadence","compress","create.group","create.mode","create.owner","dependsOn[]","enforce","firstAction.command[]","kind","lastAction.command[]","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","paths[]","policy","postRotate.command[]","preApplyValidation[]","preRotate.command[]","providerOptions.*.*","retention","risk","sharedScripts","validation[].command[]"]
    composed_address: m5-logging/logrotate
    governing_ids: ["OS-AEC-054","OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102","OS-AEC-099"]
    selectors:
      - make:provider-matrix-vm-system-safety
      - go-test:./internal/resourceregistry:^TestResourceSafeSummaryProjectsClassifiedFieldsAndOmitsSecretCanary$
      - go-test:./cmd/remotr:^TestSecretUploadReadsProtectedInputFileAndNeverAcceptsArgvMaterial$
      - make:test-e2e
    disposition: qualified
    reason: "The pinned Ubuntu 24.04 amd64 system-safety VM passes the registered logrotate provider for complete staged native validation, unmaterialized paths, cadence, retention, compression, create metadata, every script phase, real compressed rotation, secret-safe diagnostics, transactional recovery, lifecycle removal, unrelated-fragment preservation, and second Checks."
    tdd:
      governing_id: OS-AEC-054
      public_seam: system-safety-recovery
      expected_result: "A drifted logrotate/logrotate resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","system-recovery","capability-advertisement","secret-canary"]
      phase: verified
      red_failure: "The system-safety selector ran no registered logrotate provider fixture. The first fresh Ubuntu run then proved that native logrotate --debug rejected an otherwise valid desired fragment when its declared glob had no matching file yet. A focused ApplyResult regression captured the missing missingok directive. A separate byte-zero canary regression proved that the public failure diagnostic echoed content-bearing native stderr."
      green_result: "The provider now renders logrotate's standard missingok behavior so policy can safely precede its log producer, and native validation failures expose only a bounded generic diagnostic rather than content-bearing stderr. Fresh pinned VM evidence proves complete staged validation, all declared fields, exact root-owned fragment state, canary-safe failure preservation, real forced compressed rotation with root:adm create metadata and all four script markers, reconstructed transactional rollback, reapply, named removal, unrelated-fragment preservation, idempotence, and compliant second Checks."
      broader_checks: ["go-test:./internal/applicators/logrotate ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/ubuntuqualification ./internal/capabilitydoc ./internal/traceability plus 42,035 bounded canonical logrotate fuzz executions and the committed invalid-UTF-8 regression seed", "make:provider-matrix-vm-system-safety on a fresh pinned Ubuntu 24.04 amd64 guest with registered logrotate lifecycle, complete native validation, unmaterialized-path support, secret-canary failure preservation, forced rotation and scripts, reconstructed rollback, the existing recovery suite, controlled reboot, and teardown"]
      final_disposition: qualified
  - capability_id: desktopSetting
    backend: dconf
    contract_revision: desktopSetting-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-desktop
    risk: desktop-session
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","key","kind","level","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","path","policy","preApplyValidation[]","provider","providerOptions.*.*","risk","schema","scope","selector.mode","selector.usernames[]","validation[].command[]","value.type","value.value"]
    composed_address: m5-desktop/dconf-setting
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-desktop-session"]
    disposition: qualified
    reason: "The registered dconf contract passes the pinned Ubuntu 24.04 amd64 desktop VM for every native value type, logged-in and logged-out users, authoritative cleanup, effective system defaults and mandatory locks, unsafe-home isolation, preservation, idempotence, second Checks, and snapshot recovery."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted desktopSetting/dconf resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","desktop-session","capability-advertisement"]
      phase: verified
      red_failure: "The focused provider seam accepted a symlinked desktop state path and changed both users instead of failing the unsafe user while converging the later safe user. Canonical native forms for int32, integral double, and an empty string list did not round-trip. A mandatory-to-default transition ignored its stale lock. The first pinned VM then showed Remotr's self-reported compliant system policy was ineffective because it wrote an inactive remotr database; after selecting local.d, the second fresh VM still observed the user value because the minimal Ubuntu image had no profile consuming that database."
      green_result: "Per-user desktop execution now rejects unsafe home/config traversal before starting a provider, aggregates failures, and continues safe users. Native values use canonical GVariant forms. System dconf policy targets local.d, preserves and activates the user profile, treats stale locks as drift, and removes them on a mandatory-to-default transition. The fresh VM passes all seven native types, live and logged-out users, authoritative cleanup, effective mandatory locking, recovery after unsafe-home rejection, preservation, idempotence, compliant second Checks, snapshot restoration, and teardown."
      broader_checks: ["go-test:./internal/applicators/desktopsettings ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/capabilitydoc", "make:provider-matrix-vm-desktop-session on a fresh pinned Ubuntu 24.04 amd64 guest with registered desktopSetting/dconf and desktopSetting/gsettings providers, native effective-state assertions, snapshot restoration, and teardown"]
      final_disposition: qualified
  - capability_id: desktopSetting
    backend: gsettings
    contract_revision: desktopSetting-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-desktop
    risk: desktop-session
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","key","kind","level","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","path","policy","preApplyValidation[]","provider","providerOptions.*.*","risk","schema","scope","selector.mode","selector.usernames[]","validation[].command[]","value.type","value.value"]
    composed_address: m5-desktop/gsettings-setting
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-desktop-session"]
    disposition: qualified
    reason: "The registered GSettings contract passes the pinned Ubuntu 24.04 amd64 desktop VM for every native value type, logged-in and logged-out users, authoritative cleanup, unsafe-home isolation, preservation, idempotence, second Checks, and snapshot recovery."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted desktopSetting/gsettings resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","desktop-session","capability-advertisement"]
      phase: verified
      red_failure: "The focused provider seam accepted a symlinked desktop state path and changed both users instead of failing the unsafe user while converging the later safe user. Canonical native forms for int32, integral double, and an empty string list did not round-trip, so native GSettings output could remain drifted after Apply."
      green_result: "Per-user desktop execution now rejects unsafe home/config traversal before starting GSettings, aggregates failures, and continues safe users. All seven advertised native types use canonical GVariant forms. The fresh VM passes those types for live and logged-out users, authoritative reset cleanup, recovery after unsafe-home rejection, preservation, idempotence, compliant second Checks, snapshot restoration, and teardown."
      broader_checks: ["go-test:./internal/applicators/desktopsettings ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/capabilitydoc", "make:provider-matrix-vm-desktop-session on a fresh pinned Ubuntu 24.04 amd64 guest with registered desktopSetting/dconf and desktopSetting/gsettings providers, native effective-state assertions, snapshot restoration, and teardown"]
      final_disposition: qualified
  - capability_id: sessionPolicy
    backend: dconf
    contract_revision: sessionPolicy-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-desktop
    risk: desktop-session
    accepted_fields: ["authorizationGroup","defaultApplications.*","dependsOn[]","disableCommandLine","disableLogout","disableUserSwitching","enforce","idleTimeoutSeconds","kind","lifecycle","lockDelaySeconds","lockDomains[]","lockEnabled","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","provider","providerOptions.*.*","proxy.automaticUrl","proxy.httpHost","proxy.httpPort","proxy.httpsHost","proxy.httpsPort","proxy.ignoreHosts[]","proxy.mode","risk","selector.mode","selector.usernames[]","trustAnchors[]","validation[].command[]"]
    composed_address: m5-desktop/dconf-session-policy
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-desktop-session"]
    disposition: qualified
    reason: "The registered dconf session contract passes the pinned Ubuntu 24.04 amd64 desktop VM for supported fields, merge-only default applications, activation, multi-user execution, cleanup, unsafe-home recovery, preservation, idempotence, and second Checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted sessionPolicy/dconf resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","desktop-session","capability-advertisement"]
      phase: verified
      red_failure: "The provider always reported logout for default-application-only changes, omitted application restart targets from its plan, accepted an unsafe MIME state path, blocked later users after one failure, and used /org/gnome/system/proxy instead of Ubuntu's native /system/proxy dconf path."
      green_result: "Session changes now report only the activation classes actually changed, plan deduplicated application targets, reject unsafe state before native execution while continuing safe users and later fields, and use Ubuntu's native dconf proxy paths. The fresh VM passes supported fields, merge-only defaults, authoritative field cleanup, live/logged-out users, recovery, preservation, idempotence, and second Checks."
      broader_checks: ["go-test:./internal/applicators/sessionpolicy ./internal/applicators/desktopsettings ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/capabilitydoc", "make:provider-matrix-vm-desktop-session on a fresh pinned Ubuntu 24.04 amd64 guest with registered sessionPolicy/dconf and sessionPolicy/gsettings providers, native effective-state assertions, snapshot restoration, and teardown"]
      final_disposition: qualified
  - capability_id: sessionPolicy
    backend: gsettings
    contract_revision: sessionPolicy-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-desktop
    risk: desktop-session
    accepted_fields: ["authorizationGroup","defaultApplications.*","dependsOn[]","disableCommandLine","disableLogout","disableUserSwitching","enforce","idleTimeoutSeconds","kind","lifecycle","lockDelaySeconds","lockDomains[]","lockEnabled","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","provider","providerOptions.*.*","proxy.automaticUrl","proxy.httpHost","proxy.httpPort","proxy.httpsHost","proxy.httpsPort","proxy.ignoreHosts[]","proxy.mode","risk","selector.mode","selector.usernames[]","trustAnchors[]","validation[].command[]"]
    composed_address: m5-desktop/gsettings-session-policy
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-desktop-session"]
    disposition: qualified
    reason: "The registered GSettings session contract passes the pinned Ubuntu 24.04 amd64 desktop VM for supported fields, merge-only default applications, activation, multi-user execution, cleanup, unsafe-home recovery, preservation, idempotence, and second Checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted sessionPolicy/gsettings resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","desktop-session","capability-advertisement"]
      phase: verified
      red_failure: "The provider always reported logout for default-application-only changes, omitted application restart targets from its plan, accepted an unsafe MIME state path, and blocked later users and later fields after one selected user failed."
      green_result: "Session changes now report only the activation classes actually changed, plan deduplicated application targets, and reject unsafe state before native execution while continuing safe users and later fields. The fresh VM passes supported fields, merge-only defaults, authoritative field cleanup, live/logged-out users, recovery, preservation, idempotence, and second Checks."
      broader_checks: ["go-test:./internal/applicators/sessionpolicy ./internal/applicators/desktopsettings ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/capabilitydoc", "make:provider-matrix-vm-desktop-session on a fresh pinned Ubuntu 24.04 amd64 guest with registered sessionPolicy/dconf and sessionPolicy/gsettings providers, native effective-state assertions, snapshot restoration, and teardown"]
      final_disposition: qualified
  - capability_id: browserPolicy
    backend: chromium
    contract_revision: browserPolicy-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-desktop
    risk: desktop-session
    accepted_fields: ["authorizationGroup","browser","dependsOn[]","enforce","kind","level","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","policyName","preApplyValidation[]","providerOptions.*.*","risk","scope","trustAnchors[]","validation[].command[]","value.type","value.value"]
    composed_address: m5-desktop/chromium-policy
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-desktop-session"]
    disposition: qualified
    reason: "The registered Chromium contract passes the pinned Ubuntu VM for its exact native path, allowlisted typed policies, mandatory and supported recommended levels, lifecycle, sibling preservation, restart activation, transactional classification, idempotence, and second Checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted browserPolicy/chromium resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","desktop-session","capability-advertisement"]
      phase: verified
      red_failure: "Chromium-family present convergence replaced the whole JSON document and absent lifecycle deleted it, erasing unrelated policy keys at the same native path."
      green_result: "Chromium-family writes now merge and remove only the declared key. The fresh VM passes every allowlisted mandatory policy and every explicitly supported recommended policy with native types, sibling preservation, lifecycle, restart activation, idempotence, second Checks, snapshot recovery, and teardown."
      broader_checks: ["go-test:./internal/applicators/browserpolicy ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/capabilitydoc", "make:provider-matrix-vm-desktop-session on a fresh pinned Ubuntu 24.04 amd64 guest with 54 registered browser policy/level cases, snapshot restoration, and teardown"]
      final_disposition: qualified
  - capability_id: browserPolicy
    backend: google-chrome
    contract_revision: browserPolicy-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-desktop
    risk: desktop-session
    accepted_fields: ["authorizationGroup","browser","dependsOn[]","enforce","kind","level","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","policyName","preApplyValidation[]","providerOptions.*.*","risk","scope","trustAnchors[]","validation[].command[]","value.type","value.value"]
    composed_address: m5-desktop/chrome-policy
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-desktop-session"]
    disposition: qualified
    reason: "The registered Google Chrome contract passes the pinned Ubuntu VM for its exact native path, allowlisted typed policies, mandatory and supported recommended levels, lifecycle, sibling preservation, restart activation, transactional classification, idempotence, and second Checks."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted browserPolicy/google-chrome resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","desktop-session","capability-advertisement"]
      phase: verified
      red_failure: "Chromium-family present convergence replaced the whole JSON document and absent lifecycle deleted it, erasing unrelated policy keys at the same native path."
      green_result: "Chromium-family writes now merge and remove only the declared key. The fresh VM passes every allowlisted mandatory policy and every explicitly supported recommended policy with native types, sibling preservation, lifecycle, restart activation, idempotence, second Checks, snapshot recovery, and teardown."
      broader_checks: ["go-test:./internal/applicators/browserpolicy ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/capabilitydoc", "make:provider-matrix-vm-desktop-session on a fresh pinned Ubuntu 24.04 amd64 guest with 54 registered browser policy/level cases, snapshot restoration, and teardown"]
      final_disposition: qualified
  - capability_id: browserPolicy
    backend: firefox
    contract_revision: browserPolicy-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-desktop
    risk: desktop-session
    accepted_fields: ["authorizationGroup","browser","dependsOn[]","enforce","kind","level","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","policyName","preApplyValidation[]","providerOptions.*.*","risk","scope","trustAnchors[]","validation[].command[]","value.type","value.value"]
    composed_address: m5-desktop/firefox-policy
    governing_ids: ["OS-AEC-093","OS-AEC-096","OS-AEC-098","OS-AEC-102"]
    selectors: ["make:provider-matrix-vm-desktop-session"]
    disposition: qualified
    reason: "The registered Firefox contract passes the pinned Ubuntu VM for its exact native path, allowlisted mandatory typed policies, lifecycle, sibling preservation, restart activation, transactional classification, idempotence, and second Checks; recommended Firefox policy remains unadvertised."
    tdd:
      governing_id: OS-AEC-098
      public_seam: system-safety-recovery
      expected_result: "A drifted browserPolicy/firefox resource converges to its declared state, preserves undeclared state, and reports a compliant second Check with its required recovery evidence."
      evidence_layers: ["qualification-manifest","provider-contract","ubuntu-24.04-vm","desktop-session","capability-advertisement"]
      phase: verified
      red_failure: "The exact browser capability remained absent until native VM evidence covered Firefox separately from its Chromium-family siblings."
      green_result: "The fresh VM passes every allowlisted mandatory Firefox policy with native types, shared-document sibling preservation, lifecycle, restart activation, idempotence, second Checks, snapshot recovery, and teardown. Recommended Firefox policy remains unsupported and unadvertised."
      broader_checks: ["go-test:./internal/applicators/browserpolicy ./internal/models ./internal/resourceregistry ./internal/providermatrix ./internal/capabilitydoc", "make:provider-matrix-vm-desktop-session on a fresh pinned Ubuntu 24.04 amd64 guest with 54 registered browser policy/level cases, snapshot restoration, and teardown"]
      final_disposition: qualified
  - capability_id: bootstrap
    backend: one-shot
    contract_revision: bootstrap-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: boot
    accepted_fields: ["authorizationGroup","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","risk","steps[].exec[]","steps[].systemd.active","steps[].systemd.enabled","steps[].systemd.unit","validation[].command[]","when.pathExists","when.pathMissing"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "One-shot bootstrap execution is not a typed convergent Ubuntu applicator capability."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for bootstrap/one-shot and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: agentInstall
    backend: binary-install
    contract_revision: agentInstall-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: sensitive
    accepted_fields: ["artifactURL","authorizationGroup","dependsOn[]","enforce","enrollmentTokenSecret","extractDir","fleetURL","installBinary","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","present","providerOptions.*.*","risk","runningCheck.process","validation[].command[]","version"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096","OS-AEC-099"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "agentInstall is demand-specific upgrade behavior and not part of the typed Ubuntu M1-M5 qualification baseline."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for agentInstall/binary-install and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
  - capability_id: command
    backend: argv
    contract_revision: command-v1
    distribution: ubuntu
    release: "24.04"
    architecture: amd64
    environment: vm-system
    risk: destructive
    accepted_fields: ["apply[]","authorizationGroup","check[]","dependsOn[]","enforce","kind","lifecycle","lockDomains[]","name","notifications[].target","notifications[].type","ownership","policy","preApplyValidation[]","providerOptions.*.*","revert[]","risk","validation[].command[]"]
    composed_address: null
    governing_ids: ["OS-AEC-093","OS-AEC-094","OS-AEC-096"]
    selectors: ["go-test:./internal/ubuntuqualification:^TestUnadvertisedContractsHaveExplicitReasons$"]
    disposition: unadvertised
    reason: "Generic command execution cannot stand in for a typed Ubuntu provider contract."
    tdd:
      governing_id: OS-AEC-093
      public_seam: provider-contract
      expected_result: "Qualification rejects support advertisement for command/argv and executes no provider mutation."
      evidence_layers: ["qualification-manifest","provider-contract"]
      phase: not-applicable
      red_failure: null
      green_result: null
      broader_checks: []
      final_disposition: unadvertised
