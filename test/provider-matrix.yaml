version: 1
rows:
  # Image/backend discovery is planned evidence, never a support claim.
  - {provider: package, distribution: debian, release: "12", architecture: amd64, backend: apt, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: filesystem, distribution: debian, release: "12", architecture: amd64, backend: posix, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: identity, distribution: debian, release: "12", architecture: amd64, backend: shadow, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: service, distribution: debian, release: "12", architecture: amd64, backend: systemd, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: repository, distribution: debian, release: "12", architecture: amd64, backend: apt, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: package, distribution: ubuntu, release: "24.04", architecture: amd64, backend: apt, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: filesystem, distribution: ubuntu, release: "24.04", architecture: amd64, backend: posix, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: identity, distribution: ubuntu, release: "24.04", architecture: amd64, backend: shadow, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: service, distribution: ubuntu, release: "24.04", architecture: amd64, backend: systemd, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: repository, distribution: ubuntu, release: "24.04", architecture: amd64, backend: apt, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: package, distribution: arch, release: "2026-07-06", architecture: amd64, backend: pacman, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: filesystem, distribution: arch, release: "2026-07-06", architecture: amd64, backend: posix, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: identity, distribution: arch, release: "2026-07-06", architecture: amd64, backend: shadow, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: service, distribution: arch, release: "2026-07-06", architecture: amd64, backend: systemd, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
  - {provider: repository, distribution: arch, release: "2026-07-06", architecture: amd64, backend: pacman, contract_revision: v1, environment: container, status: untested, selectors: [make:provider-matrix-containers]}
