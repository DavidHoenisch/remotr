kind: module
schemaVersion: 1
configurations:
  - name: internal-cli
    description: Example custom app from the server catalog
    resources:
      - kind: package
        name: internal/mycli
        lifecycle: present
        version: "1.0.0"
        packageManager: remotr
