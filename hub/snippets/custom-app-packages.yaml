configurations:
  - name: internal-cli
    description: Example custom app from the server catalog
    customApps:
      - name: mycli
        package: internal/mycli
        version: "1.0.0"
        present: true
