configurations:
  - name: e2e-test-cli
    description: custom app package installed from MinIO via presigned URL
    targetDistros:
      - Debian
      - Arch
    packages:
      - name: e2e/test-cli
        version: "1.0.0"
        present: true
        packageManager: remotr
