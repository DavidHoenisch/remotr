kind: module
schemaVersion: 1
configurations:
  - name: base-packages
    description: packages shared by e2e fleet
    targetDistros:
      - Debian
      - Arch
    resources:
      - kind: package
        name: curl
        present: true
