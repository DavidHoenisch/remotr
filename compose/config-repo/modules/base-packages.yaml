kind: module
schemaVersion: 1
configurations:
  - name: base-packages
    description: packages shared by e2e fleet
    targetDistros:
      - Debian
      - Arch
    targetArch:
      - x86
    resources:
      - kind: package
        name: curl
        lifecycle: present
        present: true
